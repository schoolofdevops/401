---
phase: 03-day-2-modules
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - course-site/docs/module-05a-structured-coding/_category_.json
  - course-site/docs/module-05a-structured-coding/README.mdx
  - course-site/docs/module-05a-structured-coding/lab/_category_.json
  - course-site/docs/module-05a-structured-coding/lab/LAB-track-a-helm.mdx
  - course-site/docs/module-05a-structured-coding/lab/LAB-track-b-cicd.mdx
autonomous: true
requirements:
  - MOD5-01
  - MOD5-02

must_haves:
  truths:
    - "Participant can follow Track A lab to build a production Helm chart for the reference app via the 5-phase structured AI workflow"
    - "Participant can follow Track B lab to build a CI/CD pipeline for the reference app via the 5-phase structured AI workflow"
    - "Each lab step has an Expected result block so participants know if they succeeded"
    - "Labs use guided generation — specific context/prompts to feed Claude Code at each step"
    - "Solution artifacts are shown for comparison at the end of each phase"
  artifacts:
    - path: "course-site/docs/module-05a-structured-coding/lab/LAB-track-a-helm.mdx"
      provides: "Track A Helm chart structured AI coding lab"
      contains: "Expected result"
    - path: "course-site/docs/module-05a-structured-coding/lab/LAB-track-b-cicd.mdx"
      provides: "Track B CI/CD pipeline structured AI coding lab"
      contains: "Expected result"
    - path: "course-site/docs/module-05a-structured-coding/_category_.json"
      provides: "Docusaurus sidebar category for Module 5a"
    - path: "course-site/docs/module-05a-structured-coding/README.mdx"
      provides: "Module 5a overview with learning objectives"
  key_links:
    - from: "course-site/docs/module-05a-structured-coding/lab/LAB-track-a-helm.mdx"
      to: "reference-app/helm/reference-app/"
      via: "lab references existing Helm chart as baseline"
      pattern: "reference-app/helm"
    - from: "course-site/docs/module-05a-structured-coding/lab/LAB-track-b-cicd.mdx"
      to: ".github/workflows/ci.yml"
      via: "lab references existing CI/CD pipeline as baseline"
      pattern: "ci.yml"
---

<objective>
Build Module 5a: Structured AI Coding labs — Track A (Helm chart) and Track B (CI/CD pipeline).

Purpose: These two labs teach participants how to use a structured 5-phase AI workflow (Brainstorm, Design, Blueprint, Implement, Validate) to build real infrastructure artifacts. Participants pick ONE track. Both use the reference app from Phase 1 as the target system.

Output: Two complete LAB MDX files with guided generation prompts, expected results at every step, and solution artifacts for comparison. Plus the Docusaurus scaffolding (_category_.json, README.mdx).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-day-2-modules/03-CONTEXT.md
@.planning/phases/03-day-2-modules/03-RESEARCH.md
@course-site/docs/module-01-foundations/README.mdx
@course-site/docs/module-01-foundations/_category_.json
@course-site/docs/module-01-foundations/lab/LAB.mdx
@reference-app/helm/reference-app/Chart.yaml
@reference-app/helm/reference-app/values.yaml
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Module 5a Docusaurus scaffolding + Track A Helm lab</name>
  <files>
    course-site/docs/module-05a-structured-coding/_category_.json,
    course-site/docs/module-05a-structured-coding/README.mdx,
    course-site/docs/module-05a-structured-coding/lab/_category_.json,
    course-site/docs/module-05a-structured-coding/lab/LAB-track-a-helm.mdx
  </files>
  <read_first>
    - course-site/docs/module-01-foundations/_category_.json (pattern for _category_.json)
    - course-site/docs/module-01-foundations/README.mdx (pattern for module README)
    - course-site/docs/module-01-foundations/lab/_category_.json (pattern for lab _category_.json)
    - course-site/docs/module-01-foundations/lab/LAB.mdx (pattern for lab MDX — frontmatter, structure, details blocks)
    - reference-app/helm/reference-app/Chart.yaml (baseline chart metadata)
    - reference-app/helm/reference-app/values.yaml (baseline values — what exists)
    - reference-app/helm/reference-app/templates/ (list files to understand existing templates)
    - .planning/phases/03-day-2-modules/03-RESEARCH.md (lines 117-152 for Track A gaps: resource limits, HPA, PDB, ServiceMonitor, NOTES.txt)
    - .planning/phases/03-day-2-modules/03-CONTEXT.md (decisions D-32 through D-46)
  </read_first>
  <action>
    Create Module 5a Docusaurus directory structure and Track A lab.

    **_category_.json** (per D-32, Module 5a is "Structured AI Coding"):
    - label: "Module 5a: Structured AI Coding"
    - position: 5 (after module-04)
    - collapsed: false
    - link type: doc, id: "module-05a-structured-coding/module-05a-readme"

    **README.mdx** (per D-32, D-33):
    - id: module-05a-readme
    - title: "Module 5a: Structured AI Coding"
    - Duration: 90 minutes
    - Day: Day 2, Session 1
    - Explain: Participants choose ONE track (per D-33) — Track A (Helm chart) or Track B (CI/CD pipeline)
    - Learning objectives (4-5): structured AI workflow for IaC, gap analysis technique, guided AI generation with domain context, validation of AI-generated infrastructure artifacts
    - Prerequisites: Module 1-4 completed, Claude Code or Crush installed, KIND cluster running

    **lab/_category_.json**:
    - label: "Lab", position: 1

    **LAB-track-a-helm.mdx** (per D-34, D-45, D-46):
    - id: module-05a-lab-track-a
    - title: "Lab: Structured AI Coding — Track A (Helm Chart)"
    - sidebar_label: "Lab Track A: Helm"
    - sidebar_position: 1
    - Duration: 60 minutes
    - Deliverable: Production-quality Helm chart with HPA, PDB, ServiceMonitor, NOTES.txt

    **Lab structure follows the 5-phase workflow (per D-34):**

    **What You Need section:** Claude Code or Crush, KIND cluster with reference app deployed, existing Helm chart at reference-app/helm/reference-app/

    **Step 0: Baseline Analysis**
    - Participant examines the existing Helm chart (list what it already has per research: deployments, probes, resource requests, ConfigMap, NodePort)
    - Identify what is MISSING for production quality (per research: resource limits, HPA, PDB, ServiceMonitor, NOTES.txt, helm lint clean)
    - Expected result: A written gap list matching the 6 items above

    **Step 1: Brainstorm (Phase 1 of workflow)**
    - Provide the exact context to paste into Claude Code:
      ```
      I have a Helm chart for a microservices reference app (api-gateway on 8080, catalog on 8081, worker on 8082, dashboard). The chart already has Deployments with liveness/readiness probes and resource requests. I need to make it production-quality. What's missing?
      ```
    - Expected result: AI identifies HPA, PDB, resource limits, ServiceMonitor, NOTES.txt (should overlap with manual gap analysis)

    **Step 2: Design (Phase 2 of workflow)**
    - Provide the context to paste — include the 4-layer context model:
      Layer 1 (Task): "Add HPA, PDB, ServiceMonitor, resource limits, NOTES.txt to the reference app Helm chart"
      Layer 2 (Role): "I am an SRE building production Kubernetes infrastructure"
      Layer 3 (System): "KIND cluster running K8s 1.32, Helm 3.18, kube-prometheus-stack deployed in monitoring namespace, services: api-gateway (8080), catalog (8081), worker (8082)"
      Layer 4 (Procedure): "Use Helm 3 template syntax, Kubernetes 1.32 stable APIs only, no alpha/beta APIs"
    - Ask Claude Code to produce a design document listing each new template with its purpose
    - Expected result: Structured plan with 5 template files and their Kubernetes API versions

    **Step 3: Blueprint (Phase 3 of workflow)**
    - Provide the context to paste — ask Claude Code to generate starter skeleton files for each template (just the apiVersion, kind, metadata — no spec yet)
    - Expected result: 5 skeleton YAML files

    **Step 4: Implement (Phase 4 of workflow)**
    - Provide the context to paste — now ask Claude Code to fill in the production details for each template:
      - HPA: min 2, max 5, target CPU 70%, scaleDown stabilization 300s
      - PDB: minAvailable 1
      - ServiceMonitor: scrape /health/ready every 30s, namespace: monitoring, release: prometheus label
      - Resource limits: CPU 500m, memory 256Mi for all services
      - NOTES.txt: post-install instructions showing service URLs and how to check HPA status
    - Expected result: Complete template files with all fields populated

    **Step 5: Validate (Phase 5 of workflow)**
    - Run `helm lint reference-app/helm/reference-app/` — expected result: 0 errors
    - Run `helm template reference-app reference-app/helm/reference-app/` — expected result: valid YAML output including all new templates
    - Compare AI output against the solution shown below
    - Expected result: helm lint passes, template renders, output matches solution structure

    **Solution section:** Show the complete solution for each template file in collapsible `<details>` blocks. Include the HPA, PDB, ServiceMonitor, resource limits patch, and NOTES.txt.

    **Reflection section:** "What did the structured workflow give you?" — trace each decision back to a workflow step. This is the pedagogical payoff (per D-34).

    IMPORTANT: Use "context engineering" vocabulary throughout, never "prompt engineering" (per course convention). Every step must have an "Expected result:" block (per D-46). Use collapsible `<details>` blocks for long code outputs and solutions. Use MDX-safe syntax — no bare `<` characters in prose.
  </action>
  <verify>
    <automated>grep -c "Expected result" course-site/docs/module-05a-structured-coding/lab/LAB-track-a-helm.mdx</automated>
  </verify>
  <acceptance_criteria>
    - grep "Expected result" LAB-track-a-helm.mdx returns at least 6 matches (one per step + solution)
    - grep "sidebar_position" LAB-track-a-helm.mdx returns 1
    - grep "module-05a-readme" _category_.json returns 1
    - grep "Track A" README.mdx returns at least 1
    - grep "Track B" README.mdx returns at least 1
    - File LAB-track-a-helm.mdx contains "Brainstorm", "Design", "Blueprint", "Implement", "Validate"
    - No occurrence of "prompt engineering" in any file (grep returns 0)
    - grep "Deliverable:" LAB-track-a-helm.mdx returns at least 1
  </acceptance_criteria>
  <done>Module 5a Docusaurus scaffold exists with _category_.json, README.mdx, lab/_category_.json, and Track A Helm lab MDX with all 5 workflow phases, expected results at every step, guided generation prompts, and solution artifacts</done>
</task>

<task type="auto">
  <name>Task 2: Track B CI/CD pipeline lab</name>
  <files>
    course-site/docs/module-05a-structured-coding/lab/LAB-track-b-cicd.mdx
  </files>
  <read_first>
    - course-site/docs/module-05a-structured-coding/lab/LAB-track-a-helm.mdx (pattern consistency with Track A)
    - .github/workflows/ci.yml (baseline CI/CD pipeline — what exists)
    - .planning/phases/03-day-2-modules/03-RESEARCH.md (lines 154-173 for Track B gaps: matrix strategy, OIDC, env-specific deploy, metadata-action, job summary)
    - .planning/phases/03-day-2-modules/03-CONTEXT.md (D-35, D-45, D-46)
  </read_first>
  <action>
    Create Track B CI/CD pipeline lab following the same 5-phase structure as Track A.

    **LAB-track-b-cicd.mdx** (per D-35, D-45, D-46):
    - id: module-05a-lab-track-b
    - title: "Lab: Structured AI Coding — Track B (CI/CD Pipeline)"
    - sidebar_label: "Lab Track B: CI/CD"
    - sidebar_position: 2
    - Duration: 60 minutes
    - Deliverable: Production-quality GitHub Actions CI/CD pipeline with matrix testing, env-specific deploys, proper image tagging

    **Lab structure follows the same 5-phase workflow (per D-35):**

    **What You Need:** Claude Code or Crush, GitHub account (for Actions), existing CI pipeline at .github/workflows/ci.yml

    **Step 0: Baseline Analysis**
    - Participant examines existing .github/workflows/ci.yml
    - List what it already has (per research): cargo test, Docker builds, KIND cluster, Helm deploy, health verification
    - Identify what is MISSING for production quality (per research): matrix strategy for multi-version, OIDC-based AWS credentials, separate staging/production jobs, docker/metadata-action for image tagging, job output summary

    **Step 1: Brainstorm**
    - Provide exact context to paste into Claude Code:
      ```
      I have a GitHub Actions CI/CD pipeline for a Rust microservices app. It currently runs cargo test, builds Docker images, creates a KIND cluster, deploys via Helm, and checks health endpoints. I need to make it production-quality. What's missing?
      ```
    - Expected result: AI identifies matrix testing, OIDC, env-specific deploys, metadata-action, job summary

    **Step 2: Design**
    - Provide 4-layer context:
      Layer 1: "Add matrix testing, OIDC auth, staging/prod deploy jobs, metadata-action tagging, job summary to the CI/CD pipeline"
      Layer 2: "I am a DevOps engineer building production CI/CD for a Rust microservices app"
      Layer 3: "GitHub Actions, Docker, KIND, Helm 3.18, 3 services (api-gateway, catalog, worker), existing ci.yml"
      Layer 4: "GitHub Actions workflow syntax, use reusable workflows where possible, OIDC requires aws-actions/configure-aws-credentials@v4"
    - Ask Claude Code to produce a pipeline design document
    - Expected result: Structured plan with job names, dependency graph, environment matrix

    **Step 3: Blueprint**
    - Ask for skeleton workflow file with job names, triggers, needs dependencies — no step implementations yet
    - Expected result: Workflow skeleton with build-test, staging-deploy, production-deploy jobs

    **Step 4: Implement**
    - Provide context to fill in all steps:
      - matrix: strategy with Rust 1.75 + 1.78 (stable + MSRV)
      - docker/metadata-action for semantic image tags (sha-short, branch name, semver)
      - staging job: deploys to KIND cluster, runs smoke tests
      - production job: requires staging success + manual approval (environment: production)
      - Job summary with deployed image tags and health check results
    - Expected result: Complete workflow YAML with all jobs and steps

    **Step 5: Validate**
    - Validate YAML syntax (yamllint or actionlint if available)
    - Walk through the pipeline mentally: push → build matrix → staging → production
    - Compare against solution
    - Expected result: Valid YAML, logical job flow, solution match

    **Solution section:** Show the complete production pipeline in a collapsible `<details>` block.

    **Reflection section:** Same pattern as Track A — trace decisions to workflow steps.

    IMPORTANT: Match the format, tone, and structure of Track A exactly. Context engineering vocabulary. Expected result blocks at every step. Collapsible details for long code. MDX-safe syntax.
  </action>
  <verify>
    <automated>grep -c "Expected result" course-site/docs/module-05a-structured-coding/lab/LAB-track-b-cicd.mdx</automated>
  </verify>
  <acceptance_criteria>
    - grep "Expected result" LAB-track-b-cicd.mdx returns at least 6 matches
    - grep "sidebar_position: 2" LAB-track-b-cicd.mdx returns 1
    - File contains "Brainstorm", "Design", "Blueprint", "Implement", "Validate"
    - File contains "GitHub Actions" and "CI/CD"
    - No occurrence of "prompt engineering"
    - grep "Deliverable:" LAB-track-b-cicd.mdx returns at least 1
    - File contains "matrix" (matrix strategy for multi-version testing)
  </acceptance_criteria>
  <done>Track B CI/CD lab MDX complete with all 5 workflow phases, expected results at every step, guided generation prompts referencing the existing ci.yml baseline, and solution pipeline in details block</done>
</task>

</tasks>

<verification>
- Both lab files exist and render valid MDX (no bare < characters, proper frontmatter)
- Every lab step has an "Expected result:" block (per D-46)
- Labs use guided generation pattern — specific context provided at each step (per D-45)
- Track A references the existing Helm chart at reference-app/helm/reference-app/
- Track B references the existing CI/CD at .github/workflows/ci.yml
- "prompt engineering" does NOT appear in any file
- "context engineering" vocabulary is used throughout
- README.mdx explains the track choice (per D-33)
</verification>

<success_criteria>
- Module 5a directory exists in course-site/docs/ with proper Docusaurus structure
- Two complete lab tracks (Helm + CI/CD), each with 5-phase structured workflow
- Combined grep for "Expected result" across both files returns 12+ matches
- Both labs are self-contained and solo-completable (dual format constraint)
</success_criteria>

<output>
After completion, create `.planning/phases/03-day-2-modules/03-01-SUMMARY.md`
</output>
