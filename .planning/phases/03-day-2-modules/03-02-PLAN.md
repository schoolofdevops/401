---
phase: 03-day-2-modules
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - course-site/docs/module-05b-ai-workflows/_category_.json
  - course-site/docs/module-05b-ai-workflows/README.mdx
  - course-site/docs/module-05b-ai-workflows/lab/_category_.json
  - course-site/docs/module-05b-ai-workflows/lab/LAB.mdx
  - course-site/docs/module-05b-ai-workflows/exploratory/_category_.json
  - course-site/docs/module-05b-ai-workflows/exploratory/PROJECTS.mdx
autonomous: true
requirements:
  - MOD5-03
  - MOD5-04
  - MOD5-05
  - MOD5-06
  - MOD5-07

must_haves:
  truths:
    - "Participant can follow the GSD workflow lab to build a monitoring stack (Prometheus alerting rules + Grafana dashboard) for the reference app"
    - "Participant understands CLAUDE.md as system context and can create one for their project"
    - "Participant can configure cross-session memory (claude-mem for Claude Code, MCP for Crush)"
    - "Participant can use plan modes (Claude Code /plan, GSD plan-phase) and knows when to use each"
    - "Participant has exploratory content for superpowers (TDD, debugging, code review)"
    - "Each lab section has an Expected result block"
  artifacts:
    - path: "course-site/docs/module-05b-ai-workflows/lab/LAB.mdx"
      provides: "Module 5b AI Workflow Tools lab with 4 sections + exploratory"
      contains: "Expected result"
    - path: "course-site/docs/module-05b-ai-workflows/README.mdx"
      provides: "Module 5b overview with learning objectives"
    - path: "course-site/docs/module-05b-ai-workflows/exploratory/PROJECTS.mdx"
      provides: "Superpowers exploratory projects"
  key_links:
    - from: "course-site/docs/module-05b-ai-workflows/lab/LAB.mdx"
      to: "reference-app/"
      via: "GSD lab builds monitoring for reference app services"
      pattern: "api-gateway.*catalog.*worker"
    - from: "course-site/docs/module-05b-ai-workflows/lab/LAB.mdx"
      to: "infrastructure/helm/prometheus-lab-values.yaml"
      via: "GSD lab extends existing Prometheus config"
      pattern: "prometheus-lab-values"
---

<objective>
Build Module 5b: AI Workflow Tools — GSD workflow lab, context engineering practical, memory systems lab, plan modes lab, and superpowers exploratory.

Purpose: These labs teach the "meta-skills" for using AI tools effectively. The GSD lab is the most pedagogically valuable piece — participants experience the exact workflow used for the rest of the course. The other sections cover context engineering (CLAUDE.md), memory (claude-mem + Crush MCP), and plan modes. Superpowers (TDD, debugging, code review) are exploratory stretch content.

Output: One composite LAB.mdx with 4 sequential sections, exploratory PROJECTS.mdx, plus Docusaurus scaffolding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-day-2-modules/03-CONTEXT.md
@.planning/phases/03-day-2-modules/03-RESEARCH.md
@course-site/docs/module-01-foundations/README.mdx
@course-site/docs/module-01-foundations/_category_.json
@course-site/docs/module-01-foundations/lab/LAB.mdx
@infrastructure/helm/prometheus-lab-values.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Module 5b scaffolding + composite lab (GSD workflow, context engineering, memory, plan modes)</name>
  <files>
    course-site/docs/module-05b-ai-workflows/_category_.json,
    course-site/docs/module-05b-ai-workflows/README.mdx,
    course-site/docs/module-05b-ai-workflows/lab/_category_.json,
    course-site/docs/module-05b-ai-workflows/lab/LAB.mdx
  </files>
  <read_first>
    - course-site/docs/module-01-foundations/_category_.json (Docusaurus pattern)
    - course-site/docs/module-01-foundations/README.mdx (README pattern)
    - course-site/docs/module-01-foundations/lab/LAB.mdx (lab MDX pattern — frontmatter, details blocks, expected results)
    - infrastructure/helm/prometheus-lab-values.yaml (existing Prometheus config the GSD lab extends)
    - .planning/phases/03-day-2-modules/03-RESEARCH.md (lines 174-198 for GSD lab structure, lines 314-383 for Prometheus alerting rules, lines 386-435 for context engineering teaching, lines 438-487 for memory and plan modes)
    - .planning/phases/03-day-2-modules/03-CONTEXT.md (D-36 through D-40)
    - reference-app/services/api-gateway/src/main.rs (health endpoints the monitoring targets)
    - reference-app/services/catalog/src/main.rs (health endpoints)
    - reference-app/services/worker/src/main.rs (health endpoints)
  </read_first>
  <action>
    Create Module 5b directory structure and composite lab file.

    **_category_.json** (per D-32):
    - label: "Module 5b: AI Workflow Tools"
    - position: 6 (after module-05a at position 5)
    - collapsed: false
    - link type: doc, id: "module-05b-ai-workflows/module-05b-readme"

    **README.mdx** (per D-32):
    - id: module-05b-readme
    - title: "Module 5b: AI Workflow Tools"
    - Duration: 90 minutes
    - Day: Day 2, Session 2
    - Explain: This module teaches the meta-skills — how to use AI tools effectively for infrastructure work
    - Learning objectives (5): GSD structured workflow for multi-file IaC, CLAUDE.md as system context, cross-session memory systems, plan mode selection (quick vs structured), extending AI with disciplined workflows (superpowers)
    - Prerequisites: Module 1-4 completed, Claude Code or Crush installed, KIND cluster with Prometheus deployed

    **lab/_category_.json**: label: "Lab", position: 1

    **LAB.mdx** — the composite lab with 4 main sections (per D-36, D-37, D-38, D-39):

    Frontmatter:
    - id: module-05b-lab
    - title: "Lab: AI Workflow Tools"
    - sidebar_label: "Lab"
    - sidebar_position: 1
    - Duration: 75 minutes
    - Deliverable: A monitoring stack (Prometheus alerting rules + Grafana dashboard config) built via GSD workflow, a project CLAUDE.md, configured memory system, and a plan mode comparison

    ---

    **SECTION 1: GSD Workflow Lab (30 minutes)** (per D-36)

    This is the centerpiece. Participants run the full GSD cycle to build a monitoring stack for the reference app.

    Deliverable artifacts: `monitoring/alerting-rules.yaml` (PrometheusRule CRD) and `monitoring/grafana-dashboard.json`

    **Step 1: Initialize the GSD project**
    - Run `/gsd:new-project` in Claude Code
    - When asked for description, paste: "Add Prometheus alerting rules and a Grafana dashboard for our reference app. The app has three services: api-gateway (port 8080, /health/live, /health/ready, /api/status), catalog (port 8081, /health/live, /health/ready, /items), and worker (port 8082, /health/live, /health/ready, /events). Prometheus with kube-prometheus-stack is deployed in the monitoring namespace. Grafana is at NodePort 30090."
    - Expected result: GSD creates a .planning/ directory with PROJECT.md

    **Step 2: Discuss the phase**
    - Run `/gsd:discuss-phase 1`
    - When asked for requirements, specify:
      - 3 alerting rules: ApiGatewayDegraded (readiness failing >1m), CatalogServiceDown (readiness failing >2m), WorkerHeartbeatMissing (no activity >3m)
      - 1 Grafana dashboard with 3 panels: service health status, request latency (placeholder), pod restart count
      - Use blackbox exporter / health probe approach (no code changes to reference app)
      - Constraint: "Do not modify reference-app/ source code"
    - Expected result: CONTEXT.md with locked decisions

    **Step 3: Plan the phase**
    - Run `/gsd:plan-phase 1`
    - Review the generated plan — should produce 1-2 plans with tasks for alerting rules and dashboard
    - Expected result: PLAN.md with tasks targeting alerting-rules.yaml and grafana-dashboard.json

    **Step 4: Execute the phase**
    - Run `/gsd:execute-phase 1`
    - Claude Code generates the two files based on the plan
    - Expected result: `monitoring/alerting-rules.yaml` (PrometheusRule CRD with 3 rules) and `monitoring/grafana-dashboard.json` (Grafana JSON)

    Show the expected alerting-rules.yaml in a `<details>` block. Use the PrometheusRule CRD from research (apiVersion: monitoring.coreos.com/v1, kind: PrometheusRule, namespace: monitoring, labels: release: prometheus). Include the 3 rules: ApiGatewayDegraded (blackbox probe, for: 1m, severity: warning), CatalogServiceDown (blackbox probe, for: 2m, severity: critical), WorkerHeartbeatMissing (placeholder — note this requires postgres-exporter as a stretch exercise).

    Show the expected grafana-dashboard.json structure in a `<details>` block (Grafana provisioning JSON with 3 panels).

    **Step 5: Verify the work**
    - If Prometheus is running on KIND: `kubectl apply -f monitoring/alerting-rules.yaml` and check `kubectl get prometheusrule -n monitoring`
    - If Grafana is running: import the dashboard JSON via Grafana UI at localhost:30090
    - Expected result: PrometheusRule resource created successfully; Grafana dashboard importable

    **Reflection:** "You just used a structured AI harness to build production monitoring. Every decision is traceable to a workflow step."

    ---

    **SECTION 2: Context Engineering Practical (20 minutes)** (per D-37)

    **Step 1: Create a project CLAUDE.md**
    - Explain: CLAUDE.md is system context — it tells the AI tool what exists, what matters, and what to avoid
    - Provide the template participants should create:
      ```
      # Monitoring Stack for Reference App

      ## System State
      - KIND cluster: context "kind-lab"
      - Services: api-gateway (8080), catalog (8081), worker (8082)
      - Namespace: app (reference app), monitoring (Prometheus + Grafana)
      - Prometheus: NodePort 30091, Grafana: NodePort 30090

      ## Constraints
      - No paid services — all resources local or free-tier
      - Kubernetes 1.32, Helm 3.18, Prometheus Operator CRD v1
      - Do not modify reference-app/ source code

      ## Vocabulary
      - "alerting rules" = PrometheusRule CRD in monitoring namespace
      - "dashboard" = Grafana JSON provisioning file in configmap
      ```
    - Expected result: CLAUDE.md file exists in project root

    **Step 2: Before/After comparison**
    - WITHOUT CLAUDE.md: Ask Claude Code "Create a Prometheus alerting rule for high CPU" — observe generic output
    - WITH CLAUDE.md: Ask the same question — observe namespace-aware, version-specific, constraint-respecting output
    - Expected result: Visible quality difference; AI uses correct namespace, CRD version, and constraints

    **Step 3: Context window management**
    - Teach 3 patterns:
      1. Selective injection: `@CLAUDE.md` vs `@entire-repo` — show token count difference with `/cost`
      2. YOLO mode vs ask mode: When to let AI proceed (low risk) vs request approval (production changes)
      3. Session handoff: GSD STATE.md as a cross-session anchor
    - Expected result: Participant can explain when to use each pattern

    ---

    **SECTION 3: Memory Systems (15 minutes)** (per D-38)

    **Claude Code path: claude-mem**
    - Explain: claude-mem captures decisions and patterns across sessions automatically
    - Demo steps:
      1. Make a decision in Claude Code (e.g., "We chose blackbox exporter over native metrics")
      2. End the session
      3. Start a new session, ask "What monitoring approach did we choose?"
      4. claude-mem injects the prior decision automatically
    - Expected result: Cross-session memory retrieval demonstrated
    - Show claude-mem web UI at localhost:37777 (if available)

    **Crush path: MCP memory server**
    - Explain: Crush uses MCP protocol for memory — same concept, different mechanism
    - Show the `.crush.json` or `~/.config/crush/crush.json` config:
      ```json
      {
        "mcp": {
          "memory": {
            "type": "stdio",
            "command": "npx",
            "args": ["-y", "@modelcontextprotocol/server-memory"],
            "timeout": 120
          }
        }
      }
      ```
    - Expected result: Participant understands MCP memory configuration

    **Comparison table:** Show when each approach is better (claude-mem: auto-capture, richer search; MCP memory: works with any MCP-compatible tool, explicit store/retrieve)

    ---

    **SECTION 4: Plan Modes (10 minutes)** (per D-39)

    **Claude Code /plan**
    - Demo: Run `claude --plan "Add HPA to the api-gateway Helm template"` (or `/plan` in interactive mode)
    - Observe: Claude Code produces a structured markdown plan before executing
    - Expected result: Plan output visible, no execution yet

    **GSD /gsd:plan-phase**
    - Remind: "You already used this in Section 1 — that was the production-grade version"
    - Compare: /plan is for quick single tasks; GSD plan-phase is for multi-file production work

    **Comparison table:**
    | Mode | When | Output | Example |
    |------|------|--------|---------|
    | Claude Code /plan | Quick, single task | Plan in chat | "Add HPA to Helm chart" |
    | GSD /gsd:plan-phase | Multi-file, production work | PLAN.md with waves | "Build monitoring stack" |

    - Expected result: Participant can articulate when to use each mode

    ---

    IMPORTANT: Use "context engineering" vocabulary throughout, never "prompt engineering." Every section must have Expected result blocks (per D-46). Use collapsible details blocks for long code. MDX-safe syntax. The GSD lab is the PRIMARY teaching section — give it the most detail and the most guided generation prompts.
  </action>
  <verify>
    <automated>grep -c "Expected result" course-site/docs/module-05b-ai-workflows/lab/LAB.mdx</automated>
  </verify>
  <acceptance_criteria>
    - grep "Expected result" LAB.mdx returns at least 10 matches (multiple per section)
    - grep "GSD" LAB.mdx returns at least 5 (GSD is the centerpiece)
    - grep "CLAUDE.md" LAB.mdx returns at least 3 (context engineering practical)
    - grep "claude-mem" LAB.mdx returns at least 2 (memory section)
    - grep "crush" -i LAB.mdx returns at least 2 (Crush memory path)
    - grep "/plan" LAB.mdx returns at least 2 (plan modes section)
    - grep "PrometheusRule" LAB.mdx returns at least 1 (monitoring deliverable)
    - grep "module-05b-readme" _category_.json returns 1
    - No occurrence of "prompt engineering" in any file
    - grep "Deliverable:" LAB.mdx returns at least 1
  </acceptance_criteria>
  <done>Module 5b composite lab complete with 4 sections (GSD workflow 30min, context engineering 20min, memory systems 15min, plan modes 10min), all expected result blocks, guided generation prompts for GSD lab, and Docusaurus scaffolding</done>
</task>

<task type="auto">
  <name>Task 2: Superpowers exploratory content</name>
  <files>
    course-site/docs/module-05b-ai-workflows/exploratory/_category_.json,
    course-site/docs/module-05b-ai-workflows/exploratory/PROJECTS.mdx
  </files>
  <read_first>
    - course-site/docs/module-05b-ai-workflows/lab/LAB.mdx (just created — maintain tone consistency)
    - .planning/phases/03-day-2-modules/03-CONTEXT.md (D-40: superpowers as exploratory)
    - .planning/phases/03-day-2-modules/03-RESEARCH.md (lines 24: D-40 superpowers TDD, debugging, code review)
  </read_first>
  <action>
    Create the exploratory PROJECTS.mdx for Module 5b superpowers content (per D-40).

    **exploratory/_category_.json**: label: "Exploratory", position: 3

    **PROJECTS.mdx** (per D-40):
    - id: module-05b-exploratory
    - title: "Exploratory: AI Superpowers Workflow"
    - sidebar_label: "Exploratory Projects"
    - sidebar_position: 1

    Content: 3 stretch projects for participants who finish early or want to go deeper:

    **Project 1: TDD with AI (30 minutes)**
    - Description: Use Claude Code's TDD workflow to add a new endpoint to the reference app's catalog service
    - What to build: A `/items/:id` endpoint that returns a single item by ID
    - The TDD cycle: RED (write a failing test with `cargo test`), GREEN (ask Claude Code to implement the minimal code), REFACTOR (clean up with AI assistance)
    - Key insight: The AI writes better code when given a failing test as context — the test IS the specification
    - Expected deliverable: A passing test + working endpoint

    **Project 2: Systematic Debugging with AI (20 minutes)**
    - Description: Given a broken Helm values.yaml (introduce 3 deliberate errors), use AI-assisted systematic debugging to find and fix all issues
    - Provided: A broken values.yaml with (1) wrong port number, (2) misspelled service name, (3) missing resource requests
    - The debugging cycle: Read errors, reproduce, check recent changes, form hypothesis, test smallest fix
    - Key insight: AI is better at debugging when you provide the error message + surrounding context, not just "it doesn't work"
    - Expected deliverable: All 3 errors identified and fixed, helm lint passes

    **Project 3: AI Code Review (15 minutes)**
    - Description: Use Claude Code to review the reference app's Dockerfile for production readiness
    - What to review: `reference-app/services/api-gateway/Dockerfile`
    - Ask Claude Code: "Review this Dockerfile for production readiness. Consider: multi-stage build efficiency, security (non-root user), layer caching, .dockerignore coverage"
    - Key insight: AI code review is most useful when you specify WHAT to review for — generic "review this" produces generic output
    - Expected deliverable: A review with specific, actionable findings

    IMPORTANT: These are exploratory/stretch — not required. Make this clear in the intro. Context engineering vocabulary throughout.
  </action>
  <verify>
    <automated>grep -c "Project" course-site/docs/module-05b-ai-workflows/exploratory/PROJECTS.mdx</automated>
  </verify>
  <acceptance_criteria>
    - PROJECTS.mdx contains 3 project descriptions (grep "Project" returns at least 6)
    - grep "TDD" PROJECTS.mdx returns at least 1
    - grep "debugging" -i PROJECTS.mdx returns at least 1
    - grep "code review" -i PROJECTS.mdx returns at least 1
    - grep "exploratory" -i PROJECTS.mdx returns at least 1
    - No occurrence of "prompt engineering"
  </acceptance_criteria>
  <done>Superpowers exploratory content exists with 3 stretch projects (TDD, debugging, code review), each with clear deliverables and key insights about AI-assisted development workflows</done>
</task>

</tasks>

<verification>
- Module 5b directory exists with proper Docusaurus structure
- Composite lab has 4 sections covering GSD workflow, context engineering, memory, plan modes
- GSD lab section has the most detail (30 minutes, full cycle demo)
- Exploratory projects cover all 3 superpowers (TDD, debugging, code review)
- "prompt engineering" does NOT appear in any file
- "context engineering" vocabulary used throughout
- Every section has Expected result blocks
</verification>

<success_criteria>
- Module 5b directory exists in course-site/docs/ with proper Docusaurus structure
- LAB.mdx contains 4 complete sections with 10+ Expected result blocks
- PROJECTS.mdx contains 3 exploratory projects
- All content is solo-completable (dual format constraint)
</success_criteria>

<output>
After completion, create `.planning/phases/03-day-2-modules/03-02-SUMMARY.md`
</output>
