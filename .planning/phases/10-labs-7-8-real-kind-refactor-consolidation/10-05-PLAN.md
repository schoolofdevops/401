---
phase: 10-labs-7-8-real-kind-refactor-consolidation
plan: 05
type: execute
wave: 3
depends_on: [10-01, 10-02, 10-04]
files_modified:
  - infrastructure/wrappers/README.md
  - infrastructure/mock-data/README.md
autonomous: true
requirements: [LAB-05]
must_haves:
  truths:
    - "infrastructure/wrappers/ and infrastructure/mock-data/ directories remain in repo (not deleted)"
    - "Both directories have README.md files marking them deprecated"
    - "README.md explains why they're kept (reference/history) and that they are not used in current course labs"
    - "All course labs tested and verified to work WITHOUT these directories"
  artifacts:
    - path: "infrastructure/wrappers/README.md"
      provides: "Deprecation notice: wrappers kept for reference, not used by course labs"
      min_lines: 20
    - path: "infrastructure/mock-data/README.md"
      provides: "Deprecation notice: mock data kept for reference, not used by course labs"
      min_lines: 20
  key_links:
    - from: "infrastructure/wrappers/README.md"
      to: "infrastructure/wrappers/ directory"
      via: "Deprecation notice"
      pattern: "deprecated"
    - from: "infrastructure/mock-data/README.md"
      to: "infrastructure/mock-data/ directory"
      via: "Deprecation notice"
      pattern: "deprecated"
---

<objective>
Archive mock-mode infrastructure (wrappers/ and mock-data/ directories) with deprecation notices, clarify that they are kept for reference/history but no longer used by course labs.

Purpose: Clean record of what was removed and why; allows rollback or future reference without confusion about whether these directories are still part of the active course.

Output: README.md files in both directories marking them deprecated but kept.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-labs-7-8-real-kind-refactor-consolidation/10-CONTEXT.md

## Key Files to Study

infrastructure/wrappers/ directory:
@infrastructure/wrappers/

infrastructure/mock-data/ directory:
@infrastructure/mock-data/

</context>

<tasks>

<task type="auto">
  <name>Task 1: Create infrastructure/wrappers/README.md — Deprecation Notice</name>
  <files>infrastructure/wrappers/README.md</files>
  <read_first>
    - infrastructure/wrappers/ (directory listing of current scripts: mock-kubectl, mock-aws, mock-psql)
    - .planning/phases/10-labs-7-8-real-kind-refactor-consolidation/10-CONTEXT.md (infrastructure deprecation section)
  </read_first>
  <action>
    Create infrastructure/wrappers/README.md with the following content:

    ```markdown
    # Mock Wrapper Scripts — Deprecated

    **Status:** Deprecated as of Phase 10 (v1.1 Labs Refactor)

    This directory contains the wrapper scripts used for mock-mode labs in earlier course iterations:
    - `mock-kubectl` — Intercepted kubectl calls and returned pre-baked JSON responses
    - `mock-aws` — Intercepted aws CLI calls and returned mock AWS API responses
    - `mock-psql` — Intercepted PostgreSQL CLI calls

    ## Why Deprecated?

    Starting in v1.1, all Track C (Kubernetes) labs use a real KIND cluster instead of mock fixtures. This eliminates:
    - Environment variable complexity (HERMES_LAB_MODE, HERMES_LAB_SCENARIO, etc.)
    - Wrapper script PATH manipulation
    - Mock data maintenance burden
    - "Is this real or simulated?" cognitive overhead

    ## Why Keep These Files?

    These scripts are retained for:
    - **Historical reference** — Understanding how mock-mode was implemented
    - **Potential rollback** — If a future release needs mock mode again
    - **Troubleshooting** — Examining wrapper behavior if needed

    ## Current Usage in Course Labs

    **None.** No current course labs depend on these wrapper scripts. Learners use real KIND clusters directly.

    If you're building a new feature that needs mocking, consult these scripts as a reference, but do not activate them in the critical path.

    ## Related

    - See `.planning/analysis_labs_7_8_10_revision.md` for the full mock-mode removal analysis
    - See `infrastructure/mock-data/README.md` for mock data deprecation notice
    ```

    **Line target:** ~30 lines
  </action>
  <verify>
    <automated>
      # Verify file exists and contains deprecation marker
      grep -c "Deprecated\|deprecated" infrastructure/wrappers/README.md && echo "PASS: deprecation notice present"
    </automated>
    Detailed checks:
    - File exists: infrastructure/wrappers/README.md
    - Contains "Deprecated" or "deprecated"
    - Explains why wrappers were removed (real KIND instead)
    - States wrappers are not used by current labs
    - Notes that file is kept for reference/history
  </verify>
  <done>
    infrastructure/wrappers/README.md created with deprecation notice; scripts marked as archived but kept for reference.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create infrastructure/mock-data/README.md — Deprecation Notice</name>
  <files>infrastructure/mock-data/README.md</files>
  <read_first>
    - infrastructure/mock-data/ (directory listing of mock JSON responses)
    - .planning/phases/10-labs-7-8-real-kind-refactor-consolidation/10-CONTEXT.md (infrastructure deprecation section)
  </read_first>
  <action>
    Create infrastructure/mock-data/README.md with the following content:

    ```markdown
    # Mock Data Fixtures — Deprecated

    **Status:** Deprecated as of Phase 10 (v1.1 Labs Refactor)

    This directory contains the mock JSON responses and fixtures used in earlier course iterations:
    - `kubernetes/` — Baked kubectl output (get pods, describe pod, logs, etc.) for 6 failure scenarios
    - `aws/` — Baked AWS API responses for EC2, CloudWatch, etc.
    - Other service fixtures

    ## Why Deprecated?

    Starting in v1.1, all Track C (Kubernetes) labs use a real KIND cluster instead of mock fixtures. This means:
    - Real kubectl commands return real Kubernetes state
    - Learners diagnose actual pod failures they inject themselves
    - No divergence between mock and live behavior

    ## Why Keep These Files?

    These fixtures are retained for:
    - **Historical reference** — Understanding what mock data was used
    - **Comparison** — Checking if real Kubernetes API responses match old mocks
    - **Troubleshooting** — Examining mock data structure if debugging wrapper script behavior

    ## Current Usage in Course Labs

    **None.** No current course labs depend on these mock fixtures. Learners use real KIND clusters and real kubectl output.

    If you're rebuilding mock-mode support or comparing live vs. mock behavior, consult these files as a reference.

    ## Related

    - See `.planning/analysis_labs_7_8_10_revision.md` for the full mock-mode removal analysis
    - See `infrastructure/wrappers/README.md` for wrapper script deprecation notice
    - See `infrastructure/scenarios/k8s/` for the actual failure scenario manifests that learners apply
    ```

    **Line target:** ~35 lines
  </action>
  <verify>
    <automated>
      # Verify file exists and contains deprecation marker
      grep -c "Deprecated\|deprecated" infrastructure/mock-data/README.md && echo "PASS: deprecation notice present"
    </automated>
    Detailed checks:
    - File exists: infrastructure/mock-data/README.md
    - Contains "Deprecated" or "deprecated"
    - Explains why mock data was removed (real KIND instead)
    - States mock data is not used by current labs
    - Notes that files are kept for reference/history
    - References infrastructure/scenarios/k8s/ as the current replacement
  </verify>
  <done>
    infrastructure/mock-data/README.md created with deprecation notice; fixtures marked as archived but kept for reference.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify All Course Labs Work Without Wrappers/Mock-Data Directories</name>
  <files></files>
  <read_first>
    - .planning/phases/10-labs-7-8-real-kind-refactor-consolidation/10-01-PLAN.md (Module 7 refactor, should work without wrappers)
    - .planning/phases/10-labs-7-8-real-kind-refactor-consolidation/10-02-PLAN.md (Module 8 consolidation, should work without wrappers)
  </read_first>
  <action>
    Verify that the refactored Track C labs (Modules 7 and 8) do not reference wrappers/ or mock-data/ directories and do not depend on them for execution.

    Search patterns to verify:
    1. No references to `infrastructure/wrappers/` in Module 7 and 8 Track C labs:
       ```bash
       grep -r "infrastructure/wrappers" course-site/docs/module-0[78]-*/lab/LAB*.mdx
       ```
       Expected: 0 results

    2. No references to `infrastructure/mock-data/` in labs:
       ```bash
       grep -r "infrastructure/mock-data\|MOCK_DATA_DIR" course-site/docs/module-0[78]-*/lab/LAB*.mdx
       ```
       Expected: 0 results

    3. No HERMES_LAB_MODE or similar in labs:
       ```bash
       grep -r "HERMES_LAB_MODE\|HERMES_LAB_SCENARIO" course-site/docs/module-0[78]-*/lab/LAB*.mdx
       ```
       Expected: 0 results

    4. References to infrastructure/scenarios/k8s/ should be present (the real failure manifests):
       ```bash
       grep -r "infrastructure/scenarios/k8s" course-site/docs/module-0[78]-*/lab/LAB*.mdx
       ```
       Expected: >= 1 result

    Run all grep commands. If any of 1-3 return results, the refactor is incomplete. If 4 returns no results, the failure scenarios are not properly integrated.
  </action>
  <verify>
    <automated>
      # Run verification greps
      echo "Checking for wrappers/ references:"
      grep -r "infrastructure/wrappers" course-site/docs/module-0[78]-*/lab/LAB*.mdx | wc -l && \
      echo "Checking for mock-data/ references:"
      grep -r "infrastructure/mock-data\|MOCK_DATA_DIR" course-site/docs/module-0[78]-*/lab/LAB*.mdx | wc -l && \
      echo "Checking for HERMES_LAB_MODE:"
      grep -r "HERMES_LAB_MODE\|HERMES_LAB_SCENARIO" course-site/docs/module-0[78]-*/lab/LAB*.mdx | wc -l && \
      echo "Checking for scenarios/k8s/ references:"
      grep -r "infrastructure/scenarios/k8s" course-site/docs/module-0[78]-*/lab/LAB*.mdx | wc -l
    </automated>
    Results:
    - Wrappers references: 0 (PASS) or >= 1 (FAIL: refactor incomplete)
    - Mock-data references: 0 (PASS) or >= 1 (FAIL: refactor incomplete)
    - HERMES_LAB_MODE references: 0 (PASS) or >= 1 (FAIL: refactor incomplete)
    - Scenarios/k8s references: >= 1 (PASS: failure scenarios integrated) or 0 (FAIL: failure scenarios not integrated)
  </verify>
  <done>
    Verified: refactored Track C labs (Modules 7 and 8) have zero dependencies on wrappers/ or mock-data/ directories and reference real failure scenarios instead.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Deprecation notices created:
   - `ls -la infrastructure/wrappers/README.md` (exists)
   - `ls -la infrastructure/mock-data/README.md` (exists)

2. Content verification:
   - Both files contain "Deprecated" or "deprecated"
   - Both explain why archives were kept
   - Both state that files are not used by current labs

3. Lab verification:
   - Zero references to wrappers/ or mock-data/ in refactored labs
   - Zero HERMES_LAB_MODE or similar in labs
   - References to infrastructure/scenarios/k8s/ present in labs

4. Git status:
   - Two new README.md files (wrappers and mock-data)
   - Original wrapper and mock-data files unchanged (preserved as historical artifacts)
</verification>

<success_criteria>
✓ infrastructure/wrappers/README.md created with deprecation notice
✓ infrastructure/mock-data/README.md created with deprecation notice
✓ Both READMEs explain why directories are kept (reference/history)
✓ Both READMEs state that current labs do not depend on these directories
✓ Verified: Track C labs (Modules 7 and 8) have zero references to wrappers/ or mock-data/
✓ Verified: Track C labs reference infrastructure/scenarios/k8s/ (real failure manifests)
✓ Directory structure preserved (no deletion, just archival with README)
</success_criteria>

<output>
After completion, create `.planning/phases/10-labs-7-8-real-kind-refactor-consolidation/10-05-SUMMARY.md` with:
- Deprecation notices created
- Directory preservation note
- Verification results (zero wrapper/mock-data dependencies in labs)
- Scenarios/k8s integration confirmed
</output>
