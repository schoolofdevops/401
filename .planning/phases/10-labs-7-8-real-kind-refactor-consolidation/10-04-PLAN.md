---
phase: 10-labs-7-8-real-kind-refactor-consolidation
plan: 04
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - course-site/docs/setup.mdx
autonomous: true
requirements: [LAB-01, LAB-04]
must_haves:
  truths:
    - "SETUP.md contains zero references to HERMES_LAB_MODE, HERMES_LAB_SCENARIO, HERMES_LAB_TRACK, HERMES_LAB_WRAPPERS, MOCK_DATA_DIR"
    - "Step 5 (Lab Wrapper Aliases) is removed entirely"
    - "Track C prerequisite is simplified to: 'Verify KIND is running: kubectl cluster-info --context kind-lab'"
    - "No ~/.bash_profile alias setup required"
  artifacts:
    - path: "course-site/docs/setup.mdx"
      provides: "Simplified setup documentation, KIND-only for Track C, no mock-mode env vars"
      min_lines: 80
  key_links:
    - from: "setup.mdx"
      to: "Module 7 and 8 Track C labs"
      via: "Prerequisites section"
      pattern: "KIND\|kubectl"
---

<objective>
Refactor SETUP.md to remove all references to mock-mode infrastructure (wrapper aliases, environment variables, ~/.bash_profile setup) and simplify to a single KIND cluster verification for Track C.

Purpose: Reduce setup cognitive load from 5 environment variables + bash_profile block to a single cluster check.

Output: Simplified SETUP.md with zero mock-mode references, KIND-only prerequisite for Track C.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-labs-7-8-real-kind-refactor-consolidation/10-CONTEXT.md

## Key Files to Study

Current SETUP.md:
@course-site/docs/setup.mdx

</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor SETUP.md — Remove Mock-Mode Wrapper Setup, Simplify to KIND Check</name>
  <files>course-site/docs/setup.mdx</files>
  <read_first>
    - course-site/docs/setup.mdx (current state)
    - .planning/phases/10-labs-7-8-real-kind-refactor-consolidation/10-CONTEXT.md (Setup documentation refactor section)
  </read_first>
  <action>
    Refactor setup.mdx following these changes:

    **REMOVE entirely:**
    1. **Step 5 (Lab Wrapper Aliases)** — Delete the entire section (typically ~30-40 lines)
       - Remove any heading like "Lab Wrapper Aliases — one-time setup" or similar
       - Remove all code blocks showing:
         ```bash
         export HERMES_LAB_MODE=mock
         export HERMES_LAB_SCENARIO=...
         export MOCK_DATA_DIR=...
         export HERMES_LAB_WRAPPERS=...
         export HERMES_LAB_TRACK=...
         ```
       - Remove any ~/.bash_profile alias block setup instructions
       - Remove any explanation about "mock mode" or wrapper interception

    2. **Scattered references to environment variables:**
       - Search and remove any mention of HERMES_LAB_MODE, HERMES_LAB_SCENARIO, MOCK_DATA_DIR, HERMES_LAB_WRAPPERS, HERMES_LAB_TRACK
       - Remove any text explaining "wrapper scripts" or "mock kubectl"

    **MODIFY Track C section:**
    - Find the section that mentions Track C or Kubernetes labs
    - Replace with:
      ```markdown
      ### Track C: Kubernetes Diagnostic Agent
      
      **Prerequisites:** You've already installed KIND in a previous module. Before starting Module 7, verify your cluster is running:
      
      \`\`\`bash
      kubectl cluster-info --context kind-lab
      # Expected output: Kubernetes control plane is running...
      
      kubectl get nodes
      # Expected output: NAME                 STATUS   ROLES
      # kind-control-plane   Ready    control-plane
      \`\`\`
      
      If the cluster isn't running, start it:
      \`\`\`bash
      kind create cluster --name lab --config infrastructure/cluster-config.yaml
      \`\`\`
      
      That's it — no environment variables, no wrapper setup. You'll use the real KIND cluster for all Track C labs (Modules 7, 8, 9+).
      ```

    **KEEP:**
    - All other setup steps (Claude Code, Hermes installation, etc.)
    - Tracks A and B setup (unchanged)
    - General prerequisite information

    **Renumber if necessary:**
    - If Step 5 is removed, Steps 6+ should be renumbered to Steps 5+
    - Update any table of contents or step references

    **Line target:** SETUP.md should be ~20-30% shorter (removing ~30-40 line Step 5 + scattered references).

    **Tone:** Direct, practical: "Verify your cluster is running. You're good to go."
  </action>
  <verify>
    <automated>
      # Verify zero mock-mode environment variable references
      grep -E "(HERMES_LAB_MODE|HERMES_LAB_SCENARIO|MOCK_DATA_DIR|HERMES_LAB_WRAPPERS|HERMES_LAB_TRACK)" course-site/docs/setup.mdx && echo "FAIL: env vars still present" || echo "PASS: env vars removed"
    </automated>
    Detailed checks:
    - Zero occurrences of: HERMES_LAB_MODE, HERMES_LAB_SCENARIO, MOCK_DATA_DIR, HERMES_LAB_WRAPPERS, HERMES_LAB_TRACK
    - Zero occurrences of: "wrapper", "mock-kubectl", "~/.bash_profile" (in setup context)
    - Contains "kubectl cluster-info --context kind-lab" or equivalent KIND verification
    - Contains "kubectl get nodes" or equivalent cluster status check
    - No Step 5 about "Lab Wrapper Aliases"
    - Track C section explains: "kind is already set up, verify it's running"
    - File is shorter than original (fewer lines due to removed Step 5)
    - Verified with: `wc -l course-site/docs/setup.mdx` before/after comparison
  </verify>
  <done>
    SETUP.mdx refactored: removed entire Step 5 (wrapper aliases) + all scattered mock-mode env var references. Track C now has single prerequisite: verify KIND is running. Setup cognitive load reduced from 5 env vars to 2 kubectl commands.
  </done>
</task>

</tasks>

<verification>
After completion:

1. Environment variable check:
   - `grep -c "HERMES_LAB_MODE\|MOCK_DATA_DIR\|HERMES_LAB_SCENARIO" course-site/docs/setup.mdx` (should be 0)

2. Kind verification present:
   - `grep -c "kubectl cluster-info\|kubectl get nodes" course-site/docs/setup.mdx` (should be >= 1)

3. File size:
   - `wc -l course-site/docs/setup.mdx` (should be ~20-30% shorter than original)

4. Track C section:
   - Explains KIND is already set up
   - Shows verification steps
   - No wrapper or env var setup required
</verification>

<success_criteria>
✓ SETUP.mdx contains zero HERMES_LAB_* environment variables
✓ Step 5 (Lab Wrapper Aliases) removed entirely
✓ ~/.bash_profile alias block setup removed
✓ Track C section simplified to KIND cluster verification
✓ No references to mock-mode, wrappers, or fixture data
✓ File is noticeably shorter (removed ~30-40 lines)
</success_criteria>

<output>
After completion, create `.planning/phases/10-labs-7-8-real-kind-refactor-consolidation/10-04-SUMMARY.md` with:
- SETUP.mdx changes summary
- Removed Step 5 and line count reduction
- Kind verification section added
- Mock-mode references removed (count)
</output>
