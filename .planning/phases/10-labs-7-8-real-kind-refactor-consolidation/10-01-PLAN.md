---
phase: 10-labs-7-8-real-kind-refactor-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - course-site/docs/module-07-agent-skills/lab/LAB-track-c-kubernetes.mdx
  - course-site/docs/module-07-agent-skills/README.mdx
autonomous: true
requirements: [LAB-01, LAB-03]
must_haves:
  truths:
    - "Module 7 Track C lab can be executed with zero environment variable setup"
    - "Module 7 Track C lab assumes real KIND cluster is running"
    - "Lab includes instructions to apply failure scenarios from infrastructure/scenarios/k8s/"
    - "Module 7 README shows updated duration and real KIND-only setup"
  artifacts:
    - path: "course-site/docs/module-07-agent-skills/lab/LAB-track-c-kubernetes.mdx"
      provides: "Track C lab without mock mode, duration 60 min, real KIND focus"
      min_lines: 150
    - path: "course-site/docs/module-07-agent-skills/README.mdx"
      provides: "Updated README with duration, prerequisites, and no mock mode mention"
      min_lines: 50
  key_links:
    - from: "LAB-track-c-kubernetes.mdx"
      to: "infrastructure/scenarios/k8s/"
      via: "kubectl apply instructions"
      pattern: "kubectl apply.*scenarios/k8s"
    - from: "LAB-track-c-kubernetes.mdx"
      to: "agents/track-c-kubernetes/skills/sre-k8s-pod-health"
      via: "SKILL.md reference"
      pattern: "sre-k8s-pod-health"
---

<objective>
Refactor Module 7 Track C lab to remove all mock-mode infrastructure and teach skill writing against a real KIND cluster with learner-applied failure scenarios.

Purpose: Eliminate environment variable confusion (HERMES_LAB_MODE, wrappers, mock-data paths) and teach real Kubernetes debugging workflow where learners create broken pods, then diagnose them.

Output: Updated LAB-track-c-kubernetes.mdx (60 min, real KIND) and Module 7 README showing no mock-mode requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/analysis_labs_7_8_10_revision.md
@.planning/phases/10-labs-7-8-real-kind-refactor-consolidation/10-CONTEXT.md

## Key Files to Study Before Implementing

Current Module 7 Track C lab (to be refactored):
@course-site/docs/module-07-agent-skills/lab/LAB-track-c-kubernetes.mdx

Current Module 7 README:
@course-site/docs/module-07-agent-skills/README.mdx

Reference SKILL.md (from Phase 6, what learners are building toward):
@agents/track-c-kubernetes/skills/sre-k8s-pod-health/SKILL.md

Failure scenario manifests (6 yamls, learners will apply):
@infrastructure/scenarios/k8s/

Current solution reference (to understand what the consolidated lab should achieve):
@course-site/docs/module-07-agent-skills/lab/solution/

</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor LAB-track-c-kubernetes.mdx — Remove Mock Mode, Add Real KIND Instructions</name>
  <files>course-site/docs/module-07-agent-skills/lab/LAB-track-c-kubernetes.mdx</files>
  <read_first>
    - course-site/docs/module-07-agent-skills/lab/LAB-track-c-kubernetes.mdx (current state)
    - .planning/analysis_labs_7_8_10_revision.md (mock mode problems section + proposed solution section for Lab 7)
    - .planning/phases/10-labs-7-8-real-kind-refactor-consolidation/10-CONTEXT.md (Module 7 specifics section)
    - agents/track-c-kubernetes/skills/sre-k8s-pod-health/SKILL.md (reference outcome)
  </read_first>
  <action>
    Rewrite LAB-track-c-kubernetes.mdx following the structure below:

    **REMOVE entirely:**
    - Prerequisites section mentioning HERMES_LAB_MODE, HERMES_LAB_SCENARIO, MOCK_DATA_DIR, HERMES_LAB_WRAPPERS, HERMES_LAB_TRACK
    - Any references to "wrapper scripts" or "mock data directory"
    - Environment variable export blocks
    - Instructions to "set HERMES_LAB_MODE=mock"

    **KEEP:**
    - The core SKILL.md writing flow (unchanged pedagogically)
    - Solution reference structure (unchanged)

    **ADD/REPLACE with:**

    1. **Prerequisites (5 min):** 
       - Verify KIND cluster is running: `kubectl cluster-info --context kind-lab`
       - Verify you can reach the cluster: `kubectl get nodes` (expect kind-control-plane Ready)
       - That's it — no environment variables

    2. **New Step: Inject Failure Scenarios (added before main skill-writing)**
       - Explain: "You'll diagnose real pod failures. First, you'll create them."
       - Show:
         ```bash
         # Pick one scenario from infrastructure/scenarios/k8s/:
         # 01-image-pull.yaml (ImagePullBackOff)
         # 02-crashloop2.yaml (CrashLoopBackOff)
         # 03-oom-memory.yaml (OOMKilled)
         # 04-liveness-probe.yaml (Liveness probe failure)
         # 05-missing-secret.yaml (Missing configmap/secret)
         # 06-port-mismatch.yaml (Service/Pod port mismatch)
         
         # Apply one:
         kubectl apply -f infrastructure/scenarios/k8s/01-image-pull.yaml
         
         # Verify the pod is now broken:
         kubectl get pods -A | grep -i imagepull
         ```
       - Emphasize: "You're creating a broken cluster state. Your SKILL.md will diagnose it."

    3. **Main flow (20-30 min):**
       - Start: "Write a SKILL.md that examines this pod and identifies what's wrong"
       - Guide through the decision tree (see sre-k8s-pod-health reference)
       - Use real `kubectl` commands — they will work against the real, broken cluster

    4. **Test Your Skill (10 min):**
       - Run: `hermes -p track-c chat`
       - Ask agent: "What's wrong with the pods in default namespace?"
       - Verify it identifies the failure mode correctly

    5. **Optional: Try Another Scenario (10 min):**
       - Clean up: `kubectl delete -f infrastructure/scenarios/k8s/01-image-pull.yaml`
       - Apply a different scenario
       - Test your SKILL.md against it

    6. **Conclusion:**
       - Your SKILL.md now works against REAL Kubernetes, not fixtures.
       - Next lab (Module 8) will wire this into a full agent configuration.

    **Tone:** Direct, practical. "You broke it, you diagnose it." No explanation of why mock mode was removed — that's not the learner's concern.

    **Line target:** ~180-220 lines (original is ~160; additions for failure scenario section + real KIND steps = net gain ~30-50 lines)
  </action>
  <verify>
    <automated>
      # Verify zero occurrences of mock-mode environment variables
      grep -c "HERMES_LAB_MODE" course-site/docs/module-07-agent-skills/lab/LAB-track-c-kubernetes.mdx && echo "FAIL: HERMES_LAB_MODE still present" || echo "PASS: HERMES_LAB_MODE removed"
    </automated>
    Detailed checks:
    - Zero occurrences: "HERMES_LAB_MODE", "HERMES_LAB_SCENARIO", "MOCK_DATA_DIR", "HERMES_LAB_WRAPPERS", "HERMES_LAB_TRACK"
    - Verified with: `grep -E "(HERMES_LAB|MOCK_DATA|wrappers/)" course-site/docs/module-07-agent-skills/lab/LAB-track-c-kubernetes.mdx | wc -l` (should be 0)
    - Contains "kubectl apply" followed by "scenarios/k8s/" (exactly once, in failure injection section)
    - Contains "kind-control-plane" or similar KIND verification step
    - Contains reference to real KIND cluster, not mock
    - Verified with: `grep -c "kubectl cluster-info\|kubectl get nodes" course-site/docs/module-07-agent-skills/lab/LAB-track-c-kubernetes.mdx` (should be >= 1)
    - File structure:
      - Heading 1: "Module 7 Track C Lab"
      - Prerequisites section with KIND check (no env vars)
      - "Inject Failure Scenarios" section with kubectl apply examples
      - SKILL.md writing flow (reference structure intact)
      - Test Your Skill section
      - Optional: Try Another Scenario (optional)
  </verify>
  <done>
    LAB-track-c-kubernetes.mdx refactored to remove all mock-mode references; learners now apply real failure scenarios to KIND and diagnose them with their SKILL.md. No environment variables, no wrapper setup required.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Module 7 README.mdx — Duration, Prerequisites, Consolidation Note</name>
  <files>course-site/docs/module-07-agent-skills/README.mdx</files>
  <read_first>
    - course-site/docs/module-07-agent-skills/README.mdx (current state)
    - .planning/phases/10-labs-7-8-real-kind-refactor-consolidation/10-CONTEXT.md (Module 7 specifics, README updates section)
  </read_first>
  <action>
    Update README.mdx with the following changes:

    1. **Duration:** Keep at 60 min (no change — just removing setup tax, not adding content)

    2. **Prerequisites section:**
       - OLD: Lists HERMES_LAB_MODE, HERMES_LAB_SCENARIO, wrapper env vars
       - NEW: "You need a running KIND cluster (from Module 6)" + "Verify: `kubectl cluster-info --context kind-lab`"

    3. **Learning objectives:** 
       - Confirm the lab teaches real Kubernetes debugging against a real cluster

    4. **Track C note (if separate section):**
       - Add note: "Track C labs (7 & 8) now use a real KIND cluster. No wrappers or environment variables required."

    5. **Remove any mention of:**
       - Mock mode, fixtures, wrapper scripts, ~/.bash_profile alias setup
       - HERMES_LAB_MODE, HERMES_LAB_SCENARIO

    **Line target:** Minimal change — a 60-line README stays ~60 lines, just with different prerequisites wording.
  </action>
  <verify>
    <automated>
      # Verify no mock-mode references in README
      grep -E "(HERMES_LAB|MOCK_DATA|mock-kubectl|mock-aws|~/.bash_profile)" course-site/docs/module-07-agent-skills/README.mdx && echo "FAIL: mock-mode references still present" || echo "PASS: mock-mode references removed"
    </automated>
    Detailed checks:
    - Zero occurrences of mock-mode environment variables or setup instructions
    - Contains "KIND" or "kubectl cluster-info"
    - Duration field shows "60 min"
    - Prerequisites mention "Module 6" (where KIND was set up)
  </verify>
  <done>
    Module 7 README.mdx updated: duration confirmed at 60 min, prerequisites simplified to KIND cluster check, all mock-mode references removed.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Lab file verification:
   - `grep -c "kubectl apply.*scenarios/k8s" course-site/docs/module-07-agent-skills/lab/LAB-track-c-kubernetes.mdx` (should be >= 1)
   - `grep -c "HERMES_LAB_MODE\|MOCK_DATA_DIR" course-site/docs/module-07-agent-skills/lab/LAB-track-c-kubernetes.mdx` (should be 0)
   - File renders without MDX syntax errors (Docusaurus check)

2. README file verification:
   - `grep -c "KIND\|kubectl cluster-info" course-site/docs/module-07-agent-skills/README.mdx` (should be >= 1)
   - `grep -c "HERMES_LAB_MODE\|mock" course-site/docs/module-07-agent-skills/README.mdx` (should be 0)
   - File renders without MDX syntax errors

3. Cross-reference check:
   - `infrastructure/scenarios/k8s/` directory exists and contains 6 scenario yamls (01-06)
   - Failure scenarios are correctly referenced in lab file

4. Pedagogical check:
   - Lab flow reads: "Here's a broken cluster. You'll diagnose it." (no mock explanation overhead)
</verification>

<success_criteria>
✓ LAB-track-c-kubernetes.mdx contains zero HERMES_LAB_* environment variable references
✓ Module 7 README.mdx shows 60 min duration, KIND cluster prerequisite only
✓ Lab includes explicit `kubectl apply infrastructure/scenarios/k8s/` instructions
✓ Lab assumes real KIND cluster, learners create and diagnose real failure scenarios
✓ All setup tax (env vars, wrappers, aliases) removed from Module 7 Track C path
✓ Core skill-writing pedagogy preserved
</success_criteria>

<output>
After completion, create `.planning/phases/10-labs-7-8-real-kind-refactor-consolidation/10-01-SUMMARY.md` with:
- Files modified count
- Mock-mode references removed (count)
- New prerequisite structure
- Failure scenario integration status
</output>
