---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - setup/SETUP.md
  - setup/verify.sh
  - setup/llm-access.md
autonomous: true
requirements:
  - FOUND-05
  - FOUND-06
  - FOUND-08
must_haves:
  truths:
    - "A participant following SETUP.md from scratch can install all prerequisites and reach a working state"
    - "SETUP.md documents both Claude Code and OpenCode (sst/opencode) paths with full setup instructions"
    - "verify.sh validates all course prerequisites (Docker, KIND, kubectl, Helm, Claude Code or OpenCode) and reports PASS/FAIL"
    - "verify.sh checks for the reference app, mock data files, and KIND cluster — not Hermes-specific checks"
    - "LLM access docs cover Claude Code, OpenCode with Gemini 2.5 Flash, OpenCode with Groq, and OpenRouter"
    - "verify.sh outputs all PASS when run on a correctly configured environment"
  artifacts:
    - path: "setup/SETUP.md"
      provides: "Complete participant setup guide"
      min_lines: 150
      contains: "Claude Code"
    - path: "setup/verify.sh"
      provides: "Environment verification script"
      contains: "PASS"
    - path: "setup/llm-access.md"
      provides: "Multi-provider LLM setup with rate limits"
      contains: "OpenCode"
  key_links:
    - from: "setup/verify.sh"
      to: "reference-app/ and infrastructure/mock-data/"
      via: "file existence checks and command verification"
      pattern: "test -f|test -d|check"
    - from: "setup/SETUP.md"
      to: "setup/verify.sh"
      via: "instruction to run verification"
      pattern: "verify.sh"
    - from: "setup/SETUP.md"
      to: "setup/llm-access.md"
      via: "link to LLM provider details"
      pattern: "llm-access.md"
---

<objective>
Create the participant setup guide (SETUP.md), rewrite verify.sh for the course (not Hermes), and update LLM access documentation for Claude Code + OpenCode dual-path.

Purpose: This is the gate for every participant — before any module lab works, participants must have a correctly configured environment. SETUP.md is the single-entry-point document that walks them through everything. verify.sh is the automated proof that everything works. LLM docs cover both Claude Code and OpenCode paths per D-14. Per D-15, the course uses OpenCode (sst/opencode at opencode.ai), NOT Crush.

Output: Complete setup guide, course-specific verification script, updated LLM access documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md

@setup/verify.sh
@setup/llm-access.md
@setup/setup-kind.md

<interfaces>
<!-- From Plan 01-03 — what exists after prior plans complete -->

Reference app structure:
  reference-app/
    Makefile (make deploy, make destroy, make status)
    services/api-gateway/, services/catalog/, services/worker/
    dashboard/
    helm/reference-app/

Mock data files (from Plan 02):
  infrastructure/mock-data/cloudwatch/describe-alarms-clean.json
  infrastructure/mock-data/cloudwatch/describe-alarms-anomaly.json
  infrastructure/mock-data/cloudwatch/describe-alarm-history.json
  infrastructure/mock-data/cost-explorer/normal-spend.json
  infrastructure/mock-data/cost-explorer/anomaly-spike.json
  infrastructure/mock-data/ec2/describe-instances.json

KIND cluster: name "lab", ports 30080 (dashboard), 30090 (Grafana), 30091 (Prometheus)
Dashboard URL: http://localhost:30080
Grafana URL: http://localhost:30090 (admin/admin)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SETUP.md participant setup guide</name>
  <files>
    setup/SETUP.md
  </files>
  <read_first>
    setup/setup-kind.md (existing KIND setup content — reuse where applicable),
    setup/llm-access.md (existing LLM guide — understand what's there, but this is Hermes-focused),
    .planning/phases/01-foundation/01-CONTEXT.md (D-14 two equal paths, D-15 OpenCode not Crush, D-16 expected outputs for BOTH paths),
    .planning/phases/01-foundation/01-RESEARCH.md (Pitfall 3 for OpenCode vs Crush distinction, Standard Stack for version numbers, Environment Availability for what's required)
  </read_first>
  <action>
    Create `setup/SETUP.md` — the single-entry-point participant setup guide per FOUND-05.

    **Document structure (in this order):**

    1. **Header and Overview**
       - Title: "Agentic DevOps Workshop — Environment Setup"
       - Estimated time: 30-45 minutes
       - What you'll set up: Docker, KIND, Helm, AI coding tools, reference app
       - Prerequisites: macOS or Linux (Windows with WSL2), terminal proficiency, text editor

    2. **Step 1: Docker**
       - Install Docker Desktop (link to docker.com/get-started)
       - Verify: `docker info` shows "Server: Docker Desktop"
       - Minimum: Docker 24+, allocate at least 6GB RAM in Docker Desktop settings (Prometheus + app + PostgreSQL need ~4GB)
       - Expected output: show `docker --version` output example

    3. **Step 2: Kubernetes Tools**
       - Install KIND v0.27+: `brew install kind` or `go install sigs.k8s.io/kind@v0.27.0`
       - Install kubectl: `brew install kubectl` or comes with Docker Desktop
       - Install Helm 3.x: `brew install helm`
       - Verify each: `kind version`, `kubectl version --client`, `helm version`
       - Expected output for each command

    4. **Step 3: AWS CLI (optional — for real AWS connection)**
       - Install AWS CLI v2: `brew install awscli` or official installer
       - Verify: `aws --version` shows `aws-cli/2.x`
       - Note: "If you don't have an AWS account, skip this step. All labs have a mock data fallback that works without AWS credentials."
       - If you have AWS: `aws configure` with your credentials
       - Expected output example

    5. **Step 4: AI Coding Tool — Choose Your Path**

       **Path A: Claude Code (recommended if you have a Claude Pro/Team subscription)**
       - Install: `npm install -g @anthropic-ai/claude-code` (requires Node.js 18+)
       - Verify: `claude --version`
       - First run: `claude` — authenticates via browser OAuth
       - Note: "Claude Code uses your existing Claude subscription. No additional API costs."
       - Expected output: version number + successful auth message

       **Path B: OpenCode (free — no subscription required)**
       - What is OpenCode: "OpenCode (opencode.ai) is a terminal-based AI coding agent that supports 75+ LLM providers including free options."
       - Install: `brew install sst/tap/opencode` (macOS) or `curl -fsSL https://opencode.ai/install.sh | sh` (Linux)
       - Verify: `opencode --version`
       - Provider setup: Run `opencode` then use `/connect` to configure a provider:
         - **Gemini 2.5 Flash (recommended free provider):** Get API key from aistudio.google.com (no credit card needed). Rate limits: 10 RPM / 500 requests per day — sufficient for all labs.
         - **Groq (fast, free):** Get API key from console.groq.com (no credit card). Rate limits: 14,400 req/day for llama-3.1-8b-instant. Best for fast inference demos.
         - **OpenRouter:** Sign up at openrouter.ai for free credits. Use models with `:free` suffix.
         - **Grok (optional):** Requires API key from console.x.ai. May require payment — verify current pricing before using.
       - Expected output for each provider setup step

       Per D-14: both paths are equal. Per D-15: this is sst/opencode (opencode.ai), NOT charmbracelet/crush. Per D-16: labs show expected outputs for BOTH paths.

    6. **Step 5: Clone the Course Repository**
       - `git clone <repo-url>` (provide actual URL or placeholder)
       - `cd course/`

    7. **Step 6: Deploy the Reference Application**
       - `cd reference-app && make deploy`
       - What this does: creates KIND cluster "lab", installs PostgreSQL, installs Prometheus + Grafana, builds and deploys the reference microservices app
       - Takes 5-10 minutes on first run
       - Expected output: deployment complete message with URLs
       - Verify: open http://localhost:30080 — you should see the health dashboard with 3 services showing "healthy" status
       - Optional: open http://localhost:30090 — Grafana dashboard (login: admin/admin)

    8. **Step 7: Verify Your Environment**
       - `bash setup/verify.sh`
       - Expected: all checks PASS
       - "If any checks FAIL, see the troubleshooting section below."

    9. **Troubleshooting**
       - Docker not running: Start Docker Desktop, wait 30s, retry
       - KIND cluster creation fails: `kind delete cluster --name lab` then retry `make deploy`
       - Dashboard not accessible: `kubectl get pods -n app` — all pods should be Running
       - OpenCode provider connection fails: verify API key, check provider rate limits
       - Pods stuck in Pending: Docker Desktop may need more RAM (increase to 8GB)
       - Port 30080 already in use: `lsof -i :30080` to find the process, or change the port in KIND config

    10. **Windows Notes (WSL2)**
        - All commands run inside WSL2 (Ubuntu 22.04+ recommended)
        - Docker Desktop integrates with WSL2 automatically
        - Claude Code and OpenCode work natively on Windows, but labs use bash scripts — run from WSL2

    Do NOT reference Hermes, hermes login, or hermes skills anywhere in this guide. This is the course foundation setup, not the Hermes module setup.
  </action>
  <verify>
    <automated>test -f setup/SETUP.md && grep -c "Claude Code" setup/SETUP.md && grep -c "OpenCode" setup/SETUP.md && grep -c "opencode.ai" setup/SETUP.md && ! grep -q "hermes" setup/SETUP.md && echo "No hermes references: PASS"</automated>
  </verify>
  <acceptance_criteria>
    - setup/SETUP.md exists with at least 150 lines
    - Contains "Claude Code" at least 3 times (install, verify, usage)
    - Contains "OpenCode" at least 3 times (install, verify, provider setup)
    - Contains "opencode.ai" (correct URL, not opencode-ai/opencode)
    - Does NOT contain the word "hermes" (case-insensitive) — `grep -i hermes setup/SETUP.md` returns nothing
    - Does NOT contain "crush" or "charmbracelet" — `grep -i crush setup/SETUP.md` returns nothing
    - Contains "make deploy" (reference app deployment step)
    - Contains "http://localhost:30080" (dashboard URL)
    - Contains "verify.sh" (verification step)
    - Contains "Gemini 2.5 Flash" (free provider for OpenCode)
    - Contains "Groq" (second free provider)
    - Contains "Docker" with version requirement
    - Contains "KIND" with version requirement
    - Contains a "Troubleshooting" section
  </acceptance_criteria>
  <done>SETUP.md is a complete, self-contained participant setup guide covering Docker, KIND, Helm, Claude Code, OpenCode (sst/opencode), reference app deployment, and environment verification. No Hermes or Crush references. Both AI tool paths are documented equally.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite verify.sh for course (not Hermes)</name>
  <files>
    setup/verify.sh
  </files>
  <read_first>
    setup/verify.sh (current Hermes-focused script — understand the check() helper function pattern to preserve),
    .planning/phases/01-foundation/01-RESEARCH.md (Pitfall 1 for Hermes coupling in verify.sh),
    .planning/phases/01-foundation/01-01-SUMMARY.md (what reference-app files exist),
    .planning/phases/01-foundation/01-02-SUMMARY.md (what mock data files exist),
    .planning/phases/01-foundation/01-03-SUMMARY.md (what Helm/KIND/dashboard files exist)
  </read_first>
  <action>
    Rewrite `setup/verify.sh` per FOUND-06. Keep the existing `check()` helper function pattern (it's clean), but replace all sections with course-specific checks.

    **Header:**
    ```bash
    #!/usr/bin/env bash
    # course/setup/verify.sh
    # Pre-workshop environment validation for Agentic DevOps course.
    #
    # Usage: bash verify.sh
    # Exits 0 if all checks pass, 1 if any fail.
    #
    # Compatibility: bash 3.2+ (macOS system bash, Linux bash)
    ```

    **Keep:** The `set -uo pipefail`, PASS/FAIL counters, SCRIPT_DIR/COURSE_ROOT derivation, and `check()` helper function exactly as-is.

    **Replace sections with:**

    Section 1: Required CLI Tools
    - check "Docker daemon running" docker info
    - check "Docker version >= 24" (existing pattern)
    - check "kubectl installed" kubectl version --client
    - check "kind installed" kind version
    - check "kind version >= 0.27" (existing pattern)
    - check "Helm installed" helm version
    - check "Helm version >= 3" bash -c 'helm version 2>&1 | grep -E "v3\."'
    - check "Node.js installed" node --version
    - check "Node.js version >= 18" bash -c 'node --version 2>&1 | grep -E "v(1[89]|[2-9][0-9])\."'

    Section 2: AI Coding Tools (at least one required)
    ```bash
    printf '--- AI Coding Tools (at least one required) ---\n'
    AI_TOOL_FOUND=0
    if command -v claude >/dev/null 2>&1; then
      check "Claude Code installed" claude --version
      AI_TOOL_FOUND=1
    else
      printf '  SKIP  Claude Code not installed (Path A — optional if using OpenCode)\n'
    fi
    if command -v opencode >/dev/null 2>&1; then
      check "OpenCode installed" opencode --version
      AI_TOOL_FOUND=1
    else
      printf '  SKIP  OpenCode not installed (Path B — optional if using Claude Code)\n'
    fi
    if [ "$AI_TOOL_FOUND" -eq 0 ]; then
      printf '  FAIL  No AI coding tool found. Install Claude Code (Path A) or OpenCode (Path B).\n'
      printf '        See setup/SETUP.md Step 4.\n'
      FAIL=$((FAIL + 1))
    fi
    ```

    Section 3: Optional Tools
    - check "AWS CLI v2 installed (optional)" with SKIP if not found (not FAIL — it's optional per D-10 mock fallback)
    - check "git installed" git --version

    Section 4: KIND Cluster
    - check "KIND cluster 'lab' exists" (same as current)
    - check "kubectl context 'kind-lab' configured" (same as current)
    - check "kubectl can reach KIND cluster" (same as current)

    Section 5: Reference App (NEW — checks Plan 01/03 outputs)
    - check "Reference app Cargo workspace exists" test -f "$COURSE_ROOT/reference-app/Cargo.toml"
    - check "Helm chart exists" test -f "$COURSE_ROOT/reference-app/helm/reference-app/Chart.yaml"
    - check "Makefile exists" test -f "$COURSE_ROOT/reference-app/Makefile"
    - check "Dashboard package.json exists" test -f "$COURSE_ROOT/reference-app/dashboard/package.json"

    Section 6: Mock Data Files
    - check "mock-data/cloudwatch/describe-alarms-clean.json" test -f "$COURSE_ROOT/infrastructure/mock-data/cloudwatch/describe-alarms-clean.json"
    - check "mock-data/cloudwatch/describe-alarms-anomaly.json" test -f "$COURSE_ROOT/infrastructure/mock-data/cloudwatch/describe-alarms-anomaly.json"
    - check "mock-data/cost-explorer/normal-spend.json" test -f "$COURSE_ROOT/infrastructure/mock-data/cost-explorer/normal-spend.json"
    - check "mock-data/cost-explorer/anomaly-spike.json" test -f "$COURSE_ROOT/infrastructure/mock-data/cost-explorer/anomaly-spike.json"
    - check "mock-data/ec2/describe-instances.json" test -f "$COURSE_ROOT/infrastructure/mock-data/ec2/describe-instances.json"

    Section 7: Mock Wrapper Scripts
    - check "mock-aws wrapper" test -x "$COURSE_ROOT/infrastructure/wrappers/mock-aws"
    - check "mock-kubectl wrapper" test -x "$COURSE_ROOT/infrastructure/wrappers/mock-kubectl"

    Section 8: Mock Mode Smoke Tests
    - check "mock-aws CloudWatch alarms return data" bash -c 'HERMES_LAB_MODE=mock MOCK_DATA_DIR="$COURSE_ROOT/infrastructure/mock-data" "$COURSE_ROOT/infrastructure/wrappers/mock-aws" cloudwatch describe-alarms 2>/dev/null | grep -q "MetricAlarms"'
    - check "mock-aws Cost Explorer returns data" bash -c 'HERMES_LAB_MODE=mock MOCK_DATA_DIR="$COURSE_ROOT/infrastructure/mock-data" "$COURSE_ROOT/infrastructure/wrappers/mock-aws" ce get-cost-and-usage 2>/dev/null | grep -q "ResultsByTime"'

    Section 9: Deployment Verification (only if KIND cluster exists)
    ```bash
    if kind get clusters 2>/dev/null | grep -q "^lab$"; then
      printf '--- Deployment Status ---\n'
      check "App pods running in namespace 'app'" \
        bash -c 'kubectl get pods -n app --context kind-lab 2>/dev/null | grep -q "Running"'
      check "Dashboard accessible at localhost:30080" \
        bash -c 'curl -sf http://localhost:30080/ >/dev/null 2>&1'
    else
      printf '--- Deployment Status ---\n'
      printf '  SKIP  KIND cluster not running — deploy with: cd reference-app && make deploy\n'
    fi
    ```

    **Results summary:**
    - Same pattern: "=== Results: X passed, Y failed ==="
    - Update common fixes to reference SETUP.md (not install-hermes.md)
    - Update reference guides to point to: setup/SETUP.md, setup/llm-access.md

    **Critical:** The script must NOT contain ANY references to "hermes", "HERMES" (except HERMES_LAB_MODE which is the existing env var pattern), hermes login, hermes skills, or install-hermes.md.
  </action>
  <verify>
    <automated>bash -n setup/verify.sh && echo "Syntax OK" && grep -c "PASS\|FAIL\|check " setup/verify.sh && ! grep -qi "hermes installed\|hermes login\|hermes home\|hermes config\|install-hermes" setup/verify.sh && echo "No Hermes checks: PASS"</automated>
  </verify>
  <acceptance_criteria>
    - setup/verify.sh is valid bash syntax (`bash -n verify.sh` exits 0)
    - Contains "Agentic DevOps course" in header comment (not "Hermes")
    - Contains check for "Docker daemon running"
    - Contains check for "kind installed" and "Helm installed"
    - Contains check for "Claude Code" or "OpenCode" (AI tool section)
    - Contains check for "Reference app Cargo workspace"
    - Contains check for "mock-data/cloudwatch/describe-alarms-clean.json"
    - Contains check for "Dashboard accessible at localhost:30080" (conditional on KIND cluster)
    - Does NOT contain `hermes installed`, `hermes login`, `hermes home`, `hermes config`, or reference to `install-hermes.md`
    - Contains `HERMES_LAB_MODE=mock` (existing env var pattern is OK for mock wrapper calls)
    - Contains reference to `setup/SETUP.md` in common fixes section
    - Contains the `check()` helper function
    - Exits 0 when sourced with bash -n (syntax check only)
  </acceptance_criteria>
  <done>verify.sh validates all course prerequisites: Docker, KIND, kubectl, Helm, Node.js, AI coding tool (Claude Code or OpenCode), reference app files, mock data files, mock wrapper smoke tests, and deployment health check. Zero Hermes-specific checks.</done>
</task>

<task type="auto">
  <name>Task 3: Update LLM access docs for Claude Code + OpenCode dual path</name>
  <files>
    setup/llm-access.md
  </files>
  <read_first>
    setup/llm-access.md (current Hermes-focused LLM guide — understand structure to transform),
    .planning/phases/01-foundation/01-CONTEXT.md (D-14 two equal paths, D-15 OpenCode not Crush, D-16 expected outputs for BOTH paths),
    .planning/phases/01-foundation/01-RESEARCH.md (Standard Stack for provider details and rate limits, Pitfall 3 for OpenCode identity, Open Questions for Grok status)
  </read_first>
  <action>
    Rewrite `setup/llm-access.md` for the course's dual-path approach per D-14, D-15, D-16.

    **Transform from:** Hermes-centric model guide (hermes config, hermes run, HERMES_LAB_MODE)
    **Transform to:** Course AI tool guide covering Claude Code and OpenCode with multiple providers

    **New structure:**

    **Title:** "AI Coding Tool Setup and Provider Guide"

    **Section 1: Choose Your Path**
    | Path | Tool | Cost | Best For |
    | Path A | Claude Code | $0 (within Claude Pro/Team subscription) | Participants with existing Claude subscription |
    | Path B | OpenCode | $0 (free providers) | Participants without Claude subscription |

    Note: "Both paths are fully supported. All labs include expected outputs for both tools." (per D-16)

    **Section 2: Path A — Claude Code Setup**
    - What it is: Anthropic's official terminal AI coding agent
    - Requires: Claude Pro ($20/mo) or Claude Team subscription
    - Install: `npm install -g @anthropic-ai/claude-code`
    - First run: `claude` — opens browser for OAuth authentication
    - Default model: Claude Sonnet (used automatically)
    - No API key needed — uses subscription
    - Rate limits: approximately 10M tokens/day within subscription
    - Lab usage pattern: `claude` in project directory, ask questions, review generated code
    - Expected output example for a typical lab interaction

    **Section 3: Path B — OpenCode Setup**
    - What it is: "OpenCode (opencode.ai) is a terminal-based AI coding agent maintained by the SST team. It supports 75+ LLM providers including free options."
    - CRITICAL: "This is sst/opencode from opencode.ai — not the archived opencode-ai/opencode project, and not charmbracelet/crush."
    - Install: `brew install sst/tap/opencode` (macOS) or `curl -fsSL https://opencode.ai/install.sh | sh`
    - Provider setup via `/connect` command inside OpenCode

    **Section 4: Free LLM Providers for OpenCode**

    Provider 1: Google Gemini 2.5 Flash (recommended)
    - Cost: Free, no credit card required
    - Rate limits: 10 RPM / 500 requests per day (as of early 2026 — verify at ai.google.dev/gemini-api/docs/rate-limits)
    - Setup: Go to aistudio.google.com → Get API Key → copy key → in OpenCode: `/connect` → select Google → paste key
    - Best for: All lab exercises. 500 req/day is far more than any lab uses (typical: 5-15 requests)
    - Model name: `gemini-2.5-flash` (NOT gemini-2.0-flash — deprecated Feb 2026)

    Provider 2: Groq (fast inference)
    - Cost: Free, no credit card required
    - Rate limits: 14,400 req/day for llama-3.1-8b-instant, 6,000 TPM
    - Setup: Go to console.groq.com → Create API Key → in OpenCode: `/connect` → select Groq → paste key
    - Best for: Fast inference demos, code-heavy labs where speed matters
    - Model: `llama-3.1-8b-instant`

    Provider 3: OpenRouter (flexible fallback)
    - Cost: Free credits on signup; `:free` suffix models are permanently free
    - Setup: Sign up at openrouter.ai → copy API key → in OpenCode: `/connect` → select OpenRouter
    - Best for: Flexibility and model variety. Use as fallback if Gemini or Groq limits are hit.
    - Recommended model: `anthropic/claude-haiku-4-5` (uses free credits) or `meta-llama/llama-3.1-8b-instruct:free`

    Provider 4: Grok (optional)
    - Cost: May require payment — verify current pricing at console.x.ai before using
    - Setup: Get API key from console.x.ai → in OpenCode: `/connect` → select xAI
    - Note: "Grok's free tier status is uncertain as of April 2026. Use Gemini or Groq as primary free providers."

    **Section 5: Token Budget Awareness**
    - Keep the existing token economics content but rewrite for generic AI tool context (not Hermes-specific)
    - What consumes tokens: system context, conversation history, tool output, AI reasoning
    - Mock mode saves tokens: HERMES_LAB_MODE=mock (keep this env var name — it's used by existing wrappers) returns compact JSON instead of verbose real CLI output
    - Tip: Start each lab fresh — close and reopen your AI tool between labs to reset conversation history

    **Section 6: Estimated Course Cost**
    | Path | Total tokens (all labs) | Estimated cost |
    | Claude Code (Claude Pro) | ~200K-400K | $0 — within subscription |
    | OpenCode + Gemini | ~200K-400K | $0 — within free tier |
    | OpenCode + Groq | ~200K-400K | $0 — within free tier |
    | OpenCode + OpenRouter | ~200K-400K | $0-$1 — within free credits |

    "Bottom line: The complete course costs effectively $0 in LLM API fees with any provider."

    **Remove all references to:**
    - `hermes` CLI commands (hermes config set, hermes run, hermes login)
    - `~/.hermes/config.yaml`
    - `hermes config set model`
    - Module numbers that reference Hermes modules (7, 8, 10-13)
    - claude-haiku-4-5 as default (Claude Code uses Sonnet automatically)
    - Hugging Face Inference (too slow/unreliable to recommend)
  </action>
  <verify>
    <automated>test -f setup/llm-access.md && grep -c "Claude Code" setup/llm-access.md && grep -c "OpenCode" setup/llm-access.md && grep -c "opencode.ai" setup/llm-access.md && ! grep -qi "hermes config\|hermes run\|hermes login\|~/.hermes" setup/llm-access.md && echo "No Hermes commands: PASS"</automated>
  </verify>
  <acceptance_criteria>
    - setup/llm-access.md contains "Claude Code" at least 3 times
    - setup/llm-access.md contains "OpenCode" at least 5 times
    - setup/llm-access.md contains "opencode.ai" at least once
    - setup/llm-access.md contains "Gemini 2.5 Flash" (NOT "Gemini 2.0 Flash")
    - setup/llm-access.md contains "Groq" with rate limit info
    - setup/llm-access.md contains "OpenRouter"
    - setup/llm-access.md does NOT contain `hermes config`, `hermes run`, `hermes login`, or `~/.hermes` (Hermes CLI commands)
    - setup/llm-access.md does NOT contain "crush" or "charmbracelet"
    - setup/llm-access.md contains "sst/tap/opencode" or "opencode.ai/install.sh" (correct install command)
    - setup/llm-access.md contains a cost estimate table showing $0 for all paths
    - setup/llm-access.md contains "/connect" (OpenCode provider setup command)
  </acceptance_criteria>
  <done>LLM access guide covers Claude Code and OpenCode dual paths equally. Four providers documented (Gemini, Groq, OpenRouter, Grok) with rate limits and setup steps. No Hermes CLI references. Cost is $0 for all paths.</done>
</task>

</tasks>

<verification>
- SETUP.md walks a participant from zero to working environment with both AI tool paths
- verify.sh checks all course prerequisites and reports clear PASS/FAIL with no Hermes artifacts
- llm-access.md documents both Claude Code and OpenCode with free providers
- No file in setup/ references "hermes config", "hermes login", or "install-hermes.md"
- `bash -n setup/verify.sh` passes (valid syntax)
</verification>

<success_criteria>
- SETUP.md is a complete setup guide (150+ lines) covering Docker, KIND, Helm, Claude Code, OpenCode, reference app deploy, and verification
- verify.sh validates: Docker, KIND, kubectl, Helm, Node.js, AI tool (Claude Code or OpenCode), reference app files, mock data, wrappers, and deployment health
- llm-access.md covers 2 tool paths and 4 LLM providers with rate limits and setup instructions
- Zero references to Hermes CLI commands across all three files
- All three files are internally consistent (SETUP.md references verify.sh and llm-access.md)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
