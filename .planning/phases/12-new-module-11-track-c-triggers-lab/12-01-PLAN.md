---
phase: 12-new-module-11-track-c-triggers-lab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - course-site/docs/module-11-triggers/lab/LAB-track-c-kubernetes.mdx
autonomous: true
requirements:
  - TRKC-01

must_haves:
  truths:
    - "LAB-track-c-kubernetes.mdx exists with all 5 trigger types as hands-on steps"
    - "Zero HERMES_LAB_MODE references in the Track C lab"
    - "Zero MOCK_DATA_DIR references in the Track C lab"
    - "Zero HERMES_LAB_TRACK references in the Track C lab"
    - "AlertManager setup is self-contained within the lab (Helm upgrade, PrometheusRule apply, UI verify)"
    - "Lab duration totals 90 minutes (70 guided + 20 free explore)"
    - "Frontmatter id is module-11-lab-track-c"
  artifacts:
    - path: "course-site/docs/module-11-triggers/lab/LAB-track-c-kubernetes.mdx"
      provides: "Track C dedicated triggers lab with 5 trigger types"
      contains: "id: module-11-lab-track-c"
  key_links:
    - from: "LAB-track-c-kubernetes.mdx Step 2"
      to: "infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml"
      via: "kubectl apply -f"
      pattern: "kubectl apply -f infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml"
    - from: "LAB-track-c-kubernetes.mdx Step 2"
      to: "infrastructure/scenarios/k8s/02-crashloop-backoff.yaml"
      via: "kubectl apply -f"
      pattern: "kubectl apply -f infrastructure/scenarios/k8s/02-crashloop-backoff.yaml"
    - from: "LAB-track-c-kubernetes.mdx Step 3"
      to: "infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml"
      via: "kubectl apply -f"
      pattern: "kubectl apply -f infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml"
    - from: "LAB-track-c-kubernetes.mdx Step 4"
      to: "infrastructure/scenarios/k8s/github-webhook/smee-setup.sh"
      via: "script execution"
      pattern: "infrastructure/scenarios/k8s/github-webhook/smee-setup.sh"
---

<objective>
Create the dedicated Track C triggers lab file (LAB-track-c-kubernetes.mdx) for Module 11.

Purpose: Track C learners need a K8s-native triggers lab that uses real KIND infrastructure throughout -- no mock mode, no wrapper scripts, no HERMES_LAB_MODE. This lab covers 5 trigger types (Hermes cron, AlertManager webhook, K8s CronJob, GitHub webhook, Telegram bot) in 90 minutes.

Output: A single ~600-800 line MDX file with frontmatter, prerequisites, 5 guided steps, verification checklist, and 3 free-explore challenges.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-new-module-11-track-c-triggers-lab/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LAB-track-c-kubernetes.mdx with all 5 trigger types</name>
  <files>course-site/docs/module-11-triggers/lab/LAB-track-c-kubernetes.mdx</files>
  <read_first>
    - .planning/phases/12-new-module-11-track-c-triggers-lab/12-CONTEXT.md (step outline with exact commands)
    - course-site/docs/module-11-triggers/lab/LAB.mdx (source material -- extract Track C patterns, adapt for no-mock)
    - course-site/docs/module-08-tool-integration/lab/LAB-track-c-kubernetes.mdx (Track C lab pattern: frontmatter, structure, tone)
    - infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml (verify PrometheusRule filename)
    - infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml (verify CronJob manifest filename)
    - infrastructure/scenarios/k8s/cronjob/Dockerfile (verify Dockerfile exists)
    - infrastructure/scenarios/k8s/github-webhook/smee-setup.sh (verify script filename)
    - infrastructure/scenarios/k8s/github-webhook/agent-prompt-template.txt (verify template file)
    - infrastructure/scenarios/k8s/github-webhook/sample-pr-payload.json (verify sample payload)
    - infrastructure/helm/prometheus-lab-values.yaml (verify Helm values file)
  </read_first>
  <action>
Create `course-site/docs/module-11-triggers/lab/LAB-track-c-kubernetes.mdx` following the CONTEXT.md step outline exactly.

**Frontmatter (exact):**
```yaml
id: module-11-lab-track-c
title: "Module 11 Lab: Triggers & Scheduling for Kubernetes (Track C)"
sidebar_label: "Lab — Track C: Kubernetes"
sidebar_position: 2
```

**H1 heading:** `# Module 11 Lab: Triggers & Scheduling for Kubernetes (Track C)`

**Header block:**
- Duration: 90 minutes (70 min guided + 20 min free explore)
- Track: C -- Kubernetes Health & Self-Healing
- Prerequisites: Module 8 Track C lab complete (working `track-c` profile); KIND cluster running
- Outcome: Working examples of all 5 trigger patterns using real KIND infrastructure

**CRITICAL CONSTRAINTS -- read these before writing ANY step:**
1. ZERO `HERMES_LAB_MODE` environment variable references anywhere in the file
2. ZERO `MOCK_DATA_DIR` environment variable references anywhere
3. ZERO `HERMES_LAB_TRACK` environment variable references anywhere
4. ZERO `HERMES_LAB_SCENARIO` environment variable references anywhere
5. ZERO `HERMES_LAB_GOVERNANCE` environment variable references anywhere
6. ZERO references to `infrastructure/wrappers` or `PATH="$(pwd)/infrastructure/wrappers:$PATH"`
7. ZERO `[MOCK MODE]` in any expected output
8. All expected outputs should reflect REAL kubectl results from a KIND cluster
9. Agent runs real kubectl against real KIND cluster -- not simulated

**Prerequisites section (5 min):**
```bash
# Verify KIND cluster
kubectl cluster-info --context kind-lab

# Verify Track C agent
hermes -p track-c chat
# Type "hello" to confirm agent responds, then exit

# Verify Hermes gateway capability
hermes --version
```

**GUIDED PHASE -- 70 minutes** (use this exact heading)

**Step 1: Hermes Cron -- Daily K8s Health Check (10 min)**
- Create cron job using `hermes cron create` with positional args (schedule first, prompt second):
  ```bash
  hermes cron create "0 8 * * *" \
    "Run morning pod health check across all namespaces. Report only if pods or nodes show issues." \
    --name "daily-k8s-check" \
    --skill "kubernetes-health" \
    --deliver local
  ```
- Verify: `hermes cron list`
- Trigger manually: `hermes cron trigger daily-k8s-check`
- Expected output should show REAL kubectl results (pods running in kind-lab cluster, actual namespace listing) -- NOT mock mode output
- Lifecycle demo: `hermes cron pause daily-k8s-check`, `hermes cron status`, `hermes cron resume daily-k8s-check`, `hermes cron status`
- Include the cron expression reference table from the unified lab (the 4-row table with expressions and meanings)
- Include the argument table (schedule, prompt, --name, --skill, --deliver, --repeat) from the unified lab
- Include the :::note about positional argument order
- Include the :::note about --deliver local

**Step 2: AlertManager -- Prometheus Stack + Live Webhook (20 min)**
This step is self-contained -- do NOT assume AlertManager was set up elsewhere.

Sub-step 2a: Enable AlertManager in Helm values
```bash
yq '.alertmanager.enabled' infrastructure/helm/prometheus-lab-values.yaml
# Expected: true

helm upgrade --install kube-prometheus prometheus-community/kube-prometheus-stack \
  --namespace monitoring --create-namespace \
  -f infrastructure/helm/prometheus-lab-values.yaml

kubectl wait --for=condition=Ready pod \
  -l app.kubernetes.io/name=alertmanager \
  -n monitoring --timeout=120s
```

Sub-step 2b: Apply PrometheusRule
```bash
kubectl apply -f infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml
```
Verify: `kubectl get prometheusrule -n monitoring hermes-lab-rules -o yaml | yq '.spec.groups[0].rules[0].alert'`
Expected: `PodCrashLooping`

Include the :::tip about `release` label from unified lab Step 9.

Sub-step 2c: Start gateway and subscribe
```bash
hermes gateway run    # Terminal 1
hermes webhook subscribe alertmanager \
  --events "alertmanager-alert" \
  --prompt "AlertManager PodCrashLooping alert fired. Details: {alerts}. Load the sre-k8s-pod-health skill and diagnose the affected pod in the namespace shown in the alert labels." \
  --skill "sre-k8s-pod-health" \
  --deliver local     # Terminal 2
```

Include the :::warning about `{alerts}` vs `{alerts[0].labels.pod}` from unified lab.

Sub-step 2d: Apply crashloop scenario and observe
```bash
kubectl apply -f infrastructure/scenarios/k8s/02-crashloop-backoff.yaml
watch kubectl get pods -n k8s-trouble-crashloop
```

Include the expected timeline (t=0 through t=125s) from the unified lab Steps 9-10.
Include teaching point about cron vs webhook (from unified lab Step 7 info box).

Cleanup:
```bash
kubectl delete -f infrastructure/scenarios/k8s/02-crashloop-backoff.yaml
hermes webhook unsubscribe alertmanager
```

**Step 3: K8s CronJob -- Agent as a Kubernetes Workload (15 min)**
```bash
docker build -t hermes-lab:cronjob infrastructure/scenarios/k8s/cronjob/
kind load docker-image hermes-lab:cronjob --name lab
kubectl create secret generic hermes-secrets \
  --from-literal=anthropic-api-key="$ANTHROPIC_API_KEY"
kubectl apply -f infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml -l track=track-c
watch kubectl get jobs,pods
```
- View logs: `kubectl logs -l job-name=$(kubectl get jobs -o jsonpath='{.items[-1].metadata.name}')`
- Include the :::tip comparing "Hermes cron when..." vs "K8s CronJob when..." from unified lab Step 11
- Include :::info about image size from unified lab Step 11
- Cleanup: delete CronJob and secret

**Step 4: GitHub Webhook -- PR Review Bot (15 min)**
Follow the exact pattern from unified lab Steps 12-13, but WITHOUT any env var block containing HERMES_LAB_MODE etc.

Key differences from unified lab:
- Only set `GITHUB_TOKEN` and `SMEE_URL` env vars (no HERMES_LAB_* vars)
- Include all 4 sub-steps: smee.io channel, GitHub PAT, gateway+smee-client, webhook config in repo
- Include the `hermes webhook subscribe github` command with `--deliver github_comment`
- Include the Solo Learner fallback with `--deliver local` and sample payload injection
- Include the :::warning about route name matching subscription name

**Step 5: Telegram Bot -- Chat Interface (10 min)**
Follow the pattern from unified lab Steps 14-15, but WITHOUT any env var block containing HERMES_LAB_MODE etc.

Key differences from unified lab:
- Only set `TELEGRAM_BOT_TOKEN` and `TELEGRAM_ALLOWED_USERS` env vars
- Include @BotFather setup (7-step numbered list from unified lab)
- Include @userinfobot step for user ID
- Include gateway start with Telegram adapter
- Send `/help`, then `/diagnose k8s-trouble-crashloop`
- Include :::warning about Telegram long-polling conflict
- Do NOT include Step 16 (governance escalation) -- that belongs in Module 13

**Verification Checklist (5 min):**
A checklist with checkboxes for all 5 triggers verified.

**FREE EXPLORE PHASE -- 20 minutes** (use this exact heading)

3 challenges from CONTEXT.md:
1. Cross-namespace cron (check kube-system + default) -- 10 min
2. AlertManager to Telegram notification chain -- 10 min
3. GitHub PR triggers full diagnosis and posts report -- 10 min

**Writing style guidelines:**
- Match the tone of the existing unified LAB.mdx and Module 8 Track C lab
- Use :::tip, :::info, :::warning, :::danger admonitions appropriately
- Include expected output blocks after every verification command
- Every command block should have a preceding sentence explaining what it does
- Use `bash` code fence language for all command blocks
- Reference infrastructure files by their actual paths (verify against read_first files)
  </action>
  <verify>
    <automated>
# Verify file exists and has correct frontmatter
grep -c "id: module-11-lab-track-c" course-site/docs/module-11-triggers/lab/LAB-track-c-kubernetes.mdx && \
# Verify zero HERMES_LAB_MODE references
test $(grep -c "HERMES_LAB_MODE" course-site/docs/module-11-triggers/lab/LAB-track-c-kubernetes.mdx) -eq 0 && echo "No HERMES_LAB_MODE: PASS" && \
# Verify zero MOCK_DATA_DIR references
test $(grep -c "MOCK_DATA_DIR" course-site/docs/module-11-triggers/lab/LAB-track-c-kubernetes.mdx) -eq 0 && echo "No MOCK_DATA_DIR: PASS" && \
# Verify zero HERMES_LAB_TRACK references
test $(grep -c "HERMES_LAB_TRACK" course-site/docs/module-11-triggers/lab/LAB-track-c-kubernetes.mdx) -eq 0 && echo "No HERMES_LAB_TRACK: PASS" && \
# Verify zero HERMES_LAB_SCENARIO references
test $(grep -c "HERMES_LAB_SCENARIO" course-site/docs/module-11-triggers/lab/LAB-track-c-kubernetes.mdx) -eq 0 && echo "No HERMES_LAB_SCENARIO: PASS" && \
# Verify zero HERMES_LAB_GOVERNANCE references
test $(grep -c "HERMES_LAB_GOVERNANCE" course-site/docs/module-11-triggers/lab/LAB-track-c-kubernetes.mdx) -eq 0 && echo "No HERMES_LAB_GOVERNANCE: PASS" && \
# Verify zero infrastructure/wrappers references
test $(grep -c "infrastructure/wrappers" course-site/docs/module-11-triggers/lab/LAB-track-c-kubernetes.mdx) -eq 0 && echo "No wrappers: PASS" && \
# Verify all 5 trigger types present
grep -c "Hermes Cron" course-site/docs/module-11-triggers/lab/LAB-track-c-kubernetes.mdx && \
grep -c "AlertManager" course-site/docs/module-11-triggers/lab/LAB-track-c-kubernetes.mdx && \
grep -c "K8s CronJob" course-site/docs/module-11-triggers/lab/LAB-track-c-kubernetes.mdx && \
grep -c "GitHub Webhook" course-site/docs/module-11-triggers/lab/LAB-track-c-kubernetes.mdx && \
grep -c "Telegram Bot" course-site/docs/module-11-triggers/lab/LAB-track-c-kubernetes.mdx && \
# Verify AlertManager setup is self-contained (helm upgrade and PrometheusRule apply present)
grep -c "helm upgrade" course-site/docs/module-11-triggers/lab/LAB-track-c-kubernetes.mdx && \
grep -c "prometheus-rules.yaml" course-site/docs/module-11-triggers/lab/LAB-track-c-kubernetes.mdx && \
# Verify 90 min duration
grep -c "90 minutes" course-site/docs/module-11-triggers/lab/LAB-track-c-kubernetes.mdx && \
echo "ALL CHECKS PASSED"
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists at course-site/docs/module-11-triggers/lab/LAB-track-c-kubernetes.mdx
    - Frontmatter id is exactly "module-11-lab-track-c"
    - Frontmatter sidebar_position is 2
    - ZERO occurrences of HERMES_LAB_MODE in the file
    - ZERO occurrences of MOCK_DATA_DIR in the file
    - ZERO occurrences of HERMES_LAB_TRACK in the file
    - ZERO occurrences of HERMES_LAB_SCENARIO in the file
    - ZERO occurrences of HERMES_LAB_GOVERNANCE in the file
    - ZERO occurrences of infrastructure/wrappers in the file
    - All 5 trigger type step headings present (Hermes Cron, AlertManager, K8s CronJob, GitHub Webhook, Telegram Bot)
    - helm upgrade command present (self-contained AlertManager setup)
    - prometheus-rules.yaml kubectl apply present
    - 02-crashloop-backoff.yaml kubectl apply present
    - agent-health-check.yaml kubectl apply present
    - smee-setup.sh reference present
    - Duration header says 90 minutes
    - File is 500-900 lines
  </acceptance_criteria>
  <done>LAB-track-c-kubernetes.mdx exists with all 5 trigger types, zero mock-mode references, self-contained AlertManager setup, and 90-minute duration</done>
</task>

</tasks>

<verification>
- `grep -c "HERMES_LAB_MODE" course-site/docs/module-11-triggers/lab/LAB-track-c-kubernetes.mdx` returns 0
- `grep -c "MOCK_DATA_DIR" course-site/docs/module-11-triggers/lab/LAB-track-c-kubernetes.mdx` returns 0
- All 5 trigger types have dedicated step headings
- AlertManager Helm upgrade is self-contained in the lab
- File length is between 500 and 900 lines
</verification>

<success_criteria>
- LAB-track-c-kubernetes.mdx is a complete, standalone lab file
- A Track C learner can follow it start-to-finish without referencing the unified LAB.mdx
- All 5 triggers use real KIND infrastructure (no mock mode)
- Duration sums to 90 minutes across all sections
</success_criteria>

<output>
After completion, create `.planning/phases/12-new-module-11-track-c-triggers-lab/12-01-SUMMARY.md`
</output>
