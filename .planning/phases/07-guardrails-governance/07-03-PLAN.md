---
phase: 07-guardrails-governance
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified:
  - course-site/docs/module-13-governance/lab/LAB.mdx
  - modules/module-13-governance/LAB.md
  - course-site/docs/module-13-governance/reading/reference.mdx
  - course-site/docs/module-13-governance/quiz/QUIZ.mdx
autonomous: true
requirements:
  - GOV-03
must_haves:
  truths:
    - "A participant opening Module 13 lab finds a GUIDED PHASE that now has 14 steps (was 10) — 4 new steps covering L4 Apply, Attempt blocked, Attempt allowed, Audit"
    - "A participant following new Step 9 (Apply L4) exports HERMES_LAB_GOVERNANCE=L4 and HERMES_LAB_TRACK=track-{a,b,c}, copies governance-L4-track-*.yaml to their profile, and sees a diff from L3 showing the wrapper_allowlist additions"
    - "A participant following new Step 10 (Attempt blocked) runs a track-specific destructive command (Track A DROP TABLE, Track B aws ec2 terminate-instances, Track C kubectl delete pod) and observes the GOVERNANCE REJECTED banner on stderr"
    - "A participant following new Step 11 (Attempt allowed) runs a track-specific safe command (Track A SELECT, Track B aws ec2 describe-instances, Track C kubectl get pods) and sees it pass through to mock output"
    - "A participant following new Step 12 (Audit) queries the session audit trail and sees the rejection event recorded"
    - "Every Phase 7 lab step includes a complete export block listing HERMES_LAB_MODE, HERMES_LAB_SCENARIO, HERMES_LAB_GOVERNANCE, HERMES_LAB_TRACK, MOCK_DATA_DIR, and PATH (per CONTEXT.md D-05 explicit user request)"
    - "The existing Step 4 (Diff L1 to L2) diff output block is updated to reflect the populated wrapper_allowlist additions"
    - "The existing Step 6 (Diff L2 to L3) diff output block is updated to reflect the populated wrapper_allowlist progression from L2 to L3"
    - "Module 13 reference.mdx section 1 YAML blocks show populated wrapper_allowlist sections alongside the preserved SOUL.md load-bearing narrative for Track B/C"
    - "Module 13 reference.mdx has a new subsection documenting HERMES_LAB_GOVERNANCE and HERMES_LAB_TRACK env vars"
    - "The three-layer defense model is explicitly taught: Layer 1 (wrapper_allowlist) + Layer 2 (DANGEROUS_PATTERNS) + Layer 3 (SOUL.md NEVER rules). Track A fires all three for DROP; Track B/C fire Layers 1 and 3 only for destructive commands"
    - "The source-of-truth modules/module-13-governance/LAB.md and Docusaurus course-site/docs/module-13-governance/lab/LAB.mdx have identical step structure (both renumbered the same way)"
  artifacts:
    - path: "course-site/docs/module-13-governance/lab/LAB.mdx"
      provides: "Module 13 lab with 4 new L4 governance enforcement steps"
      contains: "GOVERNANCE REJECTED"
    - path: "modules/module-13-governance/LAB.md"
      provides: "Source-of-truth Module 13 lab mirror"
      contains: "HERMES_LAB_GOVERNANCE"
    - path: "course-site/docs/module-13-governance/reading/reference.mdx"
      provides: "Reference doc with populated wrapper_allowlist examples and env var documentation"
      contains: "wrapper_allowlist"
  key_links:
    - from: "course-site/docs/module-13-governance/lab/LAB.mdx Step 9"
      to: "governance/governance-L4-track-*.yaml (from Plan 07-01)"
      via: "cat command in the lab step + expected diff output"
      pattern: "governance-L4-track-"
    - from: "course-site/docs/module-13-governance/lab/LAB.mdx Step 10"
      to: "infrastructure/wrappers/mock-{kubectl,aws,psql} (from Plan 07-01)"
      via: "participant runs wrapped command and sees banner"
      pattern: "GOVERNANCE REJECTED"
    - from: "course-site/docs/module-13-governance/reading/reference.mdx section 1"
      to: "governance/governance-L2.yaml populated state (from Plan 07-01)"
      via: "reference.mdx embeds the populated YAML content as a teaching example"
      pattern: "wrapper_allowlist"
---

<objective>
Extend the Module 13 lab with 4 new GUIDED PHASE steps (Apply L4, Attempt blocked, Attempt allowed, Audit) that teach the populated wrapper_allowlist mechanism built by Plans 07-01 and 07-02. Update the existing Steps 4 and 6 diff output blocks to reflect the now-populated governance yamls. Update the Module 13 reading reference.mdx section 1 to show populated YAML examples and add a new subsection documenting the HERMES_LAB_GOVERNANCE and HERMES_LAB_TRACK env vars. Optionally add a quiz question testing three-layer defense understanding.

Purpose: This is the participant-facing half of Phase 7. Without this plan, the infrastructure built in Plans 07-01 and 07-02 would exist but not be teachable. This plan turns the mechanical enforcement into a guided walkthrough where participants observe rejection, pass-through, and audit trails for themselves.

This plan depends on Plans 07-01 and 07-02 because:
• Step 9 (Apply L4) reads governance/governance-L4-track-*.yaml files populated by Plan 07-01
• Steps 10-11 (Attempt blocked/allowed) invoke the extended wrappers from Plan 07-01
• The expected cat config.yaml outputs in the lab must match the profile configs from Plan 07-02
• The diff output blocks in Steps 4 and 6 must reflect the populated yamls from Plan 07-01

Output:
• Module 13 lab (both .mdx and .md copies) with 17 steps total (12 guided + 5 tail including 3 free-explore renumbered)
• Updated diff blocks in Steps 4 and 6
• Updated reference.mdx section 1 YAML examples
• New reference.mdx subsection on HERMES_LAB_GOVERNANCE and HERMES_LAB_TRACK env vars
• Optional new quiz question on three-layer defense
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-guardrails-governance/07-CONTEXT.md
@.planning/phases/07-guardrails-governance/07-RESEARCH.md
@.planning/phases/07-guardrails-governance/07-01-SUMMARY.md
@.planning/phases/07-guardrails-governance/07-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- The current lab structure. Executor must read these before editing. -->

Module 13 lab current structure (both files identical in step layout, differ only in MDX frontmatter/callouts):
• Step 1: Prerequisites (5 min)
• Step 2: View All Governance Levels (5 min)
• Step 3: Apply L1 (10 min)
• Step 4: Diff L1 to L2 (5 min) — UPDATE diff output block
• Step 5: Apply L2 (10 min)
• Step 6: Diff L2 to L3 (5 min) — UPDATE diff output block
• Step 7: Apply L3 (10 min)
• Step 8: Read Your Session Audit Trail (10 min)
• Step 9: Review DANGEROUS_PATTERNS (5 min) — RENUMBER to 13
• Step 10: Restore Agent to Working Config (5 min) — RENUMBER to 14
• Step 11: Challenge 1 — Write Promotion Criteria — RENUMBER to 15
• Step 12: Challenge 2 — Add a Command to command_allowlist — RENUMBER to 16
• Step 13: Challenge 3 — Compare Track A vs Track B L4 Governance — RENUMBER to 17

Phase 7 INSERTION STRATEGY (per 07-RESEARCH.md Pitfall 2 recommendation): Insert 4 new steps AFTER current Step 8 (audit) and BEFORE current Step 9 (DANGEROUS_PATTERNS review).

Rationale: This keeps the L4 walkthrough within the L1→L2→L3→L4 progression flow. The existing Step 9 (DANGEROUS_PATTERNS review) and Step 10 (Restore) remain bookends AFTER the L4 steps. Participants see the full progression, then review the mechanical layer (DANGEROUS_PATTERNS), then restore. This is the cleanest pedagogical flow.

Final numbering after Phase 7 insertion:
• Old 1-8 stay as 1-8
• NEW Step 9: Apply L4 — Populated Allowlists
• NEW Step 10: Attempt a Blocked Command at L4
• NEW Step 11: Attempt an Allowed Command at L4
• NEW Step 12: Query Audit Trail for the Rejection Event
• Old 9 (DANGEROUS_PATTERNS) becomes Step 13
• Old 10 (Restore) becomes Step 14
• Old 11 (Challenge 1) becomes Step 15
• Old 12 (Challenge 2) becomes Step 16
• Old 13 (Challenge 3) becomes Step 17

Environment variable table (D-05 + research Open Question 1 resolution):
• HERMES_LAB_MODE: mock | live — Route to mock JSON or real infra (Phase 1)
• HERMES_LAB_SCENARIO: clean | messy | crashloop | etc — Select scenario fixture (Phase 1 + 6)
• HERMES_LAB_GOVERNANCE: L1 | L2 | L3 | L4 — Select wrapper_allowlist level (Phase 7 NEW)
• HERMES_LAB_TRACK: track-a | track-b | track-c — Required when GOVERNANCE=L4 (Phase 7 NEW)
• MOCK_DATA_DIR: path — Mock fixtures location (Phase 1)
• PATH additions: infrastructure/wrappers:$PATH — Wrapper precedence (Phase 1)

Track-specific test commands for the new Steps 10 and 11:
• Track A blocked: psql -c "DROP TABLE test_archive_old" — rejected at Layer 1 (not in wrapper_allowlist.psql)
• Track A allowed: psql -c "SELECT pid, state, query FROM pg_stat_activity" — pass-through to mock output
• Track B blocked: aws ec2 terminate-instances --instance-ids i-0abc123def456789a — rejected at Layer 1
• Track B allowed: aws ec2 describe-instances — pass-through to mock JSON
• Track C blocked: kubectl delete pod api-deployment-def456 — rejected at Layer 1
• Track C allowed: kubectl get pods — pass-through to mock JSON

Three-layer defense narrative per track:
• Track A DROP TABLE: Layer 1 wrapper rejects (not in allowlist), Layer 2 DANGEROUS_PATTERNS would fire (SQL DROP is hardcoded), Layer 3 SOUL.md NEVER rule. All three fire simultaneously — ideal teaching example.
• Track B aws ec2 terminate-instances: Layer 1 wrapper rejects (not in allowlist). Layer 2 does NOT fire (not in DANGEROUS_PATTERNS). Layer 3 SOUL.md NEVER rule. Two layers, not three.
• Track C kubectl delete pod: Layer 1 wrapper rejects (not in allowlist). Layer 2 does NOT fire (not in DANGEROUS_PATTERNS). Layer 3 SOUL.md NEVER rule. Two layers, not three.

This is the honest teaching moment the course has been building toward.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update Steps 4 and 6 diff blocks, insert 4 new L4 steps, renumber tail in course-site MDX</name>
  <files>course-site/docs/module-13-governance/lab/LAB.mdx</files>

  <read_first>
    - course-site/docs/module-13-governance/lab/LAB.mdx — FULL file (749 lines). Read all of Steps 4, 6, 8, 9, 10, 11, 12, 13 for exact current text
    - governance/governance-L1.yaml — to confirm L1 has no wrapper_allowlist (Step 4 diff shows added lines only)
    - governance/governance-L2.yaml — AFTER Plan 07-01 runs, to get the exact wrapper_allowlist block for the Step 4 expected diff output
    - governance/governance-L3.yaml — AFTER Plan 07-01 runs, for Step 6 expected diff output
    - governance/governance-L4-track-c.yaml — AFTER Plan 07-01 runs, for Step 9 (Apply L4) expected content
    - .planning/phases/07-guardrails-governance/07-01-SUMMARY.md — for confirmed runtime behavior of wrappers
    - .planning/phases/07-guardrails-governance/07-02-SUMMARY.md — for confirmed profile config content
    - .planning/phases/07-guardrails-governance/07-CONTEXT.md — D-05 (env var block on every step), D-15 (extend not rewrite), D-16 (diff block updates)
    - .planning/phases/07-guardrails-governance/07-RESEARCH.md — Pitfall 2 (step count 13 not 10), Pitfall 6 (banner to stderr), three-layer defense model
  </read_first>

  <action>
SCOPE: Module 13 lab Docusaurus MDX file, 749 lines. This task does 4 edits in one file:

1. UPDATE Step 4 diff output block (around lines 173-185) to show wrapper_allowlist additions at L2
2. UPDATE Step 6 diff output block (around lines 293-303) to show wrapper_allowlist additions at L3
3. INSERT 4 new steps between Step 8 (ends around line 486) and current Step 9 "Review DANGEROUS_PATTERNS" (starts around line 489)
4. RENUMBER existing Steps 9-13 to 13-17

EDIT 1: STEP 4 DIFF BLOCK — lines 173 through 185

The current block is a fenced diff code block inside Step 4 showing only the platform_toolsets change between L1 and L2. After Plan 07-01 runs, the actual diff output will be longer because L2 now has a populated wrapper_allowlist. Replace the current diff output code fence so it shows BOTH the platform_toolsets change AND the new wrapper_allowlist block that is added at L2.

The new Step 4 expected-output content must include:
• The existing platform_toolsets diff lines (web,skills vs terminal,file,web,skills)
• Below those, new diff lines showing L1 had only a comment placeholder (wrapper_allowlist not present at L1) and L2 has the added wrapper_allowlist: block with kubectl/aws/psql subsections
• An abbreviated list inside each subsection (L2 kubectl shows "get pods", "get pod ", "describe pod ", "logs ", "top pods", and an ellipsis "... 4 more entries ..." for readability — the lab abbreviates the full 9-entry list)
• Same abbreviation pattern for aws (show "sts get-caller-identity", "ec2 describe-", "rds describe-", "cloudwatch get-metric-", "ce get-", plus "... 5 more entries ...")
• Same abbreviation pattern for psql (show "SELECT ", "EXPLAIN ", "SHOW ", plus "... 4 more entries ...")
• Followed by a prose paragraph explaining: "Two keys changed: platform_toolsets.cli gains terminal and file, AND a new wrapper_allowlist section is added with per-tool subsections listing the L2 baseline of read-only commands."

Use standard markdown diff fence syntax: three backticks plus "diff", then the `<` prefix for L1 lines, `>` prefix for L2 lines, and a line of three dashes as the diff separator (this is standard diff format; the executor writes this into the .mdx file so the reader sees the diff rendering).

EDIT 2: STEP 6 DIFF BLOCK — lines 293 through 303

Similar to Edit 1. The current Step 6 diff block shows only the approvals.mode change between L2 and L3. Replace with an expanded diff block that shows BOTH:
• The existing approvals.mode change (manual → smart)
• The wrapper_allowlist additions at L3 (L2 baseline plus L3 investigation commands)

For L3 additions, show:
• kubectl L3 adds "rollout history", "rollout status", "diff ", "explain "
• aws L3 adds "logs describe-", "logs get-log-events", "iam get-", "iam list-"
• psql L3 adds "EXPLAIN ANALYZE ", "DELETE FROM ", "UPDATE "

Use prose paragraph form rather than literal diff syntax if the diff gets too long. Format suggestion: "Two key categories changed: approvals.mode from manual to smart, AND wrapper_allowlist gains investigation-level additions per tool — kubectl adds rollout history/status/diff/explain, aws adds logs and IAM read-only, psql adds EXPLAIN ANALYZE and scoped DELETE/UPDATE."

Add a teaching callout as an :::info admonition block:
> Title: "Teaching moment: the wrapper_allowlist diff"
> Body: "The wrapper_allowlist additions at L3 are the concrete form of 'trust escalation.' At L2 the agent runs read-only commands. At L3 it gains investigation commands (history, logs, explain) that are still non-destructive but reveal more state. This is a small, observable change — but it is the entire content of the governance promotion decision."

EDIT 3: INSERT 4 NEW STEPS BETWEEN STEP 8 AND STEP 9

Locate the line `## Step 9: Review DANGEROUS_PATTERNS (5 min)` in the current file (around line 489). BEFORE that heading, insert 4 new step sections. Each section ends with a standard markdown horizontal rule (three dashes on their own line).

STEP 9 NEW HEADING: "## Step 9: Apply L4 — Populated Allowlists (10 min)"

Body of Step 9 must include:
• Intro paragraph: "In Steps 3, 5, and 7 you applied L1, L2, and L3 by editing only the approvals.mode and platform_toolsets.cli keys. At L4, Phase 7 adds a new key: wrapper_allowlist. This is the course-local enforcement list read by the mock wrappers."
• Prose instruction: "First, set the complete environment variable block for your track. This is the block every Phase 7 step needs — keep it in your shell history so you can re-export it when you start a new terminal:"
• Three fenced bash code blocks labeled Track A, Track B, Track C, each containing the complete export block (HERMES_LAB_MODE=mock, HERMES_LAB_SCENARIO=clean, HERMES_LAB_GOVERNANCE=L4, HERMES_LAB_TRACK=track-X, MOCK_DATA_DIR="$PWD/course/infrastructure/mock-data", PATH="$PWD/course/infrastructure/wrappers:$PATH")
• Solo Learner :::info admonition: "If you are following the Udemy self-paced version, keep the above block in a scratchpad file like ~/.lab-env and source it at the start of each terminal session: `source ~/.lab-env`. This avoids retyping the 6 exports every time."
• Instruction: "Apply the L4 governance config to your profile. For Track A:" followed by a fenced bash block running `cat course/governance/governance-L4-track-a.yaml` (with comment noting B and C equivalents)
• Expected output prose for Track C: "You should see the L4-track-c.yaml content including a populated wrapper_allowlist.kubectl section ending with \"apply \" and \"rollout undo\". These are the two L4-only additions on top of the L3 baseline."
• Diff instruction: "Now diff the L3 file against your track's L4 file to see exactly what L4 unlocks:" followed by a fenced bash block: `diff course/governance/governance-L3.yaml course/governance/governance-L4-track-c.yaml`
• Expected diff prose: "The diff shows the additions at the end of the wrapper_allowlist.kubectl list — \"apply \" and \"rollout undo\" — plus Track C-specific header comments. Track A and Track B show their analogous additions (INSERT for A, create-tags for B)."
• :::info admonition titled "Teaching callout: what L4 means" with body: "L4 is NOT 'no governance.' L4 is 'targeted mutations allowed, with smart-approval and SOUL.md NEVER rules still active as Layers 2 and 3.' The wrapper_allowlist addition is Layer 1 — the command prefix must be in the list. Layer 2 (DANGEROUS_PATTERNS) still fires for patterns like SQL DROP. Layer 3 (SOUL.md NEVER rules) still refuses destructive intent regardless of the allowlist."
• End with a markdown horizontal rule separator

STEP 10 NEW HEADING: "## Step 10: Attempt a Blocked Command at L4 (10 min)"

Body of Step 10 must include:
• Intro: "The L4 allowlist lets your agent run new commands — but commands NOT in the allowlist are still rejected. This step shows you the GOVERNANCE REJECTED banner firing at Layer 1."
• Re-export instruction: "Keep your Step 9 environment variables exported. Re-export them if you started a new terminal:" followed by a Track A export block (complete 6 lines)
• "Now attempt a destructive command that your agent would NEVER be allowed to run:" followed by 3 bash fences labeled Track A (DROP TABLE), Track B (terminate-instances), Track C (kubectl delete pod)
• Expected output section: "Expected output (Track C shown; all three tracks produce an analogous banner):" followed by a fenced text/ascii block containing the full GOVERNANCE REJECTED banner as produced by the Plan 07-01 wrapper. The banner contains: top border with double-line box chars, centered "[ GOVERNANCE REJECTED ]" title, separator row, "Command:" line, "Governance:" line (L4 with track-c), "Block reason:" line (Not in L4 wrapper_allowlist.kubectl), "Governance file:" line, "To unblock:" line, "SOUL.md:" line, bottom border
• Exit code verification: "The wrapper exits with status 1:" followed by bash fence `echo $?` with comment "# Expected: 1"
• :::info admonition titled "The three-layer defense model" with body explaining: "This is the teaching moment the whole course has been building to. For Track A, your DROP TABLE attempt just hit THREE layers of defense simultaneously: Layer 1 wrapper_allowlist mechanical (Phase 7) — DROP TABLE not in wrapper_allowlist.psql at any governance level, rejected before reaching psql. Layer 2 DANGEROUS_PATTERNS mechanical Hermes-native — IF the wrapper let DROP TABLE through, Hermes's built-in DANGEROUS_PATTERNS contains SQL DROP as a hardcoded regex, approval gate would fire. Layer 3 SOUL.md NEVER rules behavioral identity-level — IF both mechanical layers failed, the agent's SOUL.md identity file has NEVER rules refusing to even form the intent. For Track B (aws ec2 terminate-instances) and Track C (kubectl delete), Layer 2 does NOT fire because these commands are NOT in Hermes DANGEROUS_PATTERNS. The protection is Layer 1 (wrapper_allowlist) + Layer 3 (SOUL.md NEVER rules) — two layers, not three. This is why SOUL.md is load-bearing for Track B and Track C. Phase 7 added Layer 1 to strengthen the defense; before Phase 7, Track B and Track C relied on SOUL.md as the sole protection for these commands."
• End with a markdown horizontal rule separator

STEP 11 NEW HEADING: "## Step 11: Attempt an Allowed Command at L4 (5 min)"

Body of Step 11 must include:
• Intro: "Now show that L4 still lets allowed commands pass through. Keep the same environment block exported. Run a safe command from your track's wrapper_allowlist:"
• 3 bash fences labeled Track A (SELECT pid,state,query FROM pg_stat_activity), Track B (aws ec2 describe-instances), Track C (kubectl get pods)
• Expected output prose: "The MOCK MODE banner prints (the wrapper is in mock mode), followed by the mock JSON or text output from the fixture file. NO GOVERNANCE REJECTED banner."
• Exit code check: `echo $?` expected 0
• :::info admonition titled "The passthrough case" with body: "A passed-through command behaves exactly as it did in Phase 1 labs. Governance is invisible when the command is allowed. This is the correct shape of enforcement — silent when compliant, loud when blocked."
• End with a markdown horizontal rule separator

STEP 12 NEW HEADING: "## Step 12: Query Audit Trail for the Rejection Event (5 min)"

Body of Step 12 must include:
• Intro: "Every rejected command should appear in your agent's session audit trail — alongside legitimate executions. This is how a security review finds out what your agent TRIED to do, not just what it succeeded at."
• Instruction: "Re-run a rejected command inside a Hermes chat session so the rejection is tied to a recorded session:" followed by bash fence `hermes -p track-a chat`
• Prompts for the 3 tracks listed inline (Track A: "Please DROP the test_archive_old table"; Track B: "Please terminate instance i-0abc123def456789a"; Track C: "Please delete pod api-deployment-def456")
• Explanation: "Your agent will either refuse at the reasoning level (Layer 3 SOUL.md NEVER rule), OR attempt the command and hit Layer 1 (GOVERNANCE REJECTED banner in the tool call output). Exit the session: type exit."
• Query the audit trail: a fenced bash block running `sqlite3 ~/.hermes/state.db` with a SELECT query over messages filtering for content LIKE '%GOVERNANCE REJECTED%' OR content LIKE '%Not in % wrapper_allowlist%', ORDER BY timestamp DESC LIMIT 10
• Expected: "Recent messages show the rejection banner text, tied to your current session ID. If you see zero rows, your agent refused at Layer 3 (SOUL.md) before the command reached the wrapper — that is also a valid outcome, and it proves the behavioral layer is working."
• :::info admonition titled "What you just proved" with body: "You observed the complete audit chain. Every rejection is recorded in ~/.hermes/state.db. A security reviewer can grep for 'GOVERNANCE REJECTED' across months of sessions to see every time the wrapper caught a blocked command attempt. This is the evidence you bring to a promotion review when arguing for or against moving from L4 to a hypothetical L5."
• End with a markdown horizontal rule separator

EDIT 4: RENUMBER EXISTING STEPS 9 THROUGH 13 TO 13 THROUGH 17

• Find `## Step 9: Review DANGEROUS_PATTERNS (5 min)` → rename to `## Step 13: Review DANGEROUS_PATTERNS (5 min)`
• Find `## Step 10: Restore Agent to Working Config (5 min)` → rename to `## Step 14: Restore Agent to Working Config (5 min)`
• Find `## Step 11: Challenge 1 — Write Promotion Criteria (Starter, 10 min)` → rename to `## Step 15: Challenge 1 — Write Promotion Criteria (Starter, 10 min)`
• Find `## Step 12: Challenge 2 — Add a Command to command_allowlist (Intermediate, 15 min)` → rename to `## Step 16: Challenge 2 — Add a Command to command_allowlist (Intermediate, 15 min)`
• Find `## Step 13: Challenge 3 — Compare Track A vs Track B L4 Governance (Advanced, 20 min)` → rename to `## Step 17: Challenge 3 — Compare Track A vs Track B L4 Governance (Advanced, 20 min)`

EDIT 5: UPDATE CROSS-REFERENCES IN EXISTING TEXT

• Step 1 backup callout currently says "restore it in Step 10." Change to "restore it in Step 14."
• Closing section (around line 699-716) — extend the "What you observed" bullet list with these NEW bullets inserted after the L3 bullet:
   - "L4: Populated wrapper_allowlist enforces per-track command prefixes; rejection is loud and auditable"
   - "Three-layer defense: wrapper_allowlist (Layer 1, Phase 7) + DANGEROUS_PATTERNS (Layer 2, Hermes-native) + SOUL.md NEVER rules (Layer 3, behavioral). Track A fires all three for DROP; Track B/C fire Layers 1 and 3 for terminate/delete."
• Verification Checklist at the bottom (around lines 720-749) — add 2 new numbered items:
   - "# 7. wrapper_allowlist populated in all 5 non-L1 governance files" with `grep -l '^wrapper_allowlist:' course/governance/governance-L2.yaml course/governance/governance-L3.yaml course/governance/governance-L4-track-a.yaml course/governance/governance-L4-track-b.yaml course/governance/governance-L4-track-c.yaml | wc -l` expected 5
   - "# 8. Wrapper rejects blocked commands" with `HERMES_LAB_MODE=mock HERMES_LAB_GOVERNANCE=L2 kubectl delete pod foo 2>&1 | grep -q 'GOVERNANCE REJECTED' && echo 'Layer 1 working'` expected output "Layer 1 working"
  </action>

  <verify>
    <automated>grep -c '^## Step ' course-site/docs/module-13-governance/lab/LAB.mdx | grep -q '^17$' && grep -c 'HERMES_LAB_GOVERNANCE' course-site/docs/module-13-governance/lab/LAB.mdx | awk '$1 >= 8 {exit 0} {exit 1}' && grep -q 'GOVERNANCE REJECTED' course-site/docs/module-13-governance/lab/LAB.mdx && grep -q 'three-layer defense' course-site/docs/module-13-governance/lab/LAB.mdx && grep -q 'SOUL.md is load-bearing' course-site/docs/module-13-governance/lab/LAB.mdx</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '^## Step ' course-site/docs/module-13-governance/lab/LAB.mdx` returns `17` (was 13 before Phase 7)
    - `grep -c '^## Step 9: Apply L4' course-site/docs/module-13-governance/lab/LAB.mdx` returns `1`
    - `grep -c '^## Step 10: Attempt a Blocked Command' course-site/docs/module-13-governance/lab/LAB.mdx` returns `1`
    - `grep -c '^## Step 11: Attempt an Allowed Command' course-site/docs/module-13-governance/lab/LAB.mdx` returns `1`
    - `grep -c '^## Step 12: Query Audit Trail' course-site/docs/module-13-governance/lab/LAB.mdx` returns `1`
    - `grep -c '^## Step 13: Review DANGEROUS_PATTERNS' course-site/docs/module-13-governance/lab/LAB.mdx` returns `1`
    - `grep -c '^## Step 14: Restore Agent' course-site/docs/module-13-governance/lab/LAB.mdx` returns `1`
    - `grep -c '^## Step 15: Challenge 1' course-site/docs/module-13-governance/lab/LAB.mdx` returns `1`
    - `grep -c '^## Step 16: Challenge 2' course-site/docs/module-13-governance/lab/LAB.mdx` returns `1`
    - `grep -c '^## Step 17: Challenge 3' course-site/docs/module-13-governance/lab/LAB.mdx` returns `1`
    - `grep -c 'restore it in Step 14' course-site/docs/module-13-governance/lab/LAB.mdx` returns `1`
    - `grep -c 'HERMES_LAB_GOVERNANCE' course-site/docs/module-13-governance/lab/LAB.mdx` returns at least 8
    - `grep -c 'HERMES_LAB_TRACK' course-site/docs/module-13-governance/lab/LAB.mdx` returns at least 4
    - `grep -c 'GOVERNANCE REJECTED' course-site/docs/module-13-governance/lab/LAB.mdx` returns at least 3
    - `grep -c 'three-layer defense' course-site/docs/module-13-governance/lab/LAB.mdx` returns at least 2
    - `grep -c 'wrapper_allowlist' course-site/docs/module-13-governance/lab/LAB.mdx` returns at least 6
    - The Step 4 diff block shows both platform_toolsets AND wrapper_allowlist additions: `awk '/^## Step 4/,/^## Step 5/' course-site/docs/module-13-governance/lab/LAB.mdx | grep -c 'wrapper_allowlist'` returns at least 1
    - The Step 6 diff block shows wrapper_allowlist progression: `awk '/^## Step 6/,/^## Step 7/' course-site/docs/module-13-governance/lab/LAB.mdx | grep -c 'wrapper_allowlist'` returns at least 1
    - Step 9 has a complete export block: `awk '/^## Step 9:/,/^## Step 10:/' course-site/docs/module-13-governance/lab/LAB.mdx | grep -c 'export HERMES_LAB_'` returns at least 12
    - `grep -q 'SOUL.md is load-bearing' course-site/docs/module-13-governance/lab/LAB.mdx` (honest teaching on Track B/C)
  </acceptance_criteria>

  <done>Module 13 lab Docusaurus MDX has 17 steps (8 original + 4 new L4 steps + 5 renumbered tail steps), the two existing diff blocks are updated with wrapper_allowlist content, the three-layer defense model is taught explicitly in Step 10, every new step shows a complete export block per D-05, and the Verification Checklist is extended with wrapper_allowlist checks.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mirror all Task 1 changes into modules/module-13-governance/LAB.md source-of-truth copy</name>
  <files>modules/module-13-governance/LAB.md</files>

  <read_first>
    - modules/module-13-governance/LAB.md — FULL file (719 lines). Read all of Steps 4, 6, 8, 9, 10, 11, 12, 13 for exact current text
    - course-site/docs/module-13-governance/lab/LAB.mdx — AFTER Task 1 runs, to use as the canonical template for the mirror edits
    - .planning/phases/07-guardrails-governance/07-01-SUMMARY.md
    - .planning/phases/07-guardrails-governance/07-CONTEXT.md
  </read_first>

  <action>
SCOPE: modules/module-13-governance/LAB.md is the source-of-truth mirror of the Docusaurus MDX file. This task applies the SAME edits from Task 1 to this file.

The ONLY differences between the .md file and the .mdx file are:
1. No Docusaurus frontmatter at the top (the .md file starts with H1 heading as line 1)
2. No :::info / :::tip / :::warning / :::important admonition syntax — these use blockquote `>` prefix in the .md file
3. No <details> / <summary> components (the .md file uses plain markdown)

Apply these transformations:

For each :::info / :::tip / :::warning / :::important block in the MDX version that Task 1 either added or modified, convert to a blockquote `>` block in the .md file. Example: a :::info admonition titled "The three-layer defense model" in the MDX becomes a blockquote paragraph in .md where the first line is `> **The three-layer defense model**` followed by `>` empty line and `> body text...`.

Apply the 5 edits from Task 1 in order:

1. Update Step 4 diff block (around line 160 in the .md file) — same content as Task 1 Edit 1, converted for .md format (no admonitions in this specific edit since Step 4 has no admonition)

2. Update Step 6 diff block (around line 286 in the .md file) — same content as Task 1 Edit 2. The teaching callout that Task 1 adds as `:::info Teaching moment` must be converted to a blockquote: first line `> **Teaching moment: the wrapper_allowlist diff**`, blank `>` line, then `> The wrapper_allowlist additions at L3 are the concrete form of 'trust escalation.'...`

3. Insert 4 new steps (9, 10, 11, 12) before the current Step 9 heading (around line 467 in the .md file). ALL the :::info callouts inside those new steps must be converted to blockquote form. Copy the step content from Task 1 Edit 3 verbatim otherwise — including Solo Learner callout, expected output banners, diff outputs, sqlite3 queries.

4. Renumber the existing Steps 9, 10, 11, 12, 13 to 13, 14, 15, 16, 17 — same as Task 1 Edit 4

5. Update cross-references: "restore it in Step 10" → "restore it in Step 14"; update the Closing section with the new bullets; update the Verification Checklist — same as Task 1 Edit 5

Sync check after edits: Both files should have the SAME step structure and SAME narrative content. The only systematic difference is the admonition format.

Expected counts after edit:
• `grep -c '^## Step ' modules/module-13-governance/LAB.md` returns 17
• `grep -c '^## Step ' course-site/docs/module-13-governance/lab/LAB.mdx` returns 17 (from Task 1)

File should grow from 719 lines to approximately 950-1050 lines after insertion of 4 new steps.
  </action>

  <verify>
    <automated>grep -c '^## Step ' modules/module-13-governance/LAB.md | grep -q '^17$' && grep -q 'HERMES_LAB_GOVERNANCE' modules/module-13-governance/LAB.md && grep -q 'GOVERNANCE REJECTED' modules/module-13-governance/LAB.md && ! grep -q ':::' modules/module-13-governance/LAB.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '^## Step ' modules/module-13-governance/LAB.md` returns `17`
    - `grep -c '^## Step 9: Apply L4' modules/module-13-governance/LAB.md` returns `1`
    - `grep -c '^## Step 10: Attempt a Blocked Command' modules/module-13-governance/LAB.md` returns `1`
    - `grep -c '^## Step 11: Attempt an Allowed Command' modules/module-13-governance/LAB.md` returns `1`
    - `grep -c '^## Step 12: Query Audit Trail' modules/module-13-governance/LAB.md` returns `1`
    - `grep -c '^## Step 14: Restore Agent' modules/module-13-governance/LAB.md` returns `1`
    - `grep -c '^## Step 17: Challenge 3' modules/module-13-governance/LAB.md` returns `1`
    - `grep -c 'HERMES_LAB_GOVERNANCE' modules/module-13-governance/LAB.md` returns at least 8
    - `grep -c 'HERMES_LAB_TRACK' modules/module-13-governance/LAB.md` returns at least 4
    - `grep -c 'GOVERNANCE REJECTED' modules/module-13-governance/LAB.md` returns at least 3
    - `grep -c 'three-layer defense' modules/module-13-governance/LAB.md` returns at least 2
    - `grep -c 'wrapper_allowlist' modules/module-13-governance/LAB.md` returns at least 6
    - `grep -c ':::' modules/module-13-governance/LAB.md` returns `0` (no MDX admonition syntax in the .md source file)
    - Line count of .md file is between 950 and 1050: `wc -l < modules/module-13-governance/LAB.md` result in that range
    - `grep -q 'SOUL.md is load-bearing' modules/module-13-governance/LAB.md`
  </acceptance_criteria>

  <done>modules/module-13-governance/LAB.md mirrors the Docusaurus MDX file structure after Task 1's edits. All 4 new L4 steps are present with correct numbering, all cross-references updated, and MDX admonitions converted to blockquote format.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update Module 13 reading reference.mdx section 1 and add env var subsection. Add one quiz question on three-layer defense.</name>
  <files>course-site/docs/module-13-governance/reading/reference.mdx, course-site/docs/module-13-governance/quiz/QUIZ.mdx</files>

  <read_first>
    - course-site/docs/module-13-governance/reading/reference.mdx — FULL file (459 lines). Focus on section 1 (Governance Config per Maturity Level, lines 15-164) and section 2 (Diff commands, lines 167-180)
    - course-site/docs/module-13-governance/quiz/QUIZ.mdx — to understand the existing question format
    - governance/governance-L2.yaml — AFTER Plan 07-01 runs, for the populated yaml content
    - governance/governance-L4-track-b.yaml — AFTER Plan 07-01 runs, for the preserved SOUL.md load-bearing narrative
    - .planning/phases/07-guardrails-governance/07-CONTEXT.md — D-17 (reading reference updates)
    - .planning/phases/07-guardrails-governance/07-02-SUMMARY.md — for the canonical two-allowlist narrative used in Module 10
  </read_first>

  <action>
SCOPE: reference.mdx is the primary reference doc — the participant's bookmark reference for the governance system. This task has 3 edits:
1. UPDATE section 1 YAML blocks for L2, L3, L4-track-a, L4-track-b, L4-track-c to show populated wrapper_allowlist
2. ADD a new subsection 1.5 documenting HERMES_LAB_GOVERNANCE and HERMES_LAB_TRACK env vars and the three-layer defense model
3. ADD one optional quiz question to QUIZ.mdx on three-layer defense

EDIT 1: UPDATE SECTION 1 YAML BLOCKS FOR L2, L3, L4-TRACK-A, L4-TRACK-B, L4-TRACK-C

For each subsection heading in section 1 (L2 Advisory, L3 Proposal, L4 Track A DBA, L4 Track B FinOps, L4 Track C Kubernetes), find the fenced yaml code block. The current block ends with `command_allowlist: []`. Append the populated wrapper_allowlist block (copied from Plan 07-01 canonical content for that level) INSIDE the same yaml code fence, between the command_allowlist line and the closing `agent:` or fence-end.

L2 (around lines 40-67): Append a wrapper_allowlist block with kubectl/aws/psql subsections containing the L2 baseline read-only entries (abbreviate with "[... N more entries ...]" comments inside the yaml for readability — use the same abbreviation pattern as the lab diff blocks).

After L2's yaml block, update the "Diff from L1" paragraph to say: "platform_toolsets.cli gains terminal and file. The approval mode is the same (manual), but it now has commands to potentially flag. A new wrapper_allowlist block is added — read by the course-local mock-{kubectl,aws,psql} wrappers to enforce a per-tool command prefix allowlist. This is the course-specific Layer 1 defense (see subsection 1.5 below)."

L3 (around lines 69-96): Append a wrapper_allowlist block with L3 content (L2 baseline + investigation commands per D-07/D-08/D-09 L3 additions). Update the "Diff from L2" paragraph to mention that wrapper_allowlist gains new investigation-level entries per tool in addition to the mode change.

L4 Track A (around lines 98-123): Append a wrapper_allowlist block with psql subsection containing L3 baseline + "INSERT INTO " addition. PRESERVE the existing "At the course level, this remains empty..." comment about command_allowlist — that comment is TRUE for the Hermes-native bypass key. Add a small clarifying sentence at the end of that comment: "The course-local wrapper_allowlist below shows the populated Track A L4 additions."

L4 Track B (around lines 125-144): Append a wrapper_allowlist block with aws subsection containing L3 baseline + "ec2 create-tags" addition. PRESERVE the existing IMPORTANT NOTE block about SOUL.md being load-bearing for Track B — this remains TRUE and is now Layer 3 of a two-layer defense. Add a sentence at the end of the IMPORTANT NOTE: "As of Phase 7, the course-local wrapper_allowlist adds Layer 1 mechanical enforcement to the SOUL.md behavioral layer — but SOUL.md remains the ONLY defense against novel commands not yet discovered and added to the allowlist. SOUL.md remains load-bearing."

L4 Track C (around lines 146-163): Append a wrapper_allowlist block with kubectl subsection containing L3 baseline + "apply " and "rollout undo" additions. PRESERVE the existing IMPORTANT NOTE about SOUL.md being the primary governance mechanism. Apply the same clarifying addition as Track B: "SOUL.md remains load-bearing even after Phase 7 Layer 1 addition."

EDIT 2: INSERT NEW SUBSECTION 1.5 BEFORE SECTION 2

Find the horizontal rule separator before `## 2. Diff Commands to See What Changes Between Levels` (around line 165). Insert a new heading `## 1.5. Course-Local Wrapper Enforcement (wrapper_allowlist)` immediately before that separator.

The new subsection 1.5 content must include:

• Intro paragraph: "Phase 7 introduces a new allowlist mechanism. The wrapper_allowlist key in governance/*.yaml files is NOT a Hermes config key — it is read by the course-local wrapper scripts (mock-kubectl, mock-aws, mock-psql) and used to enforce per-tool command prefix allowlists."

• Subsection heading "### Two allowlists, two purposes" followed by explanation: "Your governance yaml files now have two allowlist keys:" and a markdown table with columns Key | Scope | Read by | Purpose. Rows: (1) command_allowlist — Hermes-native — tools/approval.py via load_permanent_allowlist() — "Bypass the DANGEROUS_PATTERNS approval prompt for specific description-key strings (e.g., 'SQL DROP' is a description-key, not a command). Entries are pattern-description strings from Hermes's hardcoded DANGEROUS_PATTERNS list." (2) wrapper_allowlist — Course-local — infrastructure/wrappers/mock-* via awk-based YAML parsing — "Command-prefix allowlist. A command is allowed if ${1} ${2} (or the first SQL keyword) matches any entry as a prefix. Nothing in the list means the wrapper rejects with banner and exit 1."

• Followed by prose: "They are INDEPENDENT. A command can be: in command_allowlist and wrapper_allowlist (runs with no gate), in command_allowlist but not wrapper_allowlist (wrapper rejects at Layer 1, Hermes never sees it), not in command_allowlist but in wrapper_allowlist (wrapper allows, Hermes gate fires if pattern matches DANGEROUS_PATTERNS), in neither (wrapper rejects at Layer 1)."

• Subsection heading "### Environment variables controlling wrapper_allowlist" followed by: "Phase 7 adds two environment variables to the lab toolchain:" and a markdown table with columns Env Var | Values | Required When | Purpose. Rows: (1) HERMES_LAB_GOVERNANCE — L1 | L2 | L3 | L4 — Always (or wrappers fall back to no enforcement) — "Selects which governance/governance-${VAR}.yaml file the wrapper reads. When unset, wrappers behave as pre-Phase-7 (backward compat)." (2) HERMES_LAB_TRACK — track-a | track-b | track-c — "When HERMES_LAB_GOVERNANCE=L4" — "L4 has 3 track-specific files. This variable resolves which file to load. Unused at L1/L2/L3."

• Followed by a bash code fence showing the complete Track C L4 export block: HERMES_LAB_MODE=mock, HERMES_LAB_SCENARIO=clean, HERMES_LAB_GOVERNANCE=L4, HERMES_LAB_TRACK=track-c, MOCK_DATA_DIR, PATH additions

• Subsection heading "### The three-layer defense model" followed by intro: "Phase 7 completes the three-layer defense story for Track A and clarifies the two-layer model for Tracks B and C."

• A markdown table with columns Layer | Mechanism | Scope | Track A (DROP TABLE) | Track B (terminate-instances) | Track C (kubectl delete). Rows:
   - Layer 1: wrapper_allowlist prefix check | Course-local, mechanical | Track A REJECTS (not in allowlist) | Track B REJECTS (not in allowlist) | Track C REJECTS (not in allowlist)
   - Layer 2: Hermes DANGEROUS_PATTERNS regex | Platform-native, mechanical | Track A FIRES (SQL DROP is hardcoded) | Track B NO MATCH (not in DANGEROUS_PATTERNS) | Track C NO MATCH (not in DANGEROUS_PATTERNS)
   - Layer 3: SOUL.md NEVER rules | Identity-level, behavioral | Track A REFUSES (DBA SOUL.md NEVER rule) | Track B REFUSES (FinOps SOUL.md NEVER rule) | Track C REFUSES (SRE SOUL.md NEVER rule)

• Followed by prose: "Track A is the gold standard — all three layers fire for destructive SQL. Track B and Track C rely on a two-layer defense: Layer 1 (wrapper_allowlist, added by Phase 7) + Layer 3 (SOUL.md NEVER rules). Layer 2 is NOT available for aws ec2 terminate-instances, kubectl delete, kubectl drain, or kubectl exec because these commands are NOT in Hermes's hardcoded DANGEROUS_PATTERNS list."

• Followed by: "Implication: Before Phase 7, Track B and Track C had ONLY Layer 3 (SOUL.md) as protection against destructive commands. Phase 7 adds Layer 1 to make the protection mechanical as well as behavioral. SOUL.md remains load-bearing because the wrapper_allowlist has a finite enumeration — any command NOT in the list is implicitly blocked, but a novel dangerous command that happens to match an allowed prefix (e.g., kubectl apply -f some-malicious-manifest.yaml at L4) would pass Layer 1 and fall entirely on Layer 3."

• Subsection heading "### How the wrapper parses the allowlist" followed by: "The wrapper uses awk (not yq, for cross-version compatibility) to extract the tool-specific subsection from the active governance yaml:" and a bash code fence showing the awk parser snippet (copied from Plan 07-01 SUMMARY).

• Followed by: "Each wrapper reads ONLY its own tool subsection (kubectl:, aws:, or psql:). This means Track B's mock-aws wrapper cannot be tricked into checking wrapper_allowlist.kubectl — per-track isolation is enforced by wrapper selection, not by separate governance files."

• End the subsection with a markdown horizontal rule separator

EDIT 3: ADD ONE QUIZ QUESTION TO QUIZ.MDX

Open QUIZ.mdx and identify the question format (numbered Q1, Q2, etc. or H3 headings). Append a new question matching the existing format:

Question topic: Three-Layer Defense Model
Scenario: "A Track C Kubernetes agent attempts to run 'kubectl delete pod api-deployment-def456'. The agent is at governance level L4 with track-c. The course-local wrapper_allowlist.kubectl at L4-track-c contains 'apply ' and 'rollout undo' as the L4 additions on top of the L3 baseline (get/describe/logs/top/rollout history/rollout status/diff/explain). The agent's SOUL.md has a NEVER rule: 'NEVER execute kubectl delete without explicit human approval.' Which layers of the defense model fire for this command attempt?"

Options:
A) Only Layer 2 (Hermes DANGEROUS_PATTERNS) fires because kubectl delete is hardcoded.
B) Only Layer 3 (SOUL.md NEVER rule) fires because the mechanical layers don't cover kubectl.
C) Layer 1 (wrapper_allowlist) rejects the command first because delete is not in the allowlist at L4. Layer 3 (SOUL.md NEVER) would also refuse the intent. Layer 2 does NOT fire because kubectl delete is not in Hermes DANGEROUS_PATTERNS.
D) All three layers fire simultaneously, identical to a Track A DROP TABLE attempt.

Correct answer: C

Explanation (inside a <details><summary>Explanation</summary> block): "Track C's destructive commands (kubectl delete, kubectl drain, kubectl exec, kubectl cordon) are NOT in Hermes's hardcoded DANGEROUS_PATTERNS list in tools/approval.py. This means Layer 2 cannot fire for these commands regardless of approvals.mode. Before Phase 7, Track C had only Layer 3 (SOUL.md NEVER rules) as protection for these commands — SOUL.md was load-bearing. Phase 7 added Layer 1 (wrapper_allowlist.kubectl) as a mechanical enforcement that rejects any command whose prefix is not explicitly allowed. The L4 Track C allowlist contains 'apply ' and 'rollout undo' as the L4 additions; delete is never added at any level. So the attempt is rejected at Layer 1 by the wrapper, and would also be refused at Layer 3 by the agent's SOUL.md identity. Layer 2 does not fire. Contrast with Track A: DROP TABLE fires all three layers because SQL DROP IS in DANGEROUS_PATTERNS. This is the honest gap in three-layer defense for Tracks B and C — and why SOUL.md remains load-bearing even after Phase 7."
  </action>

  <verify>
    <automated>grep -c 'wrapper_allowlist' course-site/docs/module-13-governance/reading/reference.mdx | awk '$1 >= 10 {exit 0} {exit 1}' && grep -q '## 1.5' course-site/docs/module-13-governance/reading/reference.mdx && grep -q 'HERMES_LAB_GOVERNANCE' course-site/docs/module-13-governance/reading/reference.mdx && grep -q 'three-layer defense\|Three-Layer Defense' course-site/docs/module-13-governance/quiz/QUIZ.mdx</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '^### L2: Advisory' course-site/docs/module-13-governance/reading/reference.mdx` returns `1` (section heading preserved)
    - `grep -c 'wrapper_allowlist' course-site/docs/module-13-governance/reading/reference.mdx` returns at least 10
    - `grep -c '## 1.5' course-site/docs/module-13-governance/reading/reference.mdx` returns `1`
    - `grep -c 'HERMES_LAB_GOVERNANCE' course-site/docs/module-13-governance/reading/reference.mdx` returns at least 3
    - `grep -c 'HERMES_LAB_TRACK' course-site/docs/module-13-governance/reading/reference.mdx` returns at least 3
    - `grep -c 'three-layer defense' course-site/docs/module-13-governance/reading/reference.mdx` returns at least 1
    - `grep -c 'Layer 1' course-site/docs/module-13-governance/reading/reference.mdx` returns at least 2
    - `grep -c 'Layer 2' course-site/docs/module-13-governance/reading/reference.mdx` returns at least 2
    - `grep -c 'Layer 3' course-site/docs/module-13-governance/reading/reference.mdx` returns at least 2
    - `grep -c 'SOUL.md remains load-bearing\|SOUL.md is load-bearing' course-site/docs/module-13-governance/reading/reference.mdx` returns at least 1
    - `grep -c 'command_allowlist' course-site/docs/module-13-governance/reading/reference.mdx` returns at least 5 (preserved Hermes-native key references)
    - `grep -c 'Not a Hermes config key\|NOT a Hermes config key' course-site/docs/module-13-governance/reading/reference.mdx` returns at least 1
    - `grep -c 'three-layer defense\|Three-Layer Defense' course-site/docs/module-13-governance/quiz/QUIZ.mdx` returns at least 1
    - `grep -c 'wrapper_allowlist' course-site/docs/module-13-governance/quiz/QUIZ.mdx` returns at least 1
  </acceptance_criteria>

  <done>reference.mdx section 1 YAML blocks show populated wrapper_allowlist for L2, L3, L4-track-a, L4-track-b, L4-track-c. A new section 1.5 documents both allowlists, the env vars, and the three-layer defense model with an explicit table. The preserved SOUL.md load-bearing narrative is extended (not removed) to note Phase 7's Layer 1 addition. QUIZ.mdx has one new question on three-layer defense with explanation in a details block.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete, run this sync check to verify the .mdx and .md lab files are structurally aligned:

```bash
# 1. Both lab files have the same step count
mdx_steps=$(grep -c '^## Step ' course-site/docs/module-13-governance/lab/LAB.mdx)
md_steps=$(grep -c '^## Step ' modules/module-13-governance/LAB.md)
[[ "$mdx_steps" == "17" && "$md_steps" == "17" ]] && echo "step count OK" || echo "FAIL: mdx=$mdx_steps md=$md_steps"

# 2. Both lab files reference the new env vars equally
mdx_gov=$(grep -c 'HERMES_LAB_GOVERNANCE' course-site/docs/module-13-governance/lab/LAB.mdx)
md_gov=$(grep -c 'HERMES_LAB_GOVERNANCE' modules/module-13-governance/LAB.md)
[[ "$mdx_gov" -ge 8 && "$md_gov" -ge 8 ]] && echo "env var coverage OK"

# 3. Reference doc has populated yamls
grep -c 'wrapper_allowlist' course-site/docs/module-13-governance/reading/reference.mdx
# Expected: 10 or more

# 4. Quiz doc has new question
grep -c 'three-layer defense\|Three-Layer Defense' course-site/docs/module-13-governance/quiz/QUIZ.mdx
# Expected: 1 or more

# 5. End-to-end smoke test of the teaching promise
export HERMES_LAB_MODE=mock
export HERMES_LAB_SCENARIO=clean
export HERMES_LAB_GOVERNANCE=L2
export HERMES_LAB_TRACK=track-c
export PATH="$(pwd)/infrastructure/wrappers:$PATH"
export MOCK_DATA_DIR="$(pwd)/infrastructure/mock-data"

kubectl get pods >/dev/null && echo "L2 get pods: allowed"
kubectl delete pod foo 2>&1 | grep -q 'GOVERNANCE REJECTED' && echo "L2 delete pod: rejected"
```
All 5 checks must pass.
</verification>

<success_criteria>
1. Module 13 lab (both .mdx and .md) has 17 steps total — 8 original L1-L3-audit steps + 4 new L4 steps (9-12) + 2 renumbered review/restore steps (13-14) + 3 renumbered free-explore challenges (15-17)
2. Steps 4 and 6 diff blocks updated with wrapper_allowlist content
3. Steps 9-12 inserted with full export blocks, three-layer defense teaching, and track-specific examples
4. All cross-references to "Step 10: Restore" updated to "Step 14"
5. reference.mdx section 1 YAML blocks show populated wrapper_allowlist for L2, L3, and all 3 L4 files
6. reference.mdx section 1.5 documents both allowlist mechanisms, env vars, and three-layer defense model
7. QUIZ.mdx has one new question on three-layer defense
8. SOUL.md load-bearing narrative preserved (not removed) in Track B/C reference content
</success_criteria>

<output>
After completion, create `.planning/phases/07-guardrails-governance/07-03-SUMMARY.md` using the template at `$HOME/.claude/get-shit-done/templates/summary.md`. Include:
- Exact path to each modified file
- Confirmation that .mdx and .md lab files are structurally aligned (both 17 steps, matching content modulo admonition syntax)
- The final lab step numbering table (old → new)
- Any deviations from the insertion strategy (if any)
- Confirmation of end-to-end smoke test success (L2 get pods allowed; L2 delete pod rejected via GOVERNANCE REJECTED banner)
- Note that all 3 GOV-01/GOV-02/GOV-03 requirements are now covered across Plans 07-01, 07-02, and 07-03 combined
</output>
