---
phase: 07-guardrails-governance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/track-a-database/config.yaml
  - agents/track-b-finops/config.yaml
  - agents/track-c-kubernetes/config.yaml
  - modules/module-10-agents/solution/track-a/config.yaml
  - modules/module-10-agents/solution/track-b/config.yaml
  - modules/module-10-agents/solution/track-c/config.yaml
  - modules/module-10-agents/starter/track-a/config-starter.yaml
  - modules/module-10-agents/starter/track-b/config-starter.yaml
  - modules/module-10-agents/starter/track-c/config-starter.yaml
  - modules/module-10-agents/LAB-track-a-database.md
  - modules/module-10-agents/LAB-track-b-finops.md
  - course-site/docs/module-10-domain-agent/lab/LAB-track-a-database.mdx
  - course-site/docs/module-10-domain-agent/lab/LAB-track-b-finops.mdx
autonomous: true
requirements:
  - GOV-02
must_haves:
  truths:
    - "All 3 agents/track-*/config.yaml profile configs have a populated `wrapper_allowlist` block with L2 baseline content matching the governance-L2.yaml reference (Plan 07-01 output)"
    - "All 3 modules/module-10-agents/solution/track-*/config.yaml files match the agents/track-*/config.yaml files (source-of-truth mirror)"
    - "All 3 modules/module-10-agents/starter/track-*/config-starter.yaml files document the new wrapper_allowlist key in comments (participants populate it during Module 10 lab)"
    - "Module 10 Track A and Track B lab files (both .mdx and .md copies) show updated expected `cat config.yaml` output blocks reflecting the populated L2 baseline"
    - "Track C Module 10 lab files are verified (no command_allowlist references exist per research finding; no changes needed if confirmed)"
    - "The existing Hermes-native `command_allowlist: []` key is preserved untouched in every file"
  artifacts:
    - path: "agents/track-a-database/config.yaml"
      provides: "Track A L2 baseline wrapper_allowlist (psql)"
      contains: "wrapper_allowlist:"
    - path: "agents/track-b-finops/config.yaml"
      provides: "Track B L2 baseline wrapper_allowlist (aws)"
      contains: "wrapper_allowlist:"
    - path: "agents/track-c-kubernetes/config.yaml"
      provides: "Track C L2 baseline wrapper_allowlist (kubectl)"
      contains: "wrapper_allowlist:"
    - path: "modules/module-10-agents/LAB-track-a-database.md"
      provides: "Updated Module 10 Track A lab with populated allowlist text"
      contains: "wrapper_allowlist"
    - path: "course-site/docs/module-10-domain-agent/lab/LAB-track-a-database.mdx"
      provides: "Course-site Module 10 Track A lab (Docusaurus MDX mirror)"
      contains: "wrapper_allowlist"
  key_links:
    - from: "agents/track-a-database/config.yaml"
      to: "governance/governance-L2.yaml (psql subsection)"
      via: "Content mirrors the governance-L2.yaml wrapper_allowlist.psql section"
      pattern: "wrapper_allowlist"
    - from: "modules/module-10-agents/solution/track-a/config.yaml"
      to: "agents/track-a-database/config.yaml"
      via: "Source-of-truth mirror — these files are manually kept in sync"
      pattern: "wrapper_allowlist"
    - from: "modules/module-10-agents/LAB-track-a-database.md"
      to: "course-site/docs/module-10-domain-agent/lab/LAB-track-a-database.mdx"
      via: "Source-of-truth → Docusaurus mirror; both contain same populated example"
      pattern: "wrapper_allowlist"
---

<objective>
Populate the 3 agent profile configs with L2 baseline wrapper_allowlist (resolving Phase 6 D-17 deferral), mirror the change into the Module 10 source-of-truth solution and starter configs, and cascade the text updates into the Module 10 lab files (both source .md and Docusaurus .mdx copies) so the expected `cat config.yaml` output blocks reflect the populated state.

Purpose: Phase 6 explicitly deferred `command_allowlist: []` resolution to Phase 7 (STATE.md Phase 06 D-17). This plan closes that loop. It also ensures that a participant who opens Module 10 (which comes before Module 13 in the course sequence) sees populated configs from the start, rather than encountering empty allowlists in Module 10 only to have them magically populated in Module 13.

This plan runs in parallel with Plan 07-01 (no file overlap; 07-01 owns governance/ and infrastructure/wrappers/, 07-02 owns agents/ and modules/module-10-agents/ and course-site/module-10-domain-agent/). Both plans populate YAML content from the same D-07/D-08/D-09 source — the canonical content lives in governance/*.yaml (Plan 07-01) and is replicated here.

Output:
- 3 `agents/track-*/config.yaml` files with populated wrapper_allowlist
- 3 `modules/module-10-agents/solution/track-*/config.yaml` mirror files
- 3 `modules/module-10-agents/starter/track-*/config-starter.yaml` files with documented placeholder
- 2 Module 10 lab files × 2 formats = 4 files with updated expected-output blocks (Track A and Track B; Track C verified no-op)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-guardrails-governance/07-CONTEXT.md
@.planning/phases/07-guardrails-governance/07-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Current state of files about to be modified. Executor must read these first. -->

From agents/track-a-database/config.yaml (current, line 17):
```yaml
command_allowlist: []  # L2: nothing pre-approved; L4 would add "SELECT", "EXPLAIN", "SHOW"
```

From agents/track-b-finops/config.yaml (current, line 20):
```yaml
command_allowlist: []  # L2: nothing pre-approved
```

From agents/track-c-kubernetes/config.yaml (current, line 20):
```yaml
command_allowlist: []
```

Module 10 cascade targets (from 07-RESEARCH.md Module 10 Cascade Targets section):
- course-site/docs/module-10-domain-agent/lab/LAB-track-a-database.mdx line 369: "`command_allowlist`:" text
- course-site/docs/module-10-domain-agent/lab/LAB-track-a-database.mdx line 376: `command_allowlist: ["EXPLAIN"]` code block
- course-site/docs/module-10-domain-agent/lab/LAB-track-b-finops.mdx line 322: "Add read-only AWS command patterns to `command_allowlist`"
- modules/module-10-agents/LAB-track-a-database.md line 345: "`command_allowlist`:"
- modules/module-10-agents/LAB-track-a-database.md line 352: `command_allowlist: ["EXPLAIN"]`
- modules/module-10-agents/LAB-track-b-finops.md line 310: "Add read-only AWS command patterns to `command_allowlist`"

Track C Module 10 lab files: 07-RESEARCH.md notes "grep returned no hits in Track C mdx" for command_allowlist — this plan verifies no Track C lab updates are needed.

Per-track wrapper assignment (from Plan 07-01 canonical content):
- Track A = psql only (database commands) → `wrapper_allowlist.psql` section only
- Track B = aws only (FinOps commands) → `wrapper_allowlist.aws` section only
- Track C = kubectl only (Kubernetes commands) → `wrapper_allowlist.kubectl` section only

Each profile config contains only its own tool's L2 baseline subsection — NOT all three. This preserves per-track isolation (Track B agent has no kubectl access even if it somehow invoked mock-kubectl because its config.yaml's wrapper_allowlist.kubectl section does not exist).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Populate agents/track-*/config.yaml and mirror to modules/module-10-agents/solution/ and starter/</name>
  <files>agents/track-a-database/config.yaml, agents/track-b-finops/config.yaml, agents/track-c-kubernetes/config.yaml, modules/module-10-agents/solution/track-a/config.yaml, modules/module-10-agents/solution/track-b/config.yaml, modules/module-10-agents/solution/track-c/config.yaml, modules/module-10-agents/starter/track-a/config-starter.yaml, modules/module-10-agents/starter/track-b/config-starter.yaml, modules/module-10-agents/starter/track-c/config-starter.yaml</files>

  <read_first>
    - agents/track-a-database/config.yaml — current 25-line file, line 17 is the allowlist
    - agents/track-b-finops/config.yaml — current 25-line file, line 20 is the allowlist
    - agents/track-c-kubernetes/config.yaml — current 25-line file, line 20 is the allowlist
    - modules/module-10-agents/solution/track-a/config.yaml — mirror file (07-RESEARCH.md line 511)
    - modules/module-10-agents/solution/track-b/config.yaml — mirror file
    - modules/module-10-agents/solution/track-c/config.yaml — mirror file
    - modules/module-10-agents/starter/track-a/config-starter.yaml — participant starter with "leave empty for L2" comment (07-RESEARCH.md line 514)
    - modules/module-10-agents/starter/track-b/config-starter.yaml — participant starter
    - modules/module-10-agents/starter/track-c/config-starter.yaml — participant starter
    - .planning/phases/07-guardrails-governance/07-CONTEXT.md — D-06 (symmetric across tracks), D-07/D-08/D-09 (exact content), D-11 (profile configs get L2 baseline), D-12 (cascade)
    - .planning/phases/07-guardrails-governance/07-RESEARCH.md — Agent Profile Config Status table
  </read_first>

  <action>
**SCOPE:** 9 YAML files total (3 canonical agents/ + 3 solution/ mirror + 3 starter/ doc-only).

**PRESERVATION RULES** (identical to Plan 07-01 Task 1):
1. Do NOT modify the existing `command_allowlist` key — it stays as `[]` (Hermes-native bypass; Phase 7 does not populate it)
2. Do NOT remove existing comments that explain SOUL.md safety notes
3. The `wrapper_allowlist` is APPENDED after the existing `command_allowlist` line

**FILE 1: agents/track-a-database/config.yaml**

Replace line 17:
```yaml
command_allowlist: []  # L2: nothing pre-approved; L4 would add "SELECT", "EXPLAIN", "SHOW"
```
With (preserving the line verbatim, then appending the new block):
```yaml
command_allowlist: []  # L2: nothing pre-approved (Hermes-native bypass mechanism; unchanged)

# ── Course-local wrapper enforcement (Phase 7) ──
# Read by infrastructure/wrappers/mock-psql when HERMES_LAB_GOVERNANCE=L2.
# Track A uses only the psql subsection. The wrapper matches the first SQL keyword.
wrapper_allowlist:
  psql:
    - "SELECT "
    - "EXPLAIN "
    - "SHOW "
    - "DESCRIBE "
    - "\\d"
    - "\\dt"
    - "\\l"
```

**FILE 2: agents/track-b-finops/config.yaml**

Replace line 20 region (preserving the SOUL.md safety note comment that spans lines 17-19):
```yaml
# Track B safety note: aws ec2 terminate/modify-instance-attribute are not in
# Hermes DANGEROUS_PATTERNS. SOUL.md NEVER rules enforce the behavioral boundary.
# For L4, add custom patterns to command_allowlist if your hermes version supports it.
command_allowlist: []  # L2: nothing pre-approved (Hermes-native bypass mechanism; unchanged)

# ── Course-local wrapper enforcement (Phase 7) ──
# Read by infrastructure/wrappers/mock-aws when HERMES_LAB_GOVERNANCE=L2.
# Track B uses only the aws subsection. The wrapper matches "${1} ${2}" prefix.
wrapper_allowlist:
  aws:
    - "sts get-caller-identity"
    - "ec2 describe-"
    - "rds describe-"
    - "cloudwatch get-metric-"
    - "cloudwatch describe-alarms"
    - "cloudwatch describe-alarm-history"
    - "ce get-"
    - "cost-explorer get-"
    - "elbv2 describe-target-health"
    - "cloudtrail lookup-events"
```

**FILE 3: agents/track-c-kubernetes/config.yaml**

Replace line 20 region (preserving the SOUL.md safety note comment that spans lines 18-19):
```yaml
# Track C safety note: kubectl delete, kubectl drain, kubectl cordon are not in
# Hermes DANGEROUS_PATTERNS. SOUL.md NEVER rules enforce the behavioral boundary.
command_allowlist: []  # L2: nothing pre-approved (Hermes-native bypass mechanism; unchanged)

# ── Course-local wrapper enforcement (Phase 7) ──
# Read by infrastructure/wrappers/mock-kubectl when HERMES_LAB_GOVERNANCE=L2.
# Track C uses only the kubectl subsection. The wrapper matches "${1} ${2}" prefix.
wrapper_allowlist:
  kubectl:
    - "get pods"
    - "get pod "
    - "get nodes"
    - "get endpoints"
    - "describe pod "
    - "describe node "
    - "logs "
    - "top pods"
    - "top nodes"
```

**FILES 4, 5, 6 — modules/module-10-agents/solution/track-{a,b,c}/config.yaml:**

These are source-of-truth mirror files. Apply the SAME changes as the canonical agents/ versions above. After the change, the content should be byte-identical to the canonical agents/track-*/config.yaml counterparts. Use diff to verify:
```bash
diff agents/track-a-database/config.yaml modules/module-10-agents/solution/track-a/config.yaml
# Expected: no output (files identical)
```
Pre-change difference (if any): the files may have differed in header comments. If the current files differ only in header, preserve the module-10-agents header and add only the wrapper_allowlist block at the same position. If they are already byte-identical, just apply the same block.

**FILES 7, 8, 9 — modules/module-10-agents/starter/track-{a,b,c}/config-starter.yaml:**

These are participant STARTER files. They should NOT include the populated allowlist (participants populate them during the Module 10 lab). Instead, update the existing comment to document the new `wrapper_allowlist` key.

For `modules/module-10-agents/starter/track-a/config-starter.yaml`, find line 27 ("# command_allowlist: leave empty for L2 Advisory (Track A default)") and line 29 (`command_allowlist: []`). REPLACE with:
```yaml
# command_allowlist: Hermes-native bypass key. Leave empty for L2 Advisory (Track A default)
command_allowlist: []

# wrapper_allowlist: Course-local enforcement key (Phase 7). Read by mock-psql.
# Leave empty during Module 10 exercise. Module 13 lab shows you how to populate it.
# Example (populate during Module 13):
# wrapper_allowlist:
#   psql:
#     - "SELECT "
#     - "EXPLAIN "
# wrapper_allowlist not set — intentionally absent at starter state
```

For `modules/module-10-agents/starter/track-b/config-starter.yaml`, find lines 30-34 and REPLACE with analogous Track B content mentioning `wrapper_allowlist.aws` with example `"ec2 describe-"` and `"rds describe-"`.

For `modules/module-10-agents/starter/track-c/config-starter.yaml`, find lines 32-33 and REPLACE with analogous Track C content mentioning `wrapper_allowlist.kubectl` with example `"get pods"` and `"describe pod "`.

**Final sync check:**
```bash
# Verify canonical agents/ and solution/ mirrors are identical
for track in a b c; do
  name_short=$track
  [ "$track" = "a" ] && name=database
  [ "$track" = "b" ] && name=finops
  [ "$track" = "c" ] && name=kubernetes
  diff "agents/track-${track}-${name}/config.yaml" "modules/module-10-agents/solution/track-${track}/config.yaml"
done
```
Expected: no diff output.
  </action>

  <verify>
    <automated>grep -q "^wrapper_allowlist:" agents/track-a-database/config.yaml && grep -q "^wrapper_allowlist:" agents/track-b-finops/config.yaml && grep -q "^wrapper_allowlist:" agents/track-c-kubernetes/config.yaml && diff -q agents/track-a-database/config.yaml modules/module-10-agents/solution/track-a/config.yaml && diff -q agents/track-b-finops/config.yaml modules/module-10-agents/solution/track-b/config.yaml && diff -q agents/track-c-kubernetes/config.yaml modules/module-10-agents/solution/track-c/config.yaml</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '^wrapper_allowlist:' agents/track-a-database/config.yaml` returns `1`
    - `grep -c '^wrapper_allowlist:' agents/track-b-finops/config.yaml` returns `1`
    - `grep -c '^wrapper_allowlist:' agents/track-c-kubernetes/config.yaml` returns `1`
    - `grep -c '  psql:' agents/track-a-database/config.yaml` returns `1`
    - `grep -c '  aws:' agents/track-b-finops/config.yaml` returns `1`
    - `grep -c '  kubectl:' agents/track-c-kubernetes/config.yaml` returns `1`
    - `grep -c 'kubectl:' agents/track-a-database/config.yaml` returns `0` (per-track isolation — no cross-tool entries)
    - `grep -c 'aws:' agents/track-c-kubernetes/config.yaml` returns `0`
    - `grep -c 'psql:' agents/track-b-finops/config.yaml` returns `0`
    - `grep -c 'command_allowlist:' agents/track-a-database/config.yaml` returns `1` (Hermes-native key preserved)
    - `grep -c 'command_allowlist:' agents/track-b-finops/config.yaml` returns `1`
    - `grep -c 'command_allowlist:' agents/track-c-kubernetes/config.yaml` returns `1`
    - `diff -q agents/track-a-database/config.yaml modules/module-10-agents/solution/track-a/config.yaml` exits 0 (files identical)
    - `diff -q agents/track-b-finops/config.yaml modules/module-10-agents/solution/track-b/config.yaml` exits 0
    - `diff -q agents/track-c-kubernetes/config.yaml modules/module-10-agents/solution/track-c/config.yaml` exits 0
    - `grep -c 'wrapper_allowlist' modules/module-10-agents/starter/track-a/config-starter.yaml` returns at least 1 (in comment)
    - `grep -c 'wrapper_allowlist' modules/module-10-agents/starter/track-b/config-starter.yaml` returns at least 1
    - `grep -c 'wrapper_allowlist' modules/module-10-agents/starter/track-c/config-starter.yaml` returns at least 1
    - Starter files do NOT have a populated wrapper_allowlist list: `grep -c '^wrapper_allowlist:$' modules/module-10-agents/starter/track-a/config-starter.yaml` returns `0` (only in comment form)
    - `grep -q "SOUL.md NEVER rules enforce" agents/track-b-finops/config.yaml` (existing Track B safety note preserved)
    - `grep -q "SOUL.md NEVER rules enforce" agents/track-c-kubernetes/config.yaml` (existing Track C safety note preserved)
  </acceptance_criteria>

  <done>All 9 YAML files updated per specification. Canonical agents/ files and solution/ mirror files are byte-identical. Starter files document the new key without populating it. All existing SOUL.md safety notes and command_allowlist keys preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Cascade Module 10 lab text updates (Track A and Track B, both .mdx and .md copies; verify Track C no-op)</name>
  <files>modules/module-10-agents/LAB-track-a-database.md, modules/module-10-agents/LAB-track-b-finops.md, course-site/docs/module-10-domain-agent/lab/LAB-track-a-database.mdx, course-site/docs/module-10-domain-agent/lab/LAB-track-b-finops.mdx</files>

  <read_first>
    - modules/module-10-agents/LAB-track-a-database.md — lines 340-360 to see the `command_allowlist` exercise block context (line 345 "command_allowlist:" intro, line 352 ["EXPLAIN"] code block)
    - modules/module-10-agents/LAB-track-b-finops.md — lines 305-315 to see the "Add read-only AWS command patterns" instruction (line 310)
    - course-site/docs/module-10-domain-agent/lab/LAB-track-a-database.mdx — lines 365-380 (mirror of the .md)
    - course-site/docs/module-10-domain-agent/lab/LAB-track-b-finops.mdx — lines 318-325 (mirror of the .md)
    - course-site/docs/module-10-domain-agent/lab/LAB-track-c-kubernetes.mdx — quick grep to confirm no `command_allowlist` or `wrapper_allowlist` references exist (verification task, expected no-op)
    - modules/module-10-agents/LAB-track-c-kubernetes.md — same verification
    - .planning/phases/07-guardrails-governance/07-CONTEXT.md — D-12 (Module 10 cascade update)
    - .planning/phases/07-guardrails-governance/07-RESEARCH.md — Module 10 Cascade Targets table
  </read_first>

  <action>
**SCOPE:** 4 files to edit (Track A .md, Track B .md, Track A .mdx, Track B .mdx), plus Track C verification (no edits expected).

**IMPORTANT FORMAT NOTE:** Module 10 teaches participants how the CONFIG file works at L2. The L4 progression is taught in Module 13. Module 10 should introduce `wrapper_allowlist` as a new key to know about but focus on L2 baseline. Module 10 should NOT reference Phase 7 or Module 13 by number (that's an implementation detail).

**TRACK A EDITS** — both `modules/module-10-agents/LAB-track-a-database.md` (line ~345) and `course-site/docs/module-10-domain-agent/lab/LAB-track-a-database.mdx` (line ~369).

Find the current passage that introduces `command_allowlist`. The existing text is:
```markdown
`command_allowlist`:
```
followed by a code block around line 376 (mdx) / 352 (md):
```yaml
command_allowlist: ["EXPLAIN"]
```

REPLACE the entire section (from "`command_allowlist`:" intro down through the code block close) with:

```markdown
### Two allowlists, two purposes

Your L2 config has two allowlist keys:

- **`command_allowlist`** (Hermes-native) — A bypass list for Hermes's built-in DANGEROUS_PATTERNS approval gate. An entry is a description-key string (like "SQL DROP") that tells Hermes "skip the approval prompt for this already-detected pattern." For Track A Database at L2, leave this empty — you want the approval gate to fire on every dangerous SQL match.

- **`wrapper_allowlist`** (course-local) — A command-prefix allowlist read by the `mock-psql` wrapper. Lists the SQL keywords the agent may invoke when `HERMES_LAB_GOVERNANCE=L2`. A command whose first keyword does not match any listed prefix is rejected with a loud GOVERNANCE REJECTED banner.

Your config snippet after the changes:

```yaml
command_allowlist: []

wrapper_allowlist:
  psql:
    - "SELECT "
    - "EXPLAIN "
    - "SHOW "
    - "DESCRIBE "
    - "\\d"
    - "\\dt"
    - "\\l"
```

This is the L2 baseline — read-only SQL plus psql meta-commands. Module 13 shows you how to progress this list through L3 and L4 governance levels.
```

**TRACK B EDITS** — both `modules/module-10-agents/LAB-track-b-finops.md` (line ~310) and `course-site/docs/module-10-domain-agent/lab/LAB-track-b-finops.mdx` (line ~322).

Find the current passage:
```markdown
- Add read-only AWS command patterns to `command_allowlist`
```

REPLACE with:
```markdown
- Two allowlists control what your agent can invoke:
  - `command_allowlist` (Hermes-native) — empty at L2, since Track B's most dangerous commands (`aws ec2 terminate-instances`) are NOT in DANGEROUS_PATTERNS and cannot be bypassed via this key
  - `wrapper_allowlist.aws` (course-local) — read-only AWS describe/get commands allowed; any other prefix is rejected by the `mock-aws` wrapper

Your L2 baseline looks like:

```yaml
command_allowlist: []

wrapper_allowlist:
  aws:
    - "sts get-caller-identity"
    - "ec2 describe-"
    - "rds describe-"
    - "cloudwatch get-metric-"
    - "cloudwatch describe-alarms"
    - "ce get-"
    - "cost-explorer get-"
```

Track B safety boundary: `aws ec2 terminate-instances` is not in `wrapper_allowlist.aws` and never will be at any governance level. Combined with SOUL.md NEVER rules, this is the two-layer defense Track B relies on. Module 13 walks through both layers.
```

**TRACK C VERIFICATION** — Track C was already touched by the Phase 6 cascade. Confirm no command_allowlist references exist in either file. If confirmed absent, no edits needed.

```bash
grep -c 'command_allowlist' modules/module-10-agents/LAB-track-c-kubernetes.md
grep -c 'command_allowlist' course-site/docs/module-10-domain-agent/lab/LAB-track-c-kubernetes.mdx
```

If both return 0, Track C cascade is a no-op (document this in SUMMARY.md). If either returns > 0, surface the finding and add analogous updates using the Track A/B pattern adapted for kubectl (wrapper_allowlist.kubectl with the L2 kubectl list).

**SYNC CHECK:** After editing, verify the .md and .mdx files remain functionally mirrored. The only expected differences are Docusaurus frontmatter / import statements in .mdx files. The body text of the `wrapper_allowlist` section should be identical.
  </action>

  <verify>
    <automated>grep -q "wrapper_allowlist" modules/module-10-agents/LAB-track-a-database.md && grep -q "wrapper_allowlist" modules/module-10-agents/LAB-track-b-finops.md && grep -q "wrapper_allowlist" course-site/docs/module-10-domain-agent/lab/LAB-track-a-database.mdx && grep -q "wrapper_allowlist" course-site/docs/module-10-domain-agent/lab/LAB-track-b-finops.mdx && ! grep -q "command_allowlist: \["EXPLAIN"\]" modules/module-10-agents/LAB-track-a-database.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'wrapper_allowlist' modules/module-10-agents/LAB-track-a-database.md` returns at least 2
    - `grep -c 'wrapper_allowlist' course-site/docs/module-10-domain-agent/lab/LAB-track-a-database.mdx` returns at least 2
    - `grep -c 'wrapper_allowlist' modules/module-10-agents/LAB-track-b-finops.md` returns at least 2
    - `grep -c 'wrapper_allowlist' course-site/docs/module-10-domain-agent/lab/LAB-track-b-finops.mdx` returns at least 2
    - `grep -q 'SELECT ' modules/module-10-agents/LAB-track-a-database.md`
    - `grep -q 'ec2 describe-' modules/module-10-agents/LAB-track-b-finops.md`
    - `grep -c 'command_allowlist: \["EXPLAIN"\]' modules/module-10-agents/LAB-track-a-database.md` returns `0` (old deprecated example replaced)
    - `grep -c 'command_allowlist: \["EXPLAIN"\]' course-site/docs/module-10-domain-agent/lab/LAB-track-a-database.mdx` returns `0`
    - `grep -q 'GOVERNANCE REJECTED' modules/module-10-agents/LAB-track-a-database.md` (explains the new behavior)
    - `grep -q 'two-layer defense' modules/module-10-agents/LAB-track-b-finops.md` OR `grep -q 'two allowlists' modules/module-10-agents/LAB-track-b-finops.md` (per-track safety narrative)
    - `grep -c 'Hermes-native' modules/module-10-agents/LAB-track-a-database.md` returns at least 1 (distinguishes the two allowlist mechanisms)
    - `grep -c 'Hermes-native' modules/module-10-agents/LAB-track-b-finops.md` returns at least 1
    - Track C verification command documented in SUMMARY.md output
    - Module 10 Track A .md and .mdx have equivalent wrapper_allowlist content (diff-check the new block text between the two files — they should match modulo the Docusaurus frontmatter)
  </acceptance_criteria>

  <done>Module 10 Track A and Track B lab files (both .md source and .mdx Docusaurus copies) are updated with the two-allowlist narrative and populated L2 baseline examples. The deprecated `command_allowlist: ["EXPLAIN"]` example is removed. Track C lab files are verified as no-op (or updated if stale references are found).</done>
</task>

</tasks>

<verification>
Run these validation commands after Task 1 and Task 2 complete:

```bash
# 1. Verify all 9 profile-config files are consistent
for track in a b c; do
  case "$track" in
    a) name=database; tool=psql ;;
    b) name=finops; tool=aws ;;
    c) name=kubernetes; tool=kubectl ;;
  esac
  echo "=== Track $track ($tool) ==="
  grep -A1 "^wrapper_allowlist:" "agents/track-${track}-${name}/config.yaml" | head -2
  diff -q "agents/track-${track}-${name}/config.yaml" "modules/module-10-agents/solution/track-${track}/config.yaml"
done

# 2. Verify Module 10 lab files cross-reference the new key
grep -c 'wrapper_allowlist' course-site/docs/module-10-domain-agent/lab/LAB-track-a-database.mdx
grep -c 'wrapper_allowlist' course-site/docs/module-10-domain-agent/lab/LAB-track-b-finops.mdx
grep -c 'wrapper_allowlist' modules/module-10-agents/LAB-track-a-database.md
grep -c 'wrapper_allowlist' modules/module-10-agents/LAB-track-b-finops.md

# 3. Verify no old deprecated example survives
! grep -r 'command_allowlist: \["EXPLAIN"\]' modules/module-10-agents/ course-site/docs/module-10-domain-agent/
```

All three sections must produce the expected output.
</verification>

<success_criteria>
1. All 9 YAML config files have populated `wrapper_allowlist` (canonical + solution) or documented placeholder (starter)
2. Canonical `agents/track-*/config.yaml` and solution mirror `modules/module-10-agents/solution/track-*/config.yaml` are byte-identical
3. Module 10 Track A and Track B lab files (.md and .mdx) teach the two-allowlist distinction
4. No file outside `files_modified` is touched
5. No stale `command_allowlist: ["EXPLAIN"]` references remain in Module 10 content
</success_criteria>

<output>
After completion, create `.planning/phases/07-guardrails-governance/07-02-SUMMARY.md` using the template at `$HOME/.claude/get-shit-done/templates/summary.md`. Include:
- Exact path to each modified file
- Confirmation that canonical agents/ and solution/ mirrors are byte-identical
- Track C verification outcome (no-op or updates made)
- The exact two-allowlist narrative block used in Track A (for reference by Plan 07-03 when updating Module 13 reading)
</output>
