---
phase: 13-module-13-governance-refactor-wrapper-free-track-c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - course-site/docs/module-13-governance/lab/LAB-track-c-kubernetes.mdx
autonomous: true
requirements: [GOVR-01, GOVR-02]

must_haves:
  truths:
    - "Track C lab demonstrates L1 (no terminal), L2 (SOUL.md refuses kubectl delete), L3 (smart approval, fewer NEVER rules), L4 (SOUL.md allows apply/rollout undo) -- all without wrapper_allowlist"
    - "Lab explicitly teaches SOUL.md as production governance mechanism"
    - "DANGEROUS_PATTERNS demo (rm -rf) is preserved and shown alongside SOUL.md refusal"
    - "Zero references to HERMES_LAB_MODE, HERMES_LAB_GOVERNANCE, wrapper_allowlist, GOVERNANCE REJECTED, mock-kubectl, or ~/.bash_profile alias"
  artifacts:
    - path: "course-site/docs/module-13-governance/lab/LAB-track-c-kubernetes.mdx"
      provides: "Refactored Track C governance lab with SOUL.md-primary L1-L4 progression"
      contains: "SOUL.md NEVER rules"
  key_links:
    - from: "LAB-track-c-kubernetes.mdx L2 step"
      to: "agents/track-c-kubernetes/SOUL.md"
      via: "SOUL.md NEVER rules for kubectl delete/drain/cordon"
      pattern: "NEVER.*kubectl delete"
    - from: "LAB-track-c-kubernetes.mdx L3 step"
      to: "SOUL.md editing"
      via: "Remove kubectl exec from NEVER block"
      pattern: "remove.*NEVER"
    - from: "LAB-track-c-kubernetes.mdx L4 step"
      to: "SOUL.md editing"
      via: "Remove apply and rollout undo from NEVER block"
      pattern: "apply.*rollout undo"
---

<objective>
Rewrite the Module 13 Track C governance lab (LAB-track-c-kubernetes.mdx) so the L1-L4
progression uses SOUL.md behavioral enforcement as the primary mechanism instead of
wrapper_allowlist mechanical enforcement. The wrapper infrastructure was removed in Phase 10;
this lab must work without it.

Purpose: Track C's governance story should teach that SOUL.md NEVER rules are the production
governance mechanism for kubectl commands (which are not in Hermes DANGEROUS_PATTERNS).
Wrapper allowlists were course-local teaching scaffolding, not production patterns.

Output: Fully rewritten LAB-track-c-kubernetes.mdx (~800-1000 lines)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-module-13-governance-refactor-wrapper-free-track-c/13-CONTEXT.md
@agents/track-c-kubernetes/SOUL.md
@agents/track-c-kubernetes/config.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite LAB-track-c-kubernetes.mdx with SOUL.md-primary governance</name>
  <files>course-site/docs/module-13-governance/lab/LAB-track-c-kubernetes.mdx</files>
  <read_first>
    - course-site/docs/module-13-governance/lab/LAB-track-c-kubernetes.mdx (current lab -- understand full structure)
    - .planning/phases/13-module-13-governance-refactor-wrapper-free-track-c/13-CONTEXT.md (L1-L4 step details)
    - agents/track-c-kubernetes/SOUL.md (current NEVER rules -- the governance mechanism)
    - agents/track-c-kubernetes/config.yaml (current config structure)
  </read_first>
  <action>
Complete rewrite of LAB-track-c-kubernetes.mdx. The new lab has SOUL.md NEVER rules as the
primary governance mechanism for kubectl commands, with DANGEROUS_PATTERNS as the second
independent layer for shell destructives. No wrapper_allowlist, no HERMES_LAB_MODE, no
HERMES_LAB_GOVERNANCE, no mock-kubectl, no GOVERNANCE REJECTED banner.

**Frontmatter:** Keep existing id (module-13-lab-track-c), title, sidebar_label, sidebar_position.

**Header section:**
- Duration: 90 minutes (60 min guided + 30 min free explore)
- Track: C -- Kubernetes Health and Self-Healing
- Prerequisite: Module 8 Track C complete (profile at ~/.hermes/profiles/track-c/ with SOUL.md, config.yaml, sre-k8s-pod-health skill) + KIND cluster running
- Outcome: Kiran configured through L1-L4 governance progression, SOUL.md demonstrated as load-bearing safety mechanism, DANGEROUS_PATTERNS demonstrated for shell commands, audit trail readable via sqlite3
- Tip callout: same tone as current ("Governance is not about trust...")
- Important callout: Rewrite to focus on two-layer model:
  "Track C is where the behavioral-vs-mechanical safety split is most visible. The three most
  destructive Kubernetes commands -- kubectl delete, kubectl drain, kubectl cordon -- are NOT in
  Hermes DANGEROUS_PATTERNS. They are governed ONLY by Kiran's SOUL.md NEVER rules. This makes
  SOUL.md the load-bearing safety mechanism for Track C. You will see this play out in Step 5."

**GUIDED PHASE (Steps 1-14):**

Step 1: Prerequisites and Backup (5 min)
- Verify Kiran works: hermes -p track-c chat, ask identity question
- Verify profile path: ls ~/.hermes/profiles/track-c/config.yaml
- Backup config AND SOUL.md:
  cp ~/.hermes/profiles/track-c/config.yaml ~/.hermes/profiles/track-c/config.yaml.backup
  cp ~/.hermes/profiles/track-c/SOUL.md ~/.hermes/profiles/track-c/SOUL.md.backup
- NOTE: SOUL.md backup is critical because L3/L4 steps EDIT the NEVER rules

Step 2: Understand the Two Governance Layers (5 min)
- Teaching: explain the two independent layers for Track C
  Layer 1: DANGEROUS_PATTERNS (mechanical, Hermes-native, catches rm -rf, dd, chmod 777, etc.)
  Layer 2: SOUL.md NEVER rules (behavioral, identity-level, catches kubectl delete/drain/cordon)
- Show Kiran's current SOUL.md NEVER rules:
  grep "NEVER" ~/.hermes/profiles/track-c/SOUL.md
- Show relevant DANGEROUS_PATTERNS categories: shell destructives like rm -r, dd if=, chmod 777
- Teaching callout: "kubectl delete is NOT in DANGEROUS_PATTERNS. If you removed the NEVER rules from SOUL.md, kubectl delete would run silently with no approval prompt. SOUL.md is load-bearing for Track C."

Step 3: Apply L1 -- No Terminal (10 min)
- Config change: platform_toolsets.cli: [web, skills] (no terminal)
- Demo: ask agent to list pods -- agent proposes commands as text, cannot execute
- Teaching: L1 is advisory mode, zero risk of kubectl execution
- Per CONTEXT.md L1 spec

Step 4: Diff L1 to L2 (5 min)
- Show the diff: only platform_toolsets.cli changes (gains terminal, file)
- Teaching: "One line change, one profound capability shift. Terminal access is the gate."
- NO wrapper_allowlist diff -- the diff is ONLY the toolset line
- Teaching callout: "At L2, Kiran can now run kubectl commands. But the SOUL.md NEVER rules
  constrain which kubectl verbs are allowed. The NEVER block says: never delete, drain, cordon,
  apply, patch, exec, edit. Only get, describe, logs, top, and rollout status/history pass."

Step 5: Apply L2 -- SOUL.md Read-Only Enforcement (15 min)
- Config: platform_toolsets.cli: [terminal, file, web, skills], approvals.mode: manual
- NO env var exports, NO HERMES_LAB_GOVERNANCE, NO wrapper setup
- Just: hermes -p track-c chat

  Step 5a: SOUL.md refusal demo
  - Ask: "Delete pod api-deployment-def456 in the app namespace"
  - Expected: Kiran refuses verbally citing SOUL.md NEVER rules
    "I cannot execute kubectl delete -- this is prohibited by my operational rules."
  - Observation: no approval gate fires, no banner. Kiran refused at the behavioral layer
    BEFORE forming a terminal command.

  Step 5b: DANGEROUS_PATTERNS demo (preserved from current lab)
  - Ask: "Clean up the /tmp/test-logs directory recursively so I can free up space"
  - Expected: Kiran proposes rm -rf /tmp/test-logs, DANGEROUS_PATTERNS fires approval prompt
    [o]nce | [s]ession | [a]lways | [d]eny
  - Type d to deny
  - Teaching: "Two different safety mechanisms, both active at L2"
    Table showing kubectl delete caught by SOUL.md (behavioral) vs rm -rf caught by
    DANGEROUS_PATTERNS (mechanical)

  Teaching callout (CRITICAL): Explain that kubectl verbs are NOT in DANGEROUS_PATTERNS.
  SOUL.md is the ONLY protection for kubectl delete/drain/cordon. This is by design -- kubectl
  subcommands would cause too many false positives in regex matching. SOUL.md provides
  domain-aware behavioral constraints instead.

Step 6: Diff L2 to L3 -- SOUL.md Policy Change + Smart Approval (5 min)
- Two changes happen at L3:
  1. approvals.mode: manual -> smart (reduces DANGEROUS_PATTERNS friction)
  2. SOUL.md edit: remove kubectl exec from NEVER block, effectively allowing investigation
     commands (rollout history, rollout status, diff, explain were never in NEVER block anyway)
- Show the SOUL.md diff: the line "NEVER execute kubectl exec..." is removed
- Teaching: "Trust escalation means editing SOUL.md to remove restrictions. The NEVER rules
  ARE the governance. Removing a NEVER rule is a promotion decision."

Step 7: Apply L3 -- Smart Approval + Investigation (10 min)
- Config: approvals.mode: smart
- SOUL.md edit: comment out or remove the "NEVER execute kubectl exec" line
  (keep delete, drain, cordon, apply, patch, edit in NEVER block)
- Demo: Ask for rollout history -- runs without friction
- Demo: Ask to delete pod -- still refuses (SOUL.md NEVER rule)
- Teaching: "Smart approval reduces friction for DANGEROUS_PATTERNS false positives. But
  SOUL.md NEVER rules are independent of approval mode. kubectl delete is refused by SOUL.md
  whether approval mode is manual or smart."

Step 8: Read Your Session Audit Trail (10 min)
- Keep the same audit trail section from current lab (sqlite3 queries)
- Adjust any references that mention wrapper or GOVERNANCE REJECTED -- replace with SOUL.md
  refusal patterns in queries
- Key queries:
  1. List recent sessions
  2. Show terminal tool calls in most recent session
  3. Find kubectl commands
  4. Find refusal events (SOUL.md NEVER activations)

Step 9: Apply L4 -- SOUL.md Allows Targeted Mutations (10 min)
- SOUL.md edit: remove "apply" and "patch" from the NEVER block
  The NEVER line currently says "NEVER execute kubectl exec, kubectl edit, kubectl patch, or kubectl apply"
  At L4: remove "kubectl apply" from that line (and kubectl patch if desired)
  Also: the "rollout undo" is not explicitly in a NEVER rule, but propose self-healing actions
  language restricts it. At L4: edit the scope line to say mutations like apply and rollout undo
  are permitted.
- Config stays: approvals.mode: smart (same as L3)
- Demo: Ask to apply a corrected manifest -- agent executes kubectl apply
- Demo: Ask to delete pod -- STILL refuses (delete NEVER leaves the NEVER block at any level)
- Teaching: "Even at L4, delete/drain/cordon are permanently forbidden. The agent's identity
  does not allow it. L4 is not no governance -- it is targeted mutations allowed."
- Show the SOUL.md diff from L2 baseline to L4 to illustrate the full progression:
  L2 NEVER: delete, drain, cordon, apply, patch, exec, edit
  L3 NEVER: delete, drain, cordon, apply, patch, edit (exec removed)
  L4 NEVER: delete, drain, cordon (apply, patch, edit removed; exec already removed at L3)

Step 10: Attempt a Destructive Command at L4 (5 min)
- Ask Kiran to delete a pod at L4
- Expected: Kiran still refuses (SOUL.md NEVER rule for kubectl delete is permanent)
- Teaching: "This is the core Track C governance story. Even at L4, with apply and rollout
  undo permitted, kubectl delete is forever in the NEVER block. The SOUL.md is your
  governance boundary."
- Contrast with DANGEROUS_PATTERNS: rm -rf would fire the mechanical gate regardless of
  SOUL.md. kubectl delete has ONLY the SOUL.md gate. That is why SOUL.md drift is a governance
  incident for Track C.

Step 11: Query Audit Trail for Refusal Events (5 min)
- sqlite3 query for SOUL.md refusal events (like current Step 12 but without
  GOVERNANCE REJECTED references)
- Query patterns: content LIKE '%cannot execute%kubectl%' OR content LIKE '%prohibited%'
  OR content LIKE '%NEVER%'

Step 12: Review DANGEROUS_PATTERNS Relevance for Track C (5 min)
- Keep same content as current Step 13 -- which DANGEROUS_PATTERNS Track C actually hits
  (rm -r, systemctl stop, kill -9 -1, chmod 777) vs which it does NOT hit (kubectl delete,
  drain, cordon, exec, apply)
- Remove any wrapper_allowlist references from the explanatory text

Step 13: Restore Kiran to Working Config (5 min)
- CRITICAL: Restore BOTH config.yaml AND SOUL.md from backups
  cp ~/.hermes/profiles/track-c/config.yaml.backup ~/.hermes/profiles/track-c/config.yaml
  cp ~/.hermes/profiles/track-c/SOUL.md.backup ~/.hermes/profiles/track-c/SOUL.md
- Option A: Restore from backup (returns to Module 8 state)
- Option B: Keep L3 config + partially edited SOUL.md (advanced users only)
- Verify Kiran still works

**FREE EXPLORE PHASE (Steps 14-16):**

Step 14: Challenge 1 -- Write Kiran's Promotion Criteria (Starter, 10 min)
- Same structure as current Step 15 but focus on SOUL.md refusal rates instead of
  wrapper_allowlist rejection rates
- Promotion criteria template references SOUL.md NEVER refusal counts, not GOVERNANCE
  REJECTED banner counts

Step 15: Challenge 2 -- Why SOUL.md Drift Is a Governance Incident (Intermediate, 10 min)
- Replace the current "Why command_allowlist Cannot Protect Track C" challenge
- New challenge: explain why editing SOUL.md without governance review is an incident
  for Track C (unlike Track A where DANGEROUS_PATTERNS provides a mechanical backstop)
- Thought experiment: what happens if someone removes the NEVER kubectl delete line?
  Answer: nothing catches it. No DANGEROUS_PATTERNS match. No wrapper. Silent execution.
- Security review question: "A teammate edits Kiran's SOUL.md and removes the kubectl delete
  NEVER rule so Kiran can auto-clean crashlooping pods. What happens next time Kiran
  encounters a user asking it to delete a pod?"

Step 16: Challenge 3 -- Compare Track C L4 to Track A L4 (Advanced, 20 min)
- Keep the asymmetry comparison but reframe without wrapper_allowlist
- Track A: DANGEROUS_PATTERNS catches SQL DROP mechanically + SOUL.md refuses behaviorally
  = two independent layers, both fire
- Track C: SOUL.md refuses behaviorally (only layer for kubectl delete/drain/cordon)
  + DANGEROUS_PATTERNS catches rm -rf mechanically (independent, different commands)
  = two layers but for DIFFERENT command categories
- Questions updated to remove wrapper references

**Closing section:**
- Summary of what was observed at each level
- Use two-layer model language throughout
- Governance spectrum sentence: same concept but no wrapper references
- Next: Module 14 capstone

**Verification checklist:**
- Remove checks 7 and 8 (wrapper_allowlist and mock-kubectl checks)
- Keep checks 1-6 and 9 (governance yaml presence, config level, terminal toolset, Kiran
  works, session database, diff commands, SOUL.md NEVER rules)
- Add: grep -c "wrapper_allowlist" LAB-track-c-kubernetes.mdx should return 0
- Add: grep -c "HERMES_LAB_MODE" LAB-track-c-kubernetes.mdx should return 0

**Zero-tolerance removals (grep these to verify):**
- HERMES_LAB_MODE
- HERMES_LAB_GOVERNANCE
- HERMES_LAB_TRACK
- HERMES_LAB_WRAPPERS
- MOCK_DATA_DIR
- wrapper_allowlist
- mock-kubectl
- GOVERNANCE REJECTED
- ~/.bash_profile alias
- three-layer (replace with two-layer everywhere)
- Layer 1.*wrapper (old Layer 1 was wrapper; new Layer 1 is DANGEROUS_PATTERNS)
  </action>
  <verify>
    <automated>cd /Users/gshah/work/agentic/devops/course && grep -cE 'HERMES_LAB_MODE|HERMES_LAB_GOVERNANCE|wrapper_allowlist|GOVERNANCE REJECTED|mock-kubectl|HERMES_LAB_WRAPPERS|HERMES_LAB_TRACK|MOCK_DATA_DIR|bash_profile.*alias' course-site/docs/module-13-governance/lab/LAB-track-c-kubernetes.mdx | grep -q '^0$' && echo "PASS: zero wrapper references" || echo "FAIL: wrapper references found"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "HERMES_LAB_MODE" course-site/docs/module-13-governance/lab/LAB-track-c-kubernetes.mdx returns 0
    - grep -c "HERMES_LAB_GOVERNANCE" course-site/docs/module-13-governance/lab/LAB-track-c-kubernetes.mdx returns 0
    - grep -c "wrapper_allowlist" course-site/docs/module-13-governance/lab/LAB-track-c-kubernetes.mdx returns 0
    - grep -c "GOVERNANCE REJECTED" course-site/docs/module-13-governance/lab/LAB-track-c-kubernetes.mdx returns 0
    - grep -c "mock-kubectl" course-site/docs/module-13-governance/lab/LAB-track-c-kubernetes.mdx returns 0
    - grep -c "HERMES_LAB_WRAPPERS" course-site/docs/module-13-governance/lab/LAB-track-c-kubernetes.mdx returns 0
    - grep -c "SOUL.md NEVER" course-site/docs/module-13-governance/lab/LAB-track-c-kubernetes.mdx returns at least 5
    - grep -c "DANGEROUS_PATTERNS" course-site/docs/module-13-governance/lab/LAB-track-c-kubernetes.mdx returns at least 3
    - grep -c "rm -rf" course-site/docs/module-13-governance/lab/LAB-track-c-kubernetes.mdx returns at least 1
    - grep -c "two-layer\|two layer\|Two-layer\|Two layer" course-site/docs/module-13-governance/lab/LAB-track-c-kubernetes.mdx returns at least 1
    - File contains "Step 1:" through at least "Step 13:" (full L1-L4 progression)
    - File contains "L1" "L2" "L3" "L4" in step headers
    - File contains "SOUL.md.backup" (SOUL.md backup instruction)
    - File size is between 600 and 1200 lines
  </acceptance_criteria>
  <done>
    LAB-track-c-kubernetes.mdx fully rewritten with SOUL.md-primary governance. L1-L4 progression
    uses SOUL.md NEVER rule editing (not wrapper_allowlist expansion) as the trust escalation
    mechanism. DANGEROUS_PATTERNS rm -rf demo preserved. Zero HERMES_LAB_MODE, wrapper_allowlist,
    GOVERNANCE REJECTED, or mock-kubectl references anywhere in the file.
  </done>
</task>

</tasks>

<verification>
1. Zero-tolerance grep check: HERMES_LAB_MODE, HERMES_LAB_GOVERNANCE, wrapper_allowlist, GOVERNANCE REJECTED, mock-kubectl all return 0 matches
2. SOUL.md is referenced as primary governance mechanism throughout
3. DANGEROUS_PATTERNS demo (rm -rf) is present and functional
4. L1 through L4 steps are all present with correct progression
5. SOUL.md backup instruction is present (learners edit SOUL.md during the lab)
6. Two-layer model language used (not three-layer)
</verification>

<success_criteria>
Track C lab demonstrates L1-L4 governance progression using SOUL.md NEVER rules as the primary
enforcement mechanism. Zero wrapper infrastructure references. DANGEROUS_PATTERNS demo preserved
for shell destructives. Learners understand that SOUL.md is load-bearing for Track C because
kubectl verbs are not in DANGEROUS_PATTERNS.
</success_criteria>

<output>
After completion, create `.planning/phases/13-module-13-governance-refactor-wrapper-free-track-c/13-01-SUMMARY.md`
</output>
