---
phase: 13-module-13-governance-refactor-wrapper-free-track-c
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - course-site/docs/module-13-governance/reading/concepts.mdx
  - course-site/docs/module-13-governance/reading/reference.mdx
  - course-site/docs/module-13-governance/README.mdx
autonomous: true
requirements: [GOVR-01, GOVR-02]

must_haves:
  truths:
    - "concepts.mdx teaches two-layer model (DANGEROUS_PATTERNS + SOUL.md) for Track C without mentioning wrapper_allowlist as a layer"
    - "reference.mdx L1-L4 config examples show SOUL.md progression for Track C, not wrapper_allowlist expansion"
    - "README.mdx prerequisites reference Module 8 Track C (not Module 10) and list KIND cluster as requirement"
    - "Zero wrapper_allowlist references in Track C-specific sections of reading materials"
  artifacts:
    - path: "course-site/docs/module-13-governance/reading/concepts.mdx"
      provides: "Updated governance concepts with two-layer model for Track C"
      contains: "Two Layers of Governance"
    - path: "course-site/docs/module-13-governance/reading/reference.mdx"
      provides: "Updated config templates showing SOUL.md-based Track C L4"
      contains: "SOUL.md NEVER rules"
    - path: "course-site/docs/module-13-governance/README.mdx"
      provides: "Updated prerequisites for Module 13"
      contains: "Module 8"
  key_links:
    - from: "concepts.mdx Section 3"
      to: "LAB-track-c-kubernetes.mdx"
      via: "Two-layer model teaching"
      pattern: "SOUL.md.*load-bearing"
    - from: "reference.mdx L4 Track C"
      to: "agents/track-c-kubernetes/SOUL.md"
      via: "NEVER rule progression"
      pattern: "NEVER.*kubectl"
---

<objective>
Update Module 13 reading materials (concepts.mdx, reference.mdx) and README.mdx to align
with the wrapper-free Track C governance model. The lab (Plan 01) now teaches SOUL.md as the
primary governance mechanism; reading materials must match.

Purpose: Ensure consistency between lab content and supporting reading materials. Learners
reading concepts.mdx or reference.mdx before the lab should encounter the same two-layer
model (DANGEROUS_PATTERNS + SOUL.md) taught in the lab.

Output: Updated concepts.mdx, reference.mdx, README.mdx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-module-13-governance-refactor-wrapper-free-track-c/13-CONTEXT.md
@agents/track-c-kubernetes/SOUL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update concepts.mdx -- two-layer model for Track C</name>
  <files>course-site/docs/module-13-governance/reading/concepts.mdx</files>
  <read_first>
    - course-site/docs/module-13-governance/reading/concepts.mdx (full file)
    - .planning/phases/13-module-13-governance-refactor-wrapper-free-track-c/13-CONTEXT.md
  </read_first>
  <action>
Light edits to concepts.mdx Section 3 ("The Two Layers of Governance in Hermes") and the
Track B/C subsection. The current content already uses a two-layer model (behavioral SOUL.md
+ mechanical DANGEROUS_PATTERNS) which is CORRECT. However, it needs these specific changes:

1. Section 3 heading: Already says "The Two Layers of Governance in Hermes" -- KEEP as-is.

2. Section 3, "Critical Distinction for Track B and Track C" subsection (around line 149-154):
   Currently says: "Safety for these commands is entirely behavioral (SOUL.md NEVER rules).
   This is intentional..."
   This is ALREADY CORRECT for the wrapper-free model. Verify it does NOT mention
   wrapper_allowlist anywhere. If it does, remove those references.

3. Scan the ENTIRE file for any mention of:
   - wrapper_allowlist
   - HERMES_LAB_MODE
   - HERMES_LAB_GOVERNANCE
   - mock-kubectl
   - three-layer (should be two-layer for Track C context)
   - "Layer 1" referring to wrapper_allowlist

   If any of these appear, remove or rewrite to use the two-layer model.

4. The file currently discusses the two-layer model correctly for Track B and C. The key
   teaching point to VERIFY is present: "If Finley's or Kiran's SOUL.md NEVER rules were
   removed, there would be no mechanical backstop. The behavioral governance layer is
   load-bearing for Tracks B and C, not optional."

5. Summary table at the bottom: verify "Behavioral governance" and "Mechanical governance"
   rows exist without wrapper_allowlist references.

This should be a light-touch edit. The concepts.mdx was already written with the correct
two-layer model. The task is to verify and clean up any Phase 7 wrapper references that
may have leaked in.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/agentic/devops/course && grep -cE 'wrapper_allowlist|HERMES_LAB_MODE|HERMES_LAB_GOVERNANCE|mock-kubectl' course-site/docs/module-13-governance/reading/concepts.mdx | grep -q '^0$' && echo "PASS: zero wrapper references in concepts" || echo "FAIL: wrapper references found"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "wrapper_allowlist" course-site/docs/module-13-governance/reading/concepts.mdx returns 0
    - grep -c "HERMES_LAB_MODE" course-site/docs/module-13-governance/reading/concepts.mdx returns 0
    - grep -c "load-bearing" course-site/docs/module-13-governance/reading/concepts.mdx returns at least 1
    - grep -c "Two Layers" course-site/docs/module-13-governance/reading/concepts.mdx returns at least 1
    - grep -c "SOUL.md NEVER" course-site/docs/module-13-governance/reading/concepts.mdx returns at least 2
  </acceptance_criteria>
  <done>
    concepts.mdx uses two-layer model consistently for Track C. Zero wrapper_allowlist or
    mock-mode references. SOUL.md described as load-bearing for Tracks B and C.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update reference.mdx -- remove wrapper_allowlist from Track C configs + update README.mdx prerequisites</name>
  <files>
    course-site/docs/module-13-governance/reading/reference.mdx
    course-site/docs/module-13-governance/README.mdx
  </files>
  <read_first>
    - course-site/docs/module-13-governance/reading/reference.mdx (full file -- especially sections 1, 1.5, 2)
    - course-site/docs/module-13-governance/README.mdx (full file)
    - .planning/phases/13-module-13-governance-refactor-wrapper-free-track-c/13-CONTEXT.md
  </read_first>
  <action>
**reference.mdx changes:**

1. Section 1 L2 config example (around line 44-86): Remove the entire wrapper_allowlist block
   and the "Course-local wrapper enforcement (Phase 7)" comment from the L2 YAML example.
   The L2 config should show ONLY: model, platform_toolsets, approvals, command_allowlist,
   agent. The "Diff from L1" paragraph should say: "platform_toolsets.cli gains terminal and
   file" -- remove any mention of wrapper_allowlist being added.

2. Section 1 L3 config example (around line 98-160): Same treatment -- remove wrapper_allowlist
   block from L3 YAML. The diff description should say: "approvals.mode changes from manual
   to smart" plus "SOUL.md NEVER rules are edited to allow investigation commands
   (kubectl exec removed from NEVER block)" -- no wrapper_allowlist expansion mention.

3. Section 1 L4 Track C config example (around line 255-298): Remove wrapper_allowlist block.
   The L4 Track C config should show the same model/platform_toolsets/approvals/command_allowlist
   structure. Add a comment block explaining: "L4 Track C: SOUL.md NEVER rules edited to
   allow kubectl apply and rollout undo. Delete, drain, and cordon remain in NEVER block
   permanently." Remove the "NOTE: kubectl delete, drain..." trailing comment about wrapper
   allowlist -- replace with a note about SOUL.md NEVER rules.

4. Section 1.5 "Course-Local Wrapper Enforcement (wrapper_allowlist)" (around line 300-374):
   This ENTIRE section should be REMOVED or replaced with a brief note:
   "Note: Earlier versions of this course used course-local wrapper scripts (mock-kubectl,
   mock-aws, mock-psql) with a wrapper_allowlist config key for mechanical enforcement of
   kubectl/aws/psql command prefixes. This mechanism has been removed. Track C now uses
   SOUL.md NEVER rules as the primary governance mechanism for kubectl commands, with Hermes
   DANGEROUS_PATTERNS providing mechanical enforcement for shell destructives (rm -rf, dd,
   chmod 777, etc.)."
   Keep this note brief (4-5 lines max).

5. Section 1.5 "The three-layer defense model" table (around line 339-351): Replace with a
   two-layer defense model table:
   | Layer | Mechanism | Scope | Track A (DROP TABLE) | Track B (terminate) | Track C (kubectl delete) |
   | Layer 1 | DANGEROUS_PATTERNS | Platform-native, mechanical | FIRES (SQL DROP) | NO MATCH | NO MATCH |
   | Layer 2 | SOUL.md NEVER rules | Identity-level, behavioral | REFUSES | REFUSES | REFUSES |

6. Section 1.5 wrapper parsing code block (awk script, around line 355-370): REMOVE entirely.

7. Section 2 "Diff Commands" (around line 377-388): Update diff descriptions to reflect
   simpler diffs (no wrapper_allowlist blocks in the files being diffed).

8. Section 4 "Track-Specific Pattern Notes" (around line 480-492): The Track B/C paragraph
   currently says: "Their destructive commands are intentionally NOT in the list. SOUL.md NEVER
   rules are the sole governance mechanism." This is CORRECT -- verify and keep. Remove any
   wrapper_allowlist references if present.

**README.mdx changes:**

1. Prerequisites (around line 39-41): Change from:
   "Module 10 completed (you have a working domain agent)"
   To:
   "Module 8 completed (you have a working Track C agent with Kiran's SOUL.md, config.yaml,
   and the sre-k8s-pod-health skill installed)"

2. Also update: "Modules 11-12 completed (agent is deployed with fleet and trigger configuration)"
   To: "Modules 11-12 completed (triggers and fleet configuration in place)"
   This is a minor wording improvement.

3. Lab info callout (around line 14): Keep "The hands-on lab for this module lives in the
   Hermes repository" but verify this is accurate. If Track C lab is in course-site (it is),
   update to say the lab is available per-track in this course site.

4. Important callout "Why Track C is the clearest governance story" (line 22-29 if present):
   Update to use two-layer language, remove wrapper_allowlist reference.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/agentic/devops/course && REFS=$(grep -cE 'wrapper_allowlist|HERMES_LAB_MODE|HERMES_LAB_GOVERNANCE|mock-kubectl|mock-aws|mock-psql' course-site/docs/module-13-governance/reading/reference.mdx course-site/docs/module-13-governance/README.mdx 2>/dev/null | awk -F: '{sum+=$NF} END{print sum}') && [ "$REFS" -eq 0 ] && echo "PASS: zero wrapper references in reference + README" || echo "FAIL: $REFS wrapper references found"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "wrapper_allowlist" course-site/docs/module-13-governance/reading/reference.mdx returns 0 (in active content; a single historical note mentioning removal is acceptable)
    - grep -c "HERMES_LAB_MODE" course-site/docs/module-13-governance/reading/reference.mdx returns 0
    - grep -c "HERMES_LAB_GOVERNANCE" course-site/docs/module-13-governance/reading/reference.mdx returns 0
    - grep -c "mock-kubectl" course-site/docs/module-13-governance/reading/reference.mdx returns 0 (excluding historical note)
    - grep -c "Module 8" course-site/docs/module-13-governance/README.mdx returns at least 1
    - grep -c "SOUL.md NEVER" course-site/docs/module-13-governance/reading/reference.mdx returns at least 2
    - The L4 Track C config example in reference.mdx contains a SOUL.md NEVER rules comment
    - Section 1.5 three-layer table replaced with two-layer table
  </acceptance_criteria>
  <done>
    reference.mdx cleaned of wrapper_allowlist config blocks and wrapper parsing code.
    L1-L4 config examples show platform_toolsets + approvals + command_allowlist only (no
    wrapper_allowlist). Three-layer defense model replaced with two-layer model. README.mdx
    prerequisites updated to Module 8 + KIND cluster.
  </done>
</task>

</tasks>

<verification>
1. Zero wrapper_allowlist references in active content across concepts.mdx, reference.mdx, README.mdx
2. Two-layer model language used consistently (not three-layer)
3. SOUL.md described as load-bearing for Track C in both concepts and reference
4. README prerequisites reference Module 8 (not Module 10)
5. L4 Track C config example shows SOUL.md-based governance, not wrapper-based
</verification>

<success_criteria>
All three supporting files (concepts.mdx, reference.mdx, README.mdx) align with the
wrapper-free Track C governance model. Two-layer defense (DANGEROUS_PATTERNS + SOUL.md)
taught consistently. Prerequisites updated. Zero active wrapper_allowlist references.
</success_criteria>

<output>
After completion, create `.planning/phases/13-module-13-governance-refactor-wrapper-free-track-c/13-02-SUMMARY.md`
</output>
