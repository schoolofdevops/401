---
phase: 08-agent-triggers
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
files_modified:
  - course-site/docs/module-12-triggers/lab/LAB.mdx
  - modules/module-12-triggers/LAB.md
  - course-site/docs/module-12-triggers/reading/reference.mdx
  - course-site/docs/module-12-triggers/quiz/QUIZ.mdx
autonomous: true
requirements:
  - TRIG-01
  - TRIG-02
  - TRIG-03
  - TRIG-04
must_haves:
  truths:
    - "A participant opening Module 12 lab finds the GUIDED PHASE has grown from 8 steps to 16 steps — the existing Steps 1-8 are intact, and 8 new steps (Steps 9-16) are inserted after Step 8 in the order specified by CONTEXT.md D-21"
    - "A participant following new Step 9 (AlertManager setup) runs helm upgrade with the updated prometheus-lab-values.yaml and applies the PrometheusRule manifest from infrastructure/scenarios/k8s/alertmanager/, then verifies AlertManager is reachable on port 30093"
    - "A participant following new Step 10 (AlertManager fire+observe) applies the Phase 6 crashloop2 scenario, watches the alert fire within 2 minutes, and observes the Hermes gateway receive the POST and run the agent diagnosis"
    - "A participant following new Step 11 (K8s CronJob comparison) builds the hermes-lab:cronjob image from infrastructure/scenarios/k8s/cronjob/Dockerfile, loads it into KIND, applies the per-track CronJob manifest, and watches a Job pod spawn and complete"
    - "A participant following new Step 12 (GitHub webhook setup) gets a smee.io channel, runs smee-setup.sh, configures a webhook on a personal GitHub repo, and sees forwarding events arrive at the local Hermes gateway"
    - "A participant following new Step 13 (GitHub agent comment back) opens a PR on the test repo and observes the Hermes agent post a review comment back via the built-in github_comment delivery type"
    - "A participant following new Step 14 (Telegram bot setup) creates a bot via @BotFather, exports TELEGRAM_BOT_TOKEN and TELEGRAM_ALLOWED_USERS, and starts the gateway with the Telegram adapter active"
    - "A participant following new Step 15 (Telegram /diagnose command) sends a slash command from the Telegram app and receives an agent reply in the same chat thread"
    - "A participant following new Step 16 (Telegram governance escalation) restarts the gateway with HERMES_LAB_GOVERNANCE=L4 + HERMES_LAB_TRACK=track-c and observes the per-context governance change applies to bot-triggered agent runs"
    - "Every new step includes a complete export block with the full Phase 7 + Phase 8 env var set: HERMES_LAB_MODE, HERMES_LAB_SCENARIO, HERMES_LAB_GOVERNANCE, HERMES_LAB_TRACK, MOCK_DATA_DIR, PATH, AND the Phase 8 NEW additions (GITHUB_TOKEN where TRIG-03 is exercised, TELEGRAM_BOT_TOKEN where TRIG-04 is exercised, SMEE_URL where smee.io is used)"
    - "TRIG-03 steps include a :::info Solo Learner callout with the hermes webhook test fallback using the bundled sample-pr-payload.json"
    - "TRIG-04 steps include a :::info Solo Learner callout explaining the @BotFather and @userinfobot one-time setup with time estimate"
    - "Both Module 12 lab mirrors are updated with identical step structure: course-site/docs/module-12-triggers/lab/LAB.mdx (MDX with admonitions) AND modules/module-12-triggers/LAB.md (plain markdown blockquotes)"
    - "Module 12 reading reference.mdx gets a new section with a 4-trigger comparison table covering Hermes cron, K8s CronJob, AlertManager webhook, GitHub webhook, and Telegram bot — when to use each, what state each requires, governance inheritance"
    - "Module 12 reading reference.mdx documents the three NEW Phase 8 env vars: GITHUB_TOKEN, TELEGRAM_BOT_TOKEN, SMEE_URL"
    - "Module 12 quiz adds 2-3 new questions per D-25: one on Hermes-cron-vs-K8s-CronJob tradeoff, one on AlertManager → webhook → agent flow, one on chat bot governance inheritance"
    - "The existing FREE EXPLORE PHASE and 3 challenges remain intact at the end of the lab, just shifted down to follow the new Step 16"
    - "All new lab text references the actual file paths shipped by Plans 08-01 and 08-02 — no placeholder paths, no aspirational filenames"
    - "All new lab text uses {alerts} (correct) and never {alerts[0]...} (broken array index syntax) per the prompt template constraint from RESEARCH"
  artifacts:
    - path: "course-site/docs/module-12-triggers/lab/LAB.mdx"
      provides: "Module 12 lab MDX with 8 new GUIDED steps (9-16) covering all 4 trigger types, plus updated Closing and Verification Checklist"
      contains: "Step 16: Telegram"
    - path: "modules/module-12-triggers/LAB.md"
      provides: "Module 12 lab source-of-truth markdown mirror with the same 8 new steps"
      contains: "Step 16: Telegram"
    - path: "course-site/docs/module-12-triggers/reading/reference.mdx"
      provides: "Module 12 reference reading with new 4-trigger comparison table and Phase 8 env var documentation"
      contains: "TELEGRAM_BOT_TOKEN"
    - path: "course-site/docs/module-12-triggers/quiz/QUIZ.mdx"
      provides: "Module 12 quiz with 2-3 new questions on triggers, governance inheritance, and tradeoffs"
      contains: "K8s CronJob"
  key_links:
    - from: "course-site/docs/module-12-triggers/lab/LAB.mdx Step 9 (AlertManager setup)"
      to: "infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml (from Plan 08-01)"
      via: "kubectl apply -f command in step body"
      pattern: "alertmanager/prometheus-rules.yaml"
    - from: "course-site/docs/module-12-triggers/lab/LAB.mdx Step 11 (K8s CronJob)"
      to: "infrastructure/scenarios/k8s/cronjob/Dockerfile and agent-health-check.yaml (from Plan 08-01)"
      via: "docker build + kubectl apply commands in step body"
      pattern: "scenarios/k8s/cronjob/"
    - from: "course-site/docs/module-12-triggers/lab/LAB.mdx Step 12 (GitHub webhook setup)"
      to: "infrastructure/scenarios/k8s/github-webhook/smee-setup.sh (from Plan 08-02)"
      via: "bash invocation in step body"
      pattern: "github-webhook/smee-setup.sh"
    - from: "course-site/docs/module-12-triggers/lab/LAB.mdx Step 13 (GitHub agent comment)"
      to: "infrastructure/scenarios/k8s/github-webhook/agent-prompt-template.txt (from Plan 08-02)"
      via: "$(cat ...) substitution in hermes webhook subscribe command"
      pattern: "agent-prompt-template.txt"
    - from: "course-site/docs/module-12-triggers/lab/LAB.mdx Step 14 (Telegram bot setup)"
      to: "infrastructure/scenarios/k8s/telegram-bot/README.md and slash-command-spec.md (from Plan 08-02)"
      via: "cross-reference in step body"
      pattern: "telegram-bot/"
    - from: "modules/module-12-triggers/LAB.md (mirror)"
      to: "course-site/docs/module-12-triggers/lab/LAB.mdx (canonical MDX)"
      via: "identical step structure, MDX admonitions become markdown blockquotes"
      pattern: "Step 16: Telegram"
---

<objective>
Extend Module 12 lab with 8 new GUIDED steps (9-16) covering all 4 Phase 8 trigger types, update the reading reference with a comparison table and Phase 8 env var docs, and add 2-3 new quiz questions. This is the participant-facing half of Phase 8 — without it, the infrastructure built in Plans 08-01 and 08-02 exists but is not teachable.

Purpose: Plans 08-01 and 08-02 produced 16 infrastructure files (8 per plan) but no participant-facing content. This plan turns that infrastructure into a guided walkthrough where participants observe AlertManager firing, build a K8s CronJob image, see GitHub webhook events arrive, and send Telegram slash commands — all on real services where feasible, with Solo Learner fallbacks for self-paced Udemy learners who can't acquire external service tokens.

This plan depends on Plans 08-01 and 08-02 because:
- New Step 9 references `infrastructure/helm/prometheus-lab-values.yaml` (from Plan 08-01)
- New Step 9 applies `infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml` (from Plan 08-01)
- New Step 11 builds and applies `infrastructure/scenarios/k8s/cronjob/Dockerfile` and `agent-health-check.yaml` (from Plan 08-01)
- New Step 12 runs `infrastructure/scenarios/k8s/github-webhook/smee-setup.sh` (from Plan 08-02)
- New Step 13 cats `infrastructure/scenarios/k8s/github-webhook/agent-prompt-template.txt` (from Plan 08-02)
- New Step 13's Solo Learner fallback uses `infrastructure/scenarios/k8s/github-webhook/sample-pr-payload.json` (from Plan 08-02)
- New Step 14 references `infrastructure/scenarios/k8s/telegram-bot/README.md` (from Plan 08-02)
- New Step 15 references `infrastructure/scenarios/k8s/telegram-bot/slash-command-spec.md` (from Plan 08-02)

Output:
- `course-site/docs/module-12-triggers/lab/LAB.mdx` — extended from 8 GUIDED steps to 16 GUIDED steps + intact FREE EXPLORE
- `modules/module-12-triggers/LAB.md` — same extension applied to the source-of-truth mirror
- `course-site/docs/module-12-triggers/reading/reference.mdx` — new 4-trigger comparison table section + Phase 8 env var docs
- `course-site/docs/module-12-triggers/quiz/QUIZ.mdx` — 2-3 new questions appended after existing questions
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-agent-triggers/08-CONTEXT.md
@.planning/phases/08-agent-triggers/08-RESEARCH.md
@.planning/phases/08-agent-triggers/08-01-SUMMARY.md
@.planning/phases/08-agent-triggers/08-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Current Module 12 lab structure (both files have identical step layout). -->

### Existing Module 12 lab (course-site/docs/module-12-triggers/lab/LAB.mdx, 685 lines)

**GUIDED PHASE — 50 minutes (current)**
- Step 1: Morning Startup Sequence (5 min) — line 29
- Step 2: Create a Daily Health Check Cron Job (10 min) — line 78
- Step 3: Trigger Manually and Verify Output (10 min) — line 157
- Step 4: Pause, Resume, and Status (5 min) — line 211
- Step 5: Start the Webhook Gateway (5 min) — line 264
- Step 6: Subscribe a Webhook for CloudWatch Alerts (10 min) — line 307
- Step 7: Test the Webhook with a Simulated Alert (10 min) — line 360
- Step 8: Slack — What This Looks Like in Production (5 min) — line 418

**FREE EXPLORE PHASE — 25 minutes** (line 487)
- Challenge 1 (Starter — 10 min): Cross-track cron job
- Challenge 2 (Intermediate — 15 min): Webhook for a different event type
- Challenge 3 (Advanced — 20 min): Combine cron + webhook on the same skill

**Closing** (line 617)
**Verification Checklist** (line 656)

### Insertion plan (per CONTEXT.md D-21)

Insert NEW steps 9-16 immediately after the existing Step 8 ends (around line 484-485, before the `## FREE EXPLORE PHASE — 25 minutes` separator at line 487).

Final ordering:
- Old Steps 1-8 stay as Steps 1-8 (UNTOUCHED — do not edit narrative or commands)
- NEW Step 9: AlertManager setup — install + configure (10 min)
- NEW Step 10: AlertManager fire + observe — apply crashloop2, watch alert fire (10 min)
- NEW Step 11: K8s CronJob comparison — build image, apply manifest (10 min)
- NEW Step 12: GitHub webhook setup with smee.io (10 min)
- NEW Step 13: GitHub agent comment back — full event-to-comment round trip (10 min)
- NEW Step 14: Telegram bot setup (10 min)
- NEW Step 15: Telegram /diagnose slash command (5 min)
- NEW Step 16: Telegram governance escalation (5 min)
- Old FREE EXPLORE PHASE stays — title bumped to acknowledge new total time
- Old Closing and Verification Checklist stay, BUT extended to include the new artifacts

Total guided phase grows from 50 min → ~120 min. Header at top of file should be updated:
- Duration: was "75 minutes (50 min guided + 25 min free explore)"
- New: "145 minutes (120 min guided + 25 min free explore)"

### MDX vs MD mirror differences

| MDX (course-site) | MD (modules/) |
|---|---|
| `:::info Solo Learner` admonition block | `> **Solo Learner:** ...` blockquote |
| `:::tip` admonition | `> **Tip:** ...` blockquote |
| `:::note Demo-only section` admonition | `> **Note (Demo-only):** ...` blockquote |
| `:::danger ...` admonition | `> **WARNING:** ...` blockquote |
| Frontmatter `---\nid: ...\ntitle: ...\n---` | No frontmatter |

Both files MUST have identical step structure, identical commands, identical expected outputs. Only the admonition syntax differs.

### Files Plan 08-01 and 08-02 produce (paths the new lab steps will reference)

From Plan 08-01:
- `infrastructure/helm/prometheus-lab-values.yaml` (modified — alertmanager.enabled: true)
- `infrastructure/kind/cluster-config.yaml` (modified — port 8644 added)
- `infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml`
- `infrastructure/scenarios/k8s/alertmanager/alertmanager-config.yaml`
- `infrastructure/scenarios/k8s/alertmanager/README.md`
- `infrastructure/scenarios/k8s/cronjob/Dockerfile`
- `infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml`
- `infrastructure/scenarios/k8s/cronjob/README.md`

From Plan 08-02:
- `infrastructure/scenarios/k8s/github-webhook/README.md`
- `infrastructure/scenarios/k8s/github-webhook/smee-setup.sh`
- `infrastructure/scenarios/k8s/github-webhook/sample-pr-payload.json`
- `infrastructure/scenarios/k8s/github-webhook/agent-prompt-template.txt`
- `infrastructure/scenarios/k8s/telegram-bot/README.md`
- `infrastructure/scenarios/k8s/telegram-bot/bot-config.example.yaml`
- `infrastructure/scenarios/k8s/telegram-bot/admin-allowlist.example.yaml`
- `infrastructure/scenarios/k8s/telegram-bot/slash-command-spec.md`

### Phase 8 env var additions (per CONTEXT.md D-26)

Every new lab step that exercises Phase 8 functionality MUST include the complete export block with the FULL set of env vars. The base set (from Phase 7 D-05):

```bash
export HERMES_LAB_MODE=live              # or mock
export HERMES_LAB_SCENARIO=crashloop2    # for TRIG-01 demo
export HERMES_LAB_GOVERNANCE=L2          # default
export HERMES_LAB_TRACK=track-c          # adjust per participant
export MOCK_DATA_DIR="$(pwd)/infrastructure/mock-data/kubernetes"
export PATH="$(pwd)/infrastructure/wrappers:$PATH"
```

PLUS Phase 8 NEW additions where used:
- TRIG-03 steps: `export GITHUB_TOKEN="ghp_..."` and `export SMEE_URL="https://smee.io/..."`
- TRIG-04 steps: `export TELEGRAM_BOT_TOKEN="123456:ABC..."` and `export TELEGRAM_ALLOWED_USERS="987654321"`

These follow the convention from Phase 7 D-05: every step shows the COMPLETE export block, not just the env var that changed. This is the user's explicit request — keep the full block visible for shell history continuity.

### Existing Module 12 reading reference.mdx structure (224 lines)

- Section 1: Hermes Cron Job Configuration
- Section 2: (continues with other reference content)
- ... (executor reads the file before deciding where to insert the new section)

Phase 8 adds: a new section (likely Section 6 or appended at the end) titled "Phase 8: 4 Real Trigger Types — Comparison" with a comparison table covering the 4 Phase 8 triggers + the 2 existing Module 12 triggers (Hermes cron, simulated webhook) for context. PLUS a new subsection documenting the 3 NEW env vars (GITHUB_TOKEN, TELEGRAM_BOT_TOKEN, SMEE_URL).

### Existing Module 12 quiz (201 lines, 8-10 existing questions)

Phase 8 adds 2-3 NEW questions appended to the end. Existing questions stay UNTOUCHED. Per D-25, the new questions cover:
- One on the Hermes-cron-vs-K8s-CronJob tradeoff (when to use each)
- One on the AlertManager → webhook → agent flow (which component does what)
- One on chat bot governance inheritance (per-process vs per-message)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Module 12 LAB.mdx and LAB.md mirrors with 8 new GUIDED steps for the 4 Phase 8 trigger types</name>

  <read_first>
    - course-site/docs/module-12-triggers/lab/LAB.mdx (FULL FILE — 685 lines — must understand existing step structure, callout patterns, expected output formatting, and the exact insertion point)
    - modules/module-12-triggers/LAB.md (FULL FILE — 657 lines — confirm both mirrors have the same step layout and identify the equivalent insertion point)
    - .planning/phases/08-agent-triggers/08-CONTEXT.md (D-01 extend pattern, D-21 step ordering, D-22 per-track variants, D-23 dual-mirror, D-26 env vars)
    - .planning/phases/08-agent-triggers/08-RESEARCH.md (Pattern 1-4 walkthroughs with exact commands, Pitfalls 1-7 to surface in lab text)
    - .planning/phases/08-agent-triggers/08-01-SUMMARY.md (confirms exact paths shipped by Plan 08-01)
    - .planning/phases/08-agent-triggers/08-02-SUMMARY.md (confirms exact paths shipped by Plan 08-02)
    - infrastructure/scenarios/k8s/alertmanager/README.md (already on disk after Plan 08-01)
    - infrastructure/scenarios/k8s/cronjob/README.md (already on disk after Plan 08-01)
    - infrastructure/scenarios/k8s/github-webhook/README.md (already on disk after Plan 08-02)
    - infrastructure/scenarios/k8s/telegram-bot/README.md (already on disk after Plan 08-02)
  </read_first>

  <files>
    - course-site/docs/module-12-triggers/lab/LAB.mdx (MODIFY — insert 8 new steps after Step 8)
    - modules/module-12-triggers/LAB.md (MODIFY — insert same 8 new steps after Step 8)
  </files>

  <action>
Extend BOTH Module 12 lab mirrors with 8 new GUIDED steps. Existing Steps 1-8 stay UNTOUCHED. Insert the new content immediately after Step 8 ends, before the `## FREE EXPLORE PHASE — 25 minutes` separator.

**Header update (BOTH files):**
At the top of the lab, change:
- `**Duration:** 75 minutes (50 min guided + 25 min free explore)` → `**Duration:** 145 minutes (120 min guided + 25 min free explore)`
- `**Outcome:** A working cron job that fires a daily health check + a webhook subscription that reacts to CloudWatch alerts in real time` → `**Outcome:** Working examples of all 5 trigger patterns: Hermes cron, simulated CloudWatch webhook, real AlertManager webhook on KIND, K8s CronJob, GitHub webhook via smee.io, and a Telegram bot — each agent invocation observed end-to-end`

Also bump the GUIDED PHASE header:
- `## GUIDED PHASE — 50 minutes` → `## GUIDED PHASE — 120 minutes`

**Insert these 8 NEW steps IN ORDER after existing Step 8:**

<!-- divider -->

### NEW Step 9: AlertManager — Real Prometheus Stack Setup (10 min)

**MDX version:**
```mdx
<!-- divider -->

## Step 9: AlertManager — Real Prometheus Stack Setup (10 min)

You have used `hermes webhook test` to fire simulated webhooks (Step 7). Now you wire a REAL alert source: the Prometheus + AlertManager stack on your KIND cluster, firing on a real broken pod from Phase 6.

This is the moment Hermes stops being a chat agent and starts being an incident-response agent — alerts arrive without you typing anything.

:::info Solo Learner
This step requires a running KIND cluster with `kube-prometheus-stack` already installed (from Phase 1 setup). If you skipped Phase 1's KIND setup, jump to the "Skipping AlertManager" callout at the bottom of this step — you can still complete TRIG-02, TRIG-03, and TRIG-04 without TRIG-01.
:::

### Set environment variables

```bash
export HERMES_LAB_MODE=live
export HERMES_LAB_SCENARIO=crashloop2
export HERMES_LAB_GOVERNANCE=L2
export HERMES_LAB_TRACK=track-c
export MOCK_DATA_DIR="$(pwd)/infrastructure/mock-data/kubernetes"
export PATH="$(pwd)/infrastructure/wrappers:$PATH"
```

### Enable AlertManager in the helm release

The Phase 1 helm values disabled AlertManager to save resources. Phase 8 flips it back on.

```bash
# Verify the helm values now have alertmanager.enabled: true
yq '.alertmanager.enabled' infrastructure/helm/prometheus-lab-values.yaml
# Expected output: true

# Apply the updated values
helm upgrade --install kube-prometheus prometheus-community/kube-prometheus-stack \
  --namespace monitoring --create-namespace \
  -f infrastructure/helm/prometheus-lab-values.yaml

# Wait for AlertManager pods to reach Ready
kubectl wait --for=condition=Ready pod \
  -l app.kubernetes.io/name=alertmanager \
  -n monitoring --timeout=120s
```

Expected output:
```
pod/alertmanager-kube-prometheus-alertmanager-0 condition met
```

### Apply the PrometheusRule

```bash
kubectl apply -f infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml
```

Expected output:
```
prometheusrule.monitoring.coreos.com/hermes-lab-rules created
```

### Verify the rule loaded

```bash
kubectl get prometheusrule -n monitoring hermes-lab-rules -o yaml | yq '.spec.groups[0].rules[0].alert'
```

Expected output:
```
PodCrashLooping
```

Open the Prometheus UI at http://localhost:30091 → Status → Rules. You should see `hermes-lab.k8s-crashloop` group with the `PodCrashLooping` rule listed and state "inactive" (no broken pods YET).

:::tip Why `release: kube-prometheus` matters
The PrometheusRule manifest in `infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml` includes a `labels: release: kube-prometheus` field. The kube-prometheus-stack Helm chart configures Prometheus to ONLY load rules with this label. Without it, your rule would be silently ignored — `kubectl get prometheusrule` would show it, but the Prometheus UI Rules page would not.
:::

### Skipping AlertManager (no KIND)

:::info Solo Learner
If you don't have a KIND cluster running, skip to Step 11. TRIG-02 (K8s CronJob) also requires KIND, so you'll skip Step 11 as well — go straight to Step 12 (GitHub webhook). All four trigger types are independent: missing one doesn't block the others.
:::

```

**MD version (same content with blockquote callouts):**
```markdown
<!-- divider -->

## Step 9: AlertManager — Real Prometheus Stack Setup (10 min)

You have used `hermes webhook test` to fire simulated webhooks (Step 7). Now you wire a REAL alert source: the Prometheus + AlertManager stack on your KIND cluster, firing on a real broken pod from Phase 6.

This is the moment Hermes stops being a chat agent and starts being an incident-response agent — alerts arrive without you typing anything.

> **Solo Learner:** This step requires a running KIND cluster with `kube-prometheus-stack` already installed (from Phase 1 setup). If you skipped Phase 1's KIND setup, jump to the "Skipping AlertManager" callout at the bottom of this step — you can still complete TRIG-02, TRIG-03, and TRIG-04 without TRIG-01.

### Set environment variables

```bash
export HERMES_LAB_MODE=live
export HERMES_LAB_SCENARIO=crashloop2
export HERMES_LAB_GOVERNANCE=L2
export HERMES_LAB_TRACK=track-c
export MOCK_DATA_DIR="$(pwd)/infrastructure/mock-data/kubernetes"
export PATH="$(pwd)/infrastructure/wrappers:$PATH"
```

(... same body as MDX version, just with `>` blockquotes instead of `:::info` admonitions)
```

<!-- divider -->

### NEW Step 10: AlertManager — Fire and Observe (10 min)

**Step body content (use the same MDX/MD callout split as Step 9):**

This step starts the Hermes webhook gateway (foreground), subscribes the alertmanager webhook with the correct prompt template (uses `{alerts}` — NOT `{alerts[0].labels.pod}`), applies the Phase 6 crashloop2 scenario, and watches the alert fire end-to-end.

Key commands the step body MUST include verbatim:

```bash
# Terminal 1: Start the gateway in the foreground (so participants see live POSTs)
hermes gateway run

# Terminal 2: Subscribe the alertmanager webhook
hermes webhook subscribe alertmanager \
  --events "alertmanager-alert" \
  --prompt "AlertManager PodCrashLooping alert fired. Details: {alerts}. Load the sre-k8s-pod-health skill and diagnose the affected pod in the namespace shown in the alert labels." \
  --skill "sre-k8s-pod-health" \
  --deliver local

# Verify subscription
hermes webhook list

# Terminal 3: Apply the Phase 6 crashloop2 broken pod scenario
kubectl apply -f infrastructure/scenarios/k8s/02-crashloop-backoff.yaml

# Wait ~2 min for the alert to fire (the rule requires increase > 2 over 2 min)
watch kubectl get pods -n k8s-trouble-crashloop
```

Expected timeline:
- t=0s: pod applied, status `ContainerCreating`
- t=10s: pod status `CrashLoopBackOff` with restartCount=1
- t=60s: restartCount=4-6
- t=90s: PromQL `increase()` exceeds 2 over the last 2-min window
- t=120s: AlertManager dispatches; gateway terminal shows "Received alertmanager-alert event"
- t=125s: Hermes spawns agent run; sre-k8s-pod-health skill loads; diagnosis appears in gateway terminal

The step MUST include a callout block explaining the prompt template constraint:

> **CRITICAL:** Notice the `{alerts}` placeholder in the subscribe command above — NOT `{alerts[0].labels.pod}`. The Hermes prompt template only supports dot-notation access to dict keys, NOT array index access. The agent receives the full `alerts[]` JSON array as a string and parses it to find the pod and namespace. If you accidentally use `{alerts[0].labels.pod}`, it will render as a literal string "alerts[0].labels.pod" — the agent will see that text instead of the pod name.

The step ends with cleanup: `kubectl delete -f infrastructure/scenarios/k8s/02-crashloop-backoff.yaml` and `hermes webhook unsubscribe alertmanager`.

**Include the same complete export block as Step 9 (with HERMES_LAB_MODE, SCENARIO, GOVERNANCE, TRACK, MOCK_DATA_DIR, PATH).**

<!-- divider -->

### NEW Step 11: K8s CronJob — Same Agent, Different Trigger Mechanism (10 min)

This step demonstrates that the SAME agent can run on either Hermes cron (Steps 2-4 you already built) OR a K8s CronJob — and shows when each is the right answer per CONTEXT.md D-10.

The step builds the minimal hermes-agent container image, loads it into KIND, creates the API key secret, and applies the K8s CronJob manifest with per-track selector.

Key commands the step body MUST include verbatim:

```bash
# 1. Build the minimal hermes-agent image
docker build -t hermes-lab:cronjob infrastructure/scenarios/k8s/cronjob/

# Verify the image exists
docker images | grep hermes-lab

# 2. Load the image into KIND (required because the manifest uses imagePullPolicy: IfNotPresent)
kind load docker-image hermes-lab:cronjob --name lab

# 3. Create the API key secret (NEVER commit this — it lives only in your local KIND cluster)
kubectl create secret generic hermes-secrets \
  --from-literal=anthropic-api-key="$ANTHROPIC_API_KEY"

# Verify
kubectl get secret hermes-secrets

# 4. Apply ONE track's CronJob (replace track-c with your track)
kubectl apply -f infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml -l track=track-c

# 5. Watch jobs spawn (the schedule is */5 * * * * — wait up to 5 min for the first run)
watch kubectl get jobs,pods

# 6. View logs from the first completed job
kubectl logs -l job-name=$(kubectl get jobs -o jsonpath='{.items[-1].metadata.name}')
```

The step body MUST include the use-this-when callout from CONTEXT.md D-10 in a tip block:

> **Use Hermes cron when:** the agent benefits from gateway-shared state (skills, audit trail, conversation history), you want one-stop CLI management, you're iterating fast.
>
> **Use K8s CronJob when:** stateless one-shot diagnostics, GitOps schedule-in-git, K8s-native observability, multi-tenant resource quotas.
>
> **Real-world honest stance:** most agent work uses Hermes cron because state matters. K8s CronJob shines for fire-and-forget diagnostics deployed alongside other K8s primitives.

Per-track variant note: the step instructions include `kubectl apply -l track=track-c` for Track C. A Track A or Track B participant uses `track=track-a` or `track=track-b`. The lab text MUST mention all three tracks per D-22.

The step ends with cleanup: `kubectl delete -f infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml -l track=track-c`.

**Include the same complete export block.**

<!-- divider -->

### NEW Step 12: GitHub Webhook — smee.io Setup (10 min)

This step gets a smee.io channel URL, runs the smee setup script from Plan 08-02, configures a webhook on a personal GitHub repo, and confirms forwarding.

Key commands the step body MUST include:

```bash
# 1. Get a smee.io channel URL
# Visit https://smee.io/ in your browser
# Click "Start a new channel"
# Copy the URL — looks like https://smee.io/abc123XYZ
export SMEE_URL="https://smee.io/abc123XYZ"   # Replace with your channel

# 2. Get a GitHub PAT with `repo` scope (or fine-grained "Pull requests: Read and Write")
# Visit https://github.com/settings/tokens → Generate new token (classic)
export GITHUB_TOKEN="ghp_..."

# Configure gh CLI (if not already authenticated)
gh auth login --with-token <<< "$GITHUB_TOKEN"
gh auth status   # Should show "Logged in to github.com as <you>"

# 3. Start the Hermes gateway (background OR new terminal)
hermes gateway run

# 4. In another terminal, run the smee setup script
./infrastructure/scenarios/k8s/github-webhook/smee-setup.sh

# Expected output:
#   ================================================================
#    smee.io → Hermes webhook gateway forwarder
#   ================================================================
#     Source:  https://smee.io/abc123XYZ
#     Target:  http://localhost:8644/webhooks/github
#     Client:  smee-client@5.0.0
#   ================================================================

# 5. Add the smee.io URL as a webhook on a test GitHub repo
# (Use a personal sandbox repo — never on a production repo for the lab)
# Repo Settings → Webhooks → Add webhook
#   Payload URL:    $SMEE_URL
#   Content type:   application/json
#   Events:         "Let me select individual events" → check "Pull requests"
#   Active:         checked
# → Add webhook
```

The step body MUST include:
- The `:::info Solo Learner` admonition (MDX) / blockquote (MD) explaining that participants without a personal GitHub repo can skip directly to Step 13's Solo Learner fallback section.
- A pitfalls callout listing the smee URL mismatch (the route name must match the subscription name) per RESEARCH Pitfall 5.

**Include the complete export block PLUS the new TRIG-03 env vars:**
```bash
export HERMES_LAB_MODE=live
export HERMES_LAB_SCENARIO=crashloop2
export HERMES_LAB_GOVERNANCE=L2
export HERMES_LAB_TRACK=track-c
export MOCK_DATA_DIR="$(pwd)/infrastructure/mock-data/kubernetes"
export PATH="$(pwd)/infrastructure/wrappers:$PATH"
# Phase 8 NEW (TRIG-03):
export GITHUB_TOKEN="ghp_..."
export SMEE_URL="https://smee.io/your-channel-id"
```

<!-- divider -->

### NEW Step 13: GitHub Agent Comment Back — Full Round-Trip (10 min)

This step subscribes the GitHub webhook with `--deliver github_comment`, opens a PR on the test repo, and watches the agent post a review comment back.

Key commands the step body MUST include:

```bash
# Subscribe the github webhook with the agent prompt template
hermes webhook subscribe github \
  --events "pull_request" \
  --prompt "$(cat infrastructure/scenarios/k8s/github-webhook/agent-prompt-template.txt)" \
  --deliver github_comment \
  --deliver-chat-id "{repository.full_name}:{pull_request.number}"

# Verify
hermes webhook list

# Now open a PR on your test repo (or push a commit to a branch with an open PR)
# Watch the smee terminal — you should see "Forwarding event to localhost:8644/webhooks/github"
# Watch the gateway terminal — you should see "Received github webhook event"
# Within ~10s, the agent runs and posts a comment back to the PR

# Verify the comment landed
gh pr view <PR_NUMBER> --repo <OWNER>/<REPO> --json comments
```

The step body MUST include:

> **The github_comment delivery type is built into Hermes** — `gateway/platforms/webhook.py` lines 525-558 handle the wiring. Internally it calls `gh pr comment {pr_number} --repo {repo} --body "{content}"`. You wrote zero HTTP code — the only requirement is `gh` CLI installed and authenticated.

The step body MUST include the Solo Learner fallback section using the bundled sample payload:

> **Solo Learner — no GitHub repo, no smee.io:**
>
> ```bash
> # Subscribe with --deliver local instead of --deliver github_comment
> hermes webhook subscribe github \
>   --events "pull_request" \
>   --prompt "$(cat infrastructure/scenarios/k8s/github-webhook/agent-prompt-template.txt)" \
>   --deliver local
>
> # Inject the bundled sample payload
> hermes webhook test github \
>   --payload @infrastructure/scenarios/k8s/github-webhook/sample-pr-payload.json
> ```
>
> The agent runs identically; the review goes to your terminal instead of back to GitHub. The sample payload is a valid GitHub PR webhook structure with all the standard fields the prompt template references (`pull_request.number`, `pull_request.title`, `repository.full_name`, etc.).

End with cleanup: `hermes webhook unsubscribe github`.

**Include the complete export block including GITHUB_TOKEN and SMEE_URL.**

<!-- divider -->

### NEW Step 14: Telegram Bot Setup — @BotFather to Gateway (10 min)

This step walks through the @BotFather flow, getting a user ID from @userinfobot, configuring Hermes via env vars, and starting the gateway with the Telegram adapter active.

The step body MUST include the @BotFather walkthrough verbatim:

> 1. Open Telegram (mobile app or https://web.telegram.org)
> 2. Search for **@BotFather** (verified blue checkmark)
> 3. Send `/newbot`
> 4. Choose a display name (e.g., "Hermes Lab Bot")
> 5. Choose a username — must end in `bot` (e.g., `hermes_lab_yourname_bot`)
> 6. Copy the bot token from BotFather's response (looks like `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`)
> 7. **Treat this token like a password** — anyone with it can impersonate your bot

Then:

> 1. Search for **@userinfobot** in Telegram
> 2. Send `/start`
> 3. Copy your numeric user ID (e.g., `987654321`)

Key commands the step body MUST include:

```bash
# Stop the gateway if it's already running (Telegram polling lock)
hermes gateway stop
# Wait 30 seconds for the polling lock to release (per RESEARCH Pitfall 4)
sleep 30

# Set the new env vars
export TELEGRAM_BOT_TOKEN="123456:ABC..."        # From @BotFather
export TELEGRAM_ALLOWED_USERS="987654321"        # From @userinfobot

# Verify the python-telegram-bot package is installed (from [messaging] extra)
python3 -c "from telegram import Bot; print('telegram OK')"

# Start the gateway
hermes gateway run
# Look for: "Telegram adapter started, polling..." in the output
```

:::info Solo Learner
The @BotFather setup takes ~2 minutes total. You only do it once — your bot stays registered with Telegram permanently (or until you `/deletebot` it). No payment, no admin approval, no workspace required.
:::

The step body MUST include the polling conflict pitfall callout per RESEARCH Pitfall 4.

**Include the complete export block PLUS the Phase 8 TRIG-04 additions:**
```bash
export HERMES_LAB_MODE=live
export HERMES_LAB_SCENARIO=crashloop2
export HERMES_LAB_GOVERNANCE=L2
export HERMES_LAB_TRACK=track-c
export MOCK_DATA_DIR="$(pwd)/infrastructure/mock-data/kubernetes"
export PATH="$(pwd)/infrastructure/wrappers:$PATH"
# Phase 8 NEW (TRIG-04):
export TELEGRAM_BOT_TOKEN="123456:ABC..."
export TELEGRAM_ALLOWED_USERS="987654321"
```

<!-- divider -->

### NEW Step 15: Telegram /diagnose Command — Slash Commands From Your Phone (5 min)

This is the participant-experience moment. They've been typing into a terminal for 14 steps. Now they pick up their phone, send a slash command, and watch the agent respond in Telegram.

Key actions the step body MUST describe:

1. Open Telegram on phone or web. Search for the bot's username (the one chosen with @BotFather in Step 14).
2. Open the chat. Send: `/help`
3. Expected: bot replies with the command list (driven by the agent's loaded SOUL.md and skills).
4. Send: `/diagnose <track-appropriate-argument>` (per-track examples below)

**Per-track examples (per CONTEXT.md D-22):**
- Track A: `/diagnose users` (table name) → bot loads `sre-dba-rds-slow-query`
- Track B: `/diagnose web-tier` (EC2 service tier) → bot loads `sre-ec2-health-check`
- Track C: `/diagnose k8s-trouble-crashloop` (namespace name) → bot loads `sre-k8s-pod-health`

The step body MUST include reference to the slash command spec:

> See `infrastructure/scenarios/k8s/telegram-bot/slash-command-spec.md` for the full three-command spec (`/diagnose`, `/status`, `/help`) with per-track examples.

The step body MUST include this callout:

> **The Hermes Telegram adapter handles ALL slash commands automatically.** `gateway/platforms/telegram.py` line 1549 (`_handle_command`) passes the entire message text — including the `/` and arguments — through to the agent as the prompt. There's no per-command Python registration. You can experiment: send `/whatever` and the agent will see exactly that text and respond.

**Include the complete export block (same as Step 14).**

<!-- divider -->

### NEW Step 16: Telegram Governance Escalation — Per-Process Override (5 min)

This step demonstrates that bot governance is per-PROCESS (set at gateway start time and inherited by every command run during that gateway's lifetime), per CONTEXT.md D-19.

Key commands the step body MUST include:

```bash
# 1. Stop the current gateway (releases Telegram polling lock)
hermes gateway stop
sleep 30

# 2. Restart with HERMES_LAB_GOVERNANCE=L4 — full export block including L4
export HERMES_LAB_MODE=live
export HERMES_LAB_SCENARIO=crashloop2
export HERMES_LAB_GOVERNANCE=L4
export HERMES_LAB_TRACK=track-c
export MOCK_DATA_DIR="$(pwd)/infrastructure/mock-data/kubernetes"
export PATH="$(pwd)/infrastructure/wrappers:$PATH"
export TELEGRAM_BOT_TOKEN="123456:ABC..."
export TELEGRAM_ALLOWED_USERS="987654321"

hermes gateway run

# 3. From Telegram, send a write-action command (Track C example)
# In Telegram: /diagnose --apply k8s-trouble-crashloop
# Expected: at L4, the agent CAN attempt kubectl apply (Phase 7 wrapper allows it at L4)
# At L2, the same command would have been blocked by GOVERNANCE REJECTED banner

# 4. Verify the governance level took effect by examining session logs
hermes sessions list
hermes sessions show <session-id> | grep -i governance
```

The step body MUST include this callout:

> **No per-message governance escalation.** Unlike a CLI agent where each invocation can take a `--governance` flag, the Telegram bot inherits governance from the gateway PROCESS. To change levels, you restart the gateway. The combination of `TELEGRAM_ALLOWED_USERS` (who can talk to the bot at all) plus `HERMES_LAB_GOVERNANCE` (what they can do) is the per-context governance model from Phase 7 D-19.

The step body MUST cross-reference the admin allowlist file:

> See `infrastructure/scenarios/k8s/telegram-bot/admin-allowlist.example.yaml` for the team-wide admin allowlist pattern. The actual restriction is enforced by the `TELEGRAM_ALLOWED_USERS` env var; the YAML file documents WHO each ID belongs to so the list doesn't drift over time.

End with cleanup: `hermes gateway stop` and a note that participants can restart at L2 with `HERMES_LAB_GOVERNANCE=L2` for ongoing lab work.

<!-- divider -->

**Closing section update (BOTH files):**

Update the existing "Closing" section (line 617 of LAB.mdx) to add to the "What you built in this lab" bullet list:

- A real Prometheus + AlertManager pipeline on KIND firing on a Phase 6 broken pod, with the agent receiving the alert and diagnosing without manual invocation
- A K8s CronJob running a containerized Hermes agent on a schedule, with explicit "use this when…" framing for Hermes cron vs K8s CronJob
- A GitHub webhook via smee.io routing real PR events to Hermes, with the agent posting review comments back via the built-in `github_comment` delivery type
- A Telegram bot you can poke from your phone, with three slash commands and per-process governance inheritance from Phase 7

Also extend "Key commands reference" to include the Phase 8 commands:

```bash
# Phase 8 trigger commands
hermes webhook subscribe alertmanager --events "alertmanager-alert" --prompt "..." --skill "..." --deliver local
hermes webhook subscribe github --events "pull_request" --prompt "..." --deliver github_comment --deliver-chat-id "{repository.full_name}:{pull_request.number}"
kubectl apply -f infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml -l track=track-c
./infrastructure/scenarios/k8s/github-webhook/smee-setup.sh
hermes gateway run    # With TELEGRAM_BOT_TOKEN set, activates Telegram adapter
```

**Verification Checklist update (BOTH files):**

Append these new check items to the existing checklist (line 656 area):

```bash
# Phase 8 checks
# 7. AlertManager is enabled and reachable
kubectl get pods -n monitoring -l app.kubernetes.io/name=alertmanager
# Expected: alertmanager-kube-prometheus-alertmanager-0  Running

# 8. PrometheusRule loaded
kubectl get prometheusrule -n monitoring hermes-lab-rules
# Expected: row exists

# 9. K8s CronJob image built and loaded
docker images hermes-lab:cronjob
# Expected: REPOSITORY: hermes-lab, TAG: cronjob

# 10. K8s CronJob registered (after Step 11)
kubectl get cronjob | grep hermes-track
# Expected: hermes-track-{a,b,c}-health  */5 * * * *

# 11. smee-setup.sh is executable
test -x infrastructure/scenarios/k8s/github-webhook/smee-setup.sh && echo OK

# 12. Sample GitHub PR payload is valid JSON
jq . infrastructure/scenarios/k8s/github-webhook/sample-pr-payload.json > /dev/null && echo OK

# 13. Telegram adapter prerequisites
python3 -c "from telegram import Bot; print('telegram OK')"
# Expected: telegram OK
```

**FREE EXPLORE PHASE update:**

Update the section header from `## FREE EXPLORE PHASE — 25 minutes` to leave the time as-is (the extra 70 min in guided phase doesn't change free explore). The 3 existing challenges stay UNTOUCHED — they remain challenges 1, 2, 3.

**Now apply ALL of the above to BOTH files:**

1. `course-site/docs/module-12-triggers/lab/LAB.mdx` — use `:::info`, `:::tip`, `:::note`, `:::danger`, `:::warning` admonitions
2. `modules/module-12-triggers/LAB.md` — use `>` blockquotes with prefixes like `> **Solo Learner:**`, `> **Tip:**`, `> **Note:**`, `> **WARNING:**`

Both mirrors MUST end up structurally identical: same step numbers, same commands, same expected outputs, same callout content. Only the admonition syntax differs.

**Anti-pattern reminders (do NOT do these):**
- Do NOT touch existing Steps 1-8 (narrative or commands)
- Do NOT remove or renumber existing Challenges 1-3
- Do NOT use `{alerts[0].labels.pod}` array index syntax anywhere — always use `{alerts}`
- Do NOT skip the complete export block in any new step (per D-05/D-26 — full block every step)
- Do NOT inline the contents of the infrastructure files — reference them via `cat` or path
- Do NOT add a Slack hands-on step — Slack remains demo-only per CONTEXT.md D-17 (existing Step 8 is the only Slack content)
  </action>

  <acceptance_criteria>
    - Both lab files exist and have grown in line count: `wc -l course-site/docs/module-12-triggers/lab/LAB.mdx` shows >900 lines (was 685, +~250 for 8 new steps)
    - `wc -l modules/module-12-triggers/LAB.md` shows >870 lines (was 657, similar growth)
    - All 8 new step headings present in MDX: `for n in 9 10 11 12 13 14 15 16; do grep -q "^## Step $n:" course-site/docs/module-12-triggers/lab/LAB.mdx; done`
    - All 8 new step headings present in MD mirror: same loop on `modules/module-12-triggers/LAB.md`
    - Existing Steps 1-8 still present in both files: `for n in 1 2 3 4 5 6 7 8; do grep -q "^## Step $n:" course-site/docs/module-12-triggers/lab/LAB.mdx; done`
    - Existing Challenges 1-3 still present: `grep -c '^## Challenge' course-site/docs/module-12-triggers/lab/LAB.mdx` returns 3
    - Header time updated: `grep -q '120 min guided' course-site/docs/module-12-triggers/lab/LAB.mdx`
    - Header time updated in mirror: `grep -q '120 min guided' modules/module-12-triggers/LAB.md`
    - GUIDED PHASE header bumped: `grep -q 'GUIDED PHASE — 120 minutes' course-site/docs/module-12-triggers/lab/LAB.mdx`
    - References to Plan 08-01 artifacts present: `grep -q 'infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml' course-site/docs/module-12-triggers/lab/LAB.mdx`
    - `grep -q 'infrastructure/scenarios/k8s/cronjob/Dockerfile' course-site/docs/module-12-triggers/lab/LAB.mdx`
    - `grep -q 'infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml' course-site/docs/module-12-triggers/lab/LAB.mdx`
    - References to Plan 08-02 artifacts present: `grep -q 'infrastructure/scenarios/k8s/github-webhook/smee-setup.sh' course-site/docs/module-12-triggers/lab/LAB.mdx`
    - `grep -q 'infrastructure/scenarios/k8s/github-webhook/sample-pr-payload.json' course-site/docs/module-12-triggers/lab/LAB.mdx`
    - `grep -q 'infrastructure/scenarios/k8s/github-webhook/agent-prompt-template.txt' course-site/docs/module-12-triggers/lab/LAB.mdx`
    - `grep -q 'infrastructure/scenarios/k8s/telegram-bot/' course-site/docs/module-12-triggers/lab/LAB.mdx`
    - Phase 8 env vars introduced: `grep -q 'GITHUB_TOKEN' course-site/docs/module-12-triggers/lab/LAB.mdx`
    - `grep -q 'TELEGRAM_BOT_TOKEN' course-site/docs/module-12-triggers/lab/LAB.mdx`
    - `grep -q 'TELEGRAM_ALLOWED_USERS' course-site/docs/module-12-triggers/lab/LAB.mdx`
    - `grep -q 'SMEE_URL' course-site/docs/module-12-triggers/lab/LAB.mdx`
    - Same env vars in mirror: `grep -q 'GITHUB_TOKEN' modules/module-12-triggers/LAB.md && grep -q 'TELEGRAM_BOT_TOKEN' modules/module-12-triggers/LAB.md && grep -q 'SMEE_URL' modules/module-12-triggers/LAB.md`
    - Solo Learner callouts present in both files: `grep -q 'Solo Learner' course-site/docs/module-12-triggers/lab/LAB.mdx && grep -q 'Solo Learner' modules/module-12-triggers/LAB.md`
    - Per-track examples present: `grep -q 'Track A' course-site/docs/module-12-triggers/lab/LAB.mdx && grep -q 'Track B' course-site/docs/module-12-triggers/lab/LAB.mdx && grep -q 'Track C' course-site/docs/module-12-triggers/lab/LAB.mdx`
    - Critical anti-pattern absent: `! grep -F '{alerts[0]' course-site/docs/module-12-triggers/lab/LAB.mdx`
    - `! grep -F '{alerts[0]' modules/module-12-triggers/LAB.md`
    - Correct prompt template syntax present: `grep -q '{alerts}' course-site/docs/module-12-triggers/lab/LAB.mdx`
    - github_comment delivery referenced: `grep -q 'github_comment' course-site/docs/module-12-triggers/lab/LAB.mdx`
    - Use-this-when callout from D-10 present: `grep -q 'Use Hermes cron when' course-site/docs/module-12-triggers/lab/LAB.mdx && grep -q 'Use K8s CronJob when' course-site/docs/module-12-triggers/lab/LAB.mdx`
    - @BotFather walkthrough present: `grep -q '@BotFather' course-site/docs/module-12-triggers/lab/LAB.mdx`
    - @userinfobot referenced: `grep -q '@userinfobot' course-site/docs/module-12-triggers/lab/LAB.mdx`
    - Polling conflict pitfall surfaced: `grep -qi 'polling.conflict\|polling lock' course-site/docs/module-12-triggers/lab/LAB.mdx`
    - PrometheusRule label selector pitfall surfaced: `grep -q 'release: kube-prometheus' course-site/docs/module-12-triggers/lab/LAB.mdx`
    - imagePullPolicy pitfall surfaced: `grep -q 'imagePullPolicy: IfNotPresent\|kind load' course-site/docs/module-12-triggers/lab/LAB.mdx`
    - Step ordering matches D-21: `grep -E '^## Step (9|10|11|12|13|14|15|16):' course-site/docs/module-12-triggers/lab/LAB.mdx | head -8` shows the steps in 9,10,11,12,13,14,15,16 order
    - FREE EXPLORE PHASE still present after Step 16: `grep -A1 '^## Step 16:' course-site/docs/module-12-triggers/lab/LAB.mdx | head -50` followed by FREE EXPLORE later in file
    - Number of Solo Learner callouts: `grep -c 'Solo Learner' course-site/docs/module-12-triggers/lab/LAB.mdx` returns >= 4 (at least one per trigger type that has external dependency)
  </acceptance_criteria>

  <verify>
    <automated>for n in 9 10 11 12 13 14 15 16; do grep -q "^## Step $n:" course-site/docs/module-12-triggers/lab/LAB.mdx || { echo "MISSING Step $n in LAB.mdx"; exit 1; }; done && for n in 9 10 11 12 13 14 15 16; do grep -q "^## Step $n:" modules/module-12-triggers/LAB.md || { echo "MISSING Step $n in LAB.md"; exit 1; }; done && for n in 1 2 3 4 5 6 7 8; do grep -q "^## Step $n:" course-site/docs/module-12-triggers/lab/LAB.mdx || { echo "REGRESSION: lost Step $n"; exit 1; }; done && [ "$(grep -c '^## Challenge' course-site/docs/module-12-triggers/lab/LAB.mdx)" = "3" ] && grep -q '120 min guided' course-site/docs/module-12-triggers/lab/LAB.mdx && grep -q '120 min guided' modules/module-12-triggers/LAB.md && grep -q 'GITHUB_TOKEN' course-site/docs/module-12-triggers/lab/LAB.mdx && grep -q 'TELEGRAM_BOT_TOKEN' course-site/docs/module-12-triggers/lab/LAB.mdx && grep -q 'SMEE_URL' course-site/docs/module-12-triggers/lab/LAB.mdx && ! grep -F '{alerts[0]' course-site/docs/module-12-triggers/lab/LAB.mdx && ! grep -F '{alerts[0]' modules/module-12-triggers/LAB.md && grep -q '{alerts}' course-site/docs/module-12-triggers/lab/LAB.mdx && grep -q 'Use Hermes cron when' course-site/docs/module-12-triggers/lab/LAB.mdx && grep -q 'github_comment' course-site/docs/module-12-triggers/lab/LAB.mdx && grep -q '@BotFather' course-site/docs/module-12-triggers/lab/LAB.mdx</automated>
  </verify>

  <done>
    Both Module 12 lab mirrors have grown by 8 GUIDED steps (Steps 9-16). Existing Steps 1-8 untouched. FREE EXPLORE Challenges 1-3 untouched. New steps reference real artifact paths from Plans 08-01 and 08-02. Every new step includes a complete export block with all 6 base env vars (Phase 7 set) plus the appropriate Phase 8 NEW additions (GITHUB_TOKEN/SMEE_URL for TRIG-03 steps, TELEGRAM_BOT_TOKEN/TELEGRAM_ALLOWED_USERS for TRIG-04 steps). Solo Learner callouts present for TRIG-03 and TRIG-04. Per-track variants documented per D-22. Zero `{alerts[0]...}` anti-patterns. Use-this-when callout for Hermes cron vs K8s CronJob present per D-10. github_comment built-in delivery referenced. @BotFather walkthrough present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Module 12 reading reference.mdx with 4-trigger comparison table and Phase 8 env var docs; add 2-3 new quiz questions to QUIZ.mdx</name>

  <read_first>
    - course-site/docs/module-12-triggers/reading/reference.mdx (FULL FILE — 224 lines — to identify the right section to insert into and to follow the existing structural pattern)
    - course-site/docs/module-12-triggers/quiz/QUIZ.mdx (FULL FILE — 201 lines — to follow the existing question/answer/details pattern and match question numbering)
    - .planning/phases/08-agent-triggers/08-CONTEXT.md (D-24 reading update light-touch, D-25 quiz update, D-26 env vars)
    - .planning/phases/08-agent-triggers/08-RESEARCH.md (Pattern 1-4 for the comparison table content)
    - course-site/docs/module-12-triggers/lab/LAB.mdx (just-modified by Task 1 — confirm step references match what Task 2 will mention)
  </read_first>

  <files>
    - course-site/docs/module-12-triggers/reading/reference.mdx (MODIFY — append new section + update env var section if it exists)
    - course-site/docs/module-12-triggers/quiz/QUIZ.mdx (MODIFY — append 2-3 new questions)
  </files>

  <action>
**Part A — Update reference.mdx**

Read the existing `course-site/docs/module-12-triggers/reading/reference.mdx` (224 lines) to understand the existing section numbering and structure. The file currently has sections like "1. Hermes Cron Job Configuration", "2. Webhook Setup", "3. Slack Integration" etc. (executor verifies the actual headings during read).

**Append a NEW section** at the end of the file (or before the existing closing section, executor's call) titled "5. Phase 8: Real Trigger Types — Comparison" (adjust the number to be the next sequential section number after reading the file). Use this exact content:

```mdx
<!-- divider -->

## 5. Phase 8: Real Trigger Types — Comparison

Module 12's existing content (Sections 1-4 above) covers Hermes-native cron and webhook patterns with simulated payloads. Phase 8 adds four REAL trigger sources you wire to live infrastructure:

| Trigger Type | Source | When To Use | State Required | Governance Inheritance |
|---|---|---|---|---|
| **Hermes cron** (Module 12 Steps 2-4) | Internal scheduler | Most agent work — gateway-shared state, fast iteration, audit trail context | Gateway running | `HERMES_LAB_GOVERNANCE` from gateway env |
| **Hermes webhook test** (Module 12 Step 7) | `hermes webhook test` CLI | Lab/development — simulating events without external services | Gateway running | `HERMES_LAB_GOVERNANCE` from gateway env |
| **AlertManager webhook** (Phase 8 / TRIG-01) | Real Prometheus + AlertManager on KIND | Event-driven incident response — alerts arrive without polling | KIND cluster + helm release + PrometheusRule applied | `HERMES_LAB_GOVERNANCE` from gateway env (universal inheritance) |
| **K8s CronJob** (Phase 8 / TRIG-02) | Kubernetes native CronJob resource | GitOps schedule-in-git, stateless one-shot diagnostics, multi-tenant K8s | KIND cluster + Docker image + Secret | `HERMES_LAB_GOVERNANCE` set on container env spec |
| **GitHub webhook** (Phase 8 / TRIG-03) | Real GitHub webhook via smee.io public proxy | PR review automation, push-to-investigate workflows | GitHub repo + PAT + smee.io channel + smee-client running | `HERMES_LAB_GOVERNANCE` from gateway env |
| **Telegram bot** (Phase 8 / TRIG-04) | Real Telegram bot via @BotFather | Mobile-first chat ops, on-demand agent invocation from anywhere | Telegram account + bot token + user ID allowlist | `HERMES_LAB_GOVERNANCE` from gateway process env, per-process not per-message |

### Decision tree

- **You want a scheduled health check** → Hermes cron (default) OR K8s CronJob (if GitOps + K8s primitives matter more than gateway state)
- **You want event-driven incident response** → AlertManager webhook (real metrics-based alerting)
- **You want code-review automation** → GitHub webhook (`--deliver github_comment` posts back automatically)
- **You want on-demand chat ops** → Telegram bot (or Slack as production reference — see Section 4)
- **You're prototyping** → `hermes webhook test` with hand-crafted payloads (no external services)

### Hermes cron vs K8s CronJob — the honest comparison

| Concern | Hermes cron | K8s CronJob |
|---|---|---|
| Gateway-shared state (skills, history, audit) | ✓ native | ✗ stateless container |
| GitOps schedule in git | ✗ CLI-managed | ✓ YAML resource |
| K8s-native observability (job_metrics, pod logs) | ✗ Hermes session logs only | ✓ Prometheus + Loki/Vector |
| Multi-tenant resource quotas | ✗ shared gateway | ✓ namespace + ResourceQuota |
| Iteration speed | ✓ tweak prompt, re-register | ✗ rebuild image, kubectl apply |
| Where the agent runs | Gateway process | K8s pod |
| Image size | n/a (uses host hermes install) | ~700-900MB minimal Dockerfile |

**Real-world stance:** most agent work uses Hermes cron because state matters. K8s CronJob shines for fire-and-forget diagnostic jobs deployed alongside other K8s primitives via the same GitOps pipeline.

<!-- divider -->

## 6. Phase 8 New Environment Variables

Phase 8 adds three new environment variables to the lab export block. The full set as of Phase 8 is:

| Env Var | Values | Source | Used For |
|---|---|---|---|
| `HERMES_LAB_MODE` | `mock` \| `live` | Phase 1 | Existing |
| `HERMES_LAB_SCENARIO` | `clean` \| `crashloop2` \| ... | Phase 1 + 6 | Existing |
| `HERMES_LAB_GOVERNANCE` | `L1` \| `L2` \| `L3` \| `L4` | Phase 7 | Existing — inherited by triggered agents |
| `HERMES_LAB_TRACK` | `track-a` \| `track-b` \| `track-c` | Phase 7 | Existing |
| `MOCK_DATA_DIR` | path | Phase 1 | Existing |
| `PATH` (additions) | `infrastructure/wrappers:$PATH` | Phase 1 | Existing |
| **`GITHUB_TOKEN`** | classic PAT with `repo` scope OR fine-grained PAT with "Pull requests: Read and Write" | **Phase 8 (TRIG-03)** | GitHub webhook + agent comment posting via `gh pr comment` |
| **`TELEGRAM_BOT_TOKEN`** | bot token from @BotFather | **Phase 8 (TRIG-04)** | Telegram bot connection — activates the Hermes Telegram adapter |
| **`TELEGRAM_ALLOWED_USERS`** | comma-separated Telegram user IDs (from @userinfobot) | **Phase 8 (TRIG-04)** | Restricts which Telegram users the bot will respond to |
| **`SMEE_URL`** | `https://smee.io/<channel-id>` | **Phase 8 (TRIG-03)** | Public webhook proxy URL for forwarding GitHub events to local gateway |

**How to acquire each:**

- `GITHUB_TOKEN`: https://github.com/settings/tokens → Generate new token (classic) → check `repo` scope → copy `ghp_...` value. Time: ~3 min.
- `TELEGRAM_BOT_TOKEN`: Open Telegram → search @BotFather → `/newbot` → choose name + username → copy token. Time: ~2 min.
- `TELEGRAM_ALLOWED_USERS`: Open Telegram → search @userinfobot → `/start` → copy your numeric user ID. Time: ~30 sec.
- `SMEE_URL`: Visit https://smee.io/ → click "Start a new channel" → copy URL. Time: ~30 sec.

**Storage:** put these in `~/.hermes/.env` (gitignored) or export inline before each lab session. NEVER commit real tokens — Telegram and GitHub both have secret-scanning that will email you within minutes of a leaked credential.

**Governance inheritance:** the four trigger types ALL read `HERMES_LAB_GOVERNANCE` from their execution environment. The CronJob sets it on the container env spec; the AlertManager, GitHub, and Telegram triggers all inherit it from the gateway process env. This means a scheduled or triggered agent running unattended is governed by the same allowlist as an interactive one — there is no special "scheduled agent" governance bypass.
```

**Part B — Append 2-3 new quiz questions to QUIZ.mdx**

Read the existing `course-site/docs/module-12-triggers/quiz/QUIZ.mdx` (201 lines) to understand the existing question structure. The file uses headings like `### Question N: Topic` followed by lettered options (A, B, C, D) and a `<details>` block with the answer + explanation.

**Append 3 new questions** at the end of the file (BEFORE any closing summary section, AFTER the last existing question). Use the next sequential question numbers (e.g., if existing file ends at Question 8, the new ones are 9, 10, 11). Use this exact content:

```mdx
<!-- divider -->

### Question N: Hermes Cron vs K8s CronJob Tradeoffs

A team is building a daily slow-query health check for their RDS instances. The team uses GitOps for all K8s manifests, has a working Hermes gateway with `sre-dba-rds-slow-query` skill loaded, and wants the schedule reviewed via PR before any change ships. Which trigger pattern is the right primary choice, and what is the key tradeoff?

A) Hermes cron — best because the agent benefits from gateway-shared state and the iteration speed of `hermes cron create`
B) K8s CronJob — best because the schedule lives in a YAML manifest reviewed via PR (GitOps), and the diagnostic is stateless
C) AlertManager webhook — best because RDS slow queries should be event-driven, not polled
D) Hermes webhook test — best for development; switch to K8s CronJob for production

<details>
<summary>Show Answer</summary>

**Correct answer: B) K8s CronJob — best because the schedule lives in a YAML manifest reviewed via PR (GitOps), and the diagnostic is stateless**

The team's stated requirement is "schedule reviewed via PR before any change ships." That's the GitOps signal — schedules need to be in git, reviewed, deployed via a pipeline. Hermes cron is CLI-managed, which means schedule changes happen via `hermes cron create` outside the PR workflow.

The diagnostic itself is stateless (read slow_log, identify outliers, report) — there's no conversation history or accumulated learning that would benefit from gateway-shared state. So you don't lose anything important by running it as a one-shot K8s pod.

**Why not A (Hermes cron)?** Hermes cron is the right answer when state matters (most agent work) — for example, an investigation agent that builds context from yesterday's findings to inform today's work. The slow-query check doesn't need that. And critically, the team explicitly wants GitOps review, which Hermes cron doesn't provide.

**Why not C (AlertManager)?** AlertManager is for event-driven incident response — alert fires when something is wrong. A daily health check is the opposite pattern: scheduled scan, no triggering event. You could WIRE AlertManager to fire on slow queries, but that's a different design (and it requires a metrics pipeline like pg_stat_statements + a Prometheus exporter).

**Why not D?** `hermes webhook test` is for simulating events during development — it's not a production trigger.

**Real-world honest stance from CONTEXT.md D-10:** most agent work uses Hermes cron because state matters. But when the requirement explicitly calls for GitOps + stateless one-shot, K8s CronJob is the right answer.

</details>

<!-- divider -->

### Question N+1: AlertManager → Hermes Webhook Flow

A participant applies a broken pod manifest (`02-crashloop-backoff.yaml`), enables AlertManager via the helm release, applies a PrometheusRule, and starts the Hermes gateway with an `alertmanager` webhook subscription. Two minutes pass and the agent NEVER runs. Which is the most likely cause, given the symptoms below?

Symptoms:
- `kubectl get pods -n k8s-trouble-crashloop` shows the pod in CrashLoopBackOff with restartCount=12
- `kubectl get prometheusrule -n monitoring` shows the rule resource exists
- The Prometheus UI Rules page does NOT show the `PodCrashLooping` rule
- AlertManager pods are running and healthy

A) The PrometheusRule manifest is missing the `release: kube-prometheus` label, so kube-prometheus-stack's ruleSelector silently ignores it
B) The Hermes gateway is not running on port 8644
C) The agent's `sre-k8s-pod-health` skill is not loaded
D) The webhook prompt template uses `{alerts[0].labels.pod}` which doesn't render

<details>
<summary>Show Answer</summary>

**Correct answer: A) The PrometheusRule manifest is missing the `release: kube-prometheus` label, so kube-prometheus-stack's ruleSelector silently ignores it**

The decisive symptom is: "The Prometheus UI Rules page does NOT show the `PodCrashLooping` rule." Even though the PrometheusRule resource exists in the cluster (`kubectl get prometheusrule` shows it), Prometheus has not loaded it.

The kube-prometheus-stack Helm chart configures its Prometheus CRD with a `ruleSelector` that matches PrometheusRule resources by label. The default selector is `release: kube-prometheus` (or whatever name the helm release was installed with). Without that label, the rule is silently ignored — present in the cluster, invisible to Prometheus, never evaluated, never fires.

The fix: add `labels.release: kube-prometheus` to the PrometheusRule metadata. The Phase 8 PrometheusRule manifest at `infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml` includes this label specifically to prevent this class of failure.

**Why not B (gateway not running)?** If the gateway were down, `curl http://localhost:8644/health` would fail, but the symptoms don't mention that. The symptoms are about the alert never reaching AlertManager, not about AlertManager failing to deliver.

**Why not C (skill not loaded)?** A missing skill would cause the agent to run and immediately error out with a "skill not found" message. The symptoms say the agent NEVER runs — the alert never even arrived.

**Why not D (broken prompt template)?** A broken template would render literally and the agent would still run (just with garbage prompt text). The symptoms say the agent never runs at all.

**Reference:** 08-RESEARCH.md Pitfall 1 — PrometheusRule label selector mismatch is the most common Phase 8 TRIG-01 setup failure.

</details>

<!-- divider -->

### Question N+2: Chat Bot Governance Inheritance

A team has the Hermes Telegram bot running with `HERMES_LAB_GOVERNANCE=L2` (read-only diagnostics) set when the gateway started. A team member wants to escalate a single command to L4 (with kubectl write actions) without affecting the bot's overall governance. They send `/diagnose --governance L4 crashloop-pod` from Telegram. What happens, and why?

A) The agent runs at L4 because the `--governance L4` flag in the slash command overrides the gateway env var
B) The agent runs at L2 because Telegram bot governance is per-PROCESS — set when the gateway started, inherited by every command run during that gateway's lifetime, no per-message override
C) The agent runs at L2, but logs a warning about the unsupported flag
D) The bot rejects the command entirely because slash commands cannot include `--` flags

<details>
<summary>Show Answer</summary>

**Correct answer: B) The agent runs at L2 because Telegram bot governance is per-PROCESS — set when the gateway started, inherited by every command run during that gateway's lifetime, no per-message override**

Per CONTEXT.md D-19, Telegram bot governance is **per-process, not per-message**. The `HERMES_LAB_GOVERNANCE` env var is read by the gateway when it starts and inherited by every agent run spawned by the gateway during its lifetime. To change the governance level, the operator must restart the gateway:

```bash
hermes gateway stop
sleep 30   # Telegram polling lock release
export HERMES_LAB_GOVERNANCE=L4
export HERMES_LAB_TRACK=track-c
hermes gateway run
```

The `--governance L4` text in the slash command is not a flag the bot parses — it's just part of the prompt the agent sees. The agent receives the literal text `/diagnose --governance L4 crashloop-pod` as its prompt. It might INTERPRET that as a request to use L4, but the wrapper enforcement (Phase 7 wrapper_allowlist) reads its allowlist from `governance/governance-L2.yaml` because that's what `HERMES_LAB_GOVERNANCE=L2` resolves to. Any attempt by the agent to run a kubectl write command would be REJECTED with the GOVERNANCE REJECTED banner from the wrapper.

**Why is it designed this way?** Per-message governance escalation would let any user with bot access silently elevate privileges. The per-process model forces an explicit, observable, auditable change — the operator restarts the gateway with a different env var, and the new level applies to every subsequent command. The combination of `TELEGRAM_ALLOWED_USERS` (who can talk to the bot) plus `HERMES_LAB_GOVERNANCE` (what they can do) is the per-context governance model.

**Why not A?** The Hermes Telegram adapter (`gateway/platforms/telegram.py`) does not implement per-message governance flags. Even if you pass `--governance L4` in the message, it's just text the agent reads, not a directive to the wrapper.

**Why not C?** The bot doesn't parse flags — it doesn't know to log a warning about an unsupported one. The text just becomes part of the prompt.

**Why not D?** The bot accepts any slash command text, including `--` flags as part of the prompt. The text is passed through to the agent without parsing.

**Reference:** 08-CONTEXT.md D-19 (chat bot governance inheritance with per-command override constraint).

</details>
```

**Important:** The executor MUST replace `Question N`, `Question N+1`, `Question N+2` with the actual sequential numbers based on what's already in the file. If the existing file ends at "Question 8", the new ones become Question 9, Question 10, Question 11.

**Anti-pattern reminders (do NOT do these):**
- Do NOT remove or renumber existing reference.mdx sections
- Do NOT remove or renumber existing quiz questions
- Do NOT inline a copy of the LAB.mdx steps in the reading reference — use cross-references instead
- Do NOT add a Slack hands-on quiz question — Slack stays as production reference per D-17
- Do NOT add quiz questions about content NOT covered in the lab — every question must be answerable by a participant who completed Steps 9-16
  </action>

  <acceptance_criteria>
    - `test -f course-site/docs/module-12-triggers/reading/reference.mdx` (file still exists, was modified)
    - `wc -l course-site/docs/module-12-triggers/reading/reference.mdx` shows >320 lines (was 224, +~100 for new sections)
    - New comparison table section present: `grep -q 'Phase 8: Real Trigger Types' course-site/docs/module-12-triggers/reading/reference.mdx`
    - All four Phase 8 trigger types in the table: `grep -q 'AlertManager webhook' course-site/docs/module-12-triggers/reading/reference.mdx && grep -q 'K8s CronJob' course-site/docs/module-12-triggers/reading/reference.mdx && grep -q 'GitHub webhook' course-site/docs/module-12-triggers/reading/reference.mdx && grep -q 'Telegram bot' course-site/docs/module-12-triggers/reading/reference.mdx`
    - Hermes cron vs K8s CronJob comparison present: `grep -q 'Hermes cron vs K8s CronJob' course-site/docs/module-12-triggers/reading/reference.mdx`
    - Phase 8 env vars documented: `grep -q 'GITHUB_TOKEN' course-site/docs/module-12-triggers/reading/reference.mdx && grep -q 'TELEGRAM_BOT_TOKEN' course-site/docs/module-12-triggers/reading/reference.mdx && grep -q 'SMEE_URL' course-site/docs/module-12-triggers/reading/reference.mdx`
    - TELEGRAM_ALLOWED_USERS also documented: `grep -q 'TELEGRAM_ALLOWED_USERS' course-site/docs/module-12-triggers/reading/reference.mdx`
    - Acquisition notes present: `grep -q '@BotFather' course-site/docs/module-12-triggers/reading/reference.mdx && grep -q '@userinfobot' course-site/docs/module-12-triggers/reading/reference.mdx`
    - QUIZ.mdx grew: `wc -l course-site/docs/module-12-triggers/quiz/QUIZ.mdx` shows >320 lines (was 201, +~120 for 3 new questions)
    - 3 new questions present: `grep -c '^### Question' course-site/docs/module-12-triggers/quiz/QUIZ.mdx` returns at least 3 more than the original count (executor verifies original count first)
    - Quiz topics covered: `grep -q 'Hermes Cron vs K8s CronJob\|Hermes cron vs K8s CronJob' course-site/docs/module-12-triggers/quiz/QUIZ.mdx` (Q1)
    - `grep -q 'AlertManager.*Hermes Webhook\|AlertManager → Hermes' course-site/docs/module-12-triggers/quiz/QUIZ.mdx` (Q2)
    - `grep -q 'Chat Bot Governance\|Telegram.*governance\|chat bot governance' course-site/docs/module-12-triggers/quiz/QUIZ.mdx` (Q3)
    - All 3 new questions have answers in details blocks: at least 3 new `<details>` blocks added (executor counts before/after)
    - All 3 new questions reference real Phase 8 facts: `grep -q 'release: kube-prometheus' course-site/docs/module-12-triggers/quiz/QUIZ.mdx` (Q2 references the label selector fact) AND `grep -q 'D-19\|per-process\|per process' course-site/docs/module-12-triggers/quiz/QUIZ.mdx` (Q3 references D-19)
    - No `{alerts[0]` anti-pattern in either file: `! grep -F '{alerts[0]' course-site/docs/module-12-triggers/reading/reference.mdx && ! grep -F '{alerts[0]' course-site/docs/module-12-triggers/quiz/QUIZ.mdx`
    - reference.mdx and QUIZ.mdx still parse as valid MDX (no broken frontmatter, no unclosed admonitions)
  </acceptance_criteria>

  <verify>
    <automated>grep -q 'Phase 8: Real Trigger Types' course-site/docs/module-12-triggers/reading/reference.mdx && grep -q 'GITHUB_TOKEN' course-site/docs/module-12-triggers/reading/reference.mdx && grep -q 'TELEGRAM_BOT_TOKEN' course-site/docs/module-12-triggers/reading/reference.mdx && grep -q 'SMEE_URL' course-site/docs/module-12-triggers/reading/reference.mdx && grep -q '@BotFather' course-site/docs/module-12-triggers/reading/reference.mdx && grep -q 'AlertManager webhook' course-site/docs/module-12-triggers/reading/reference.mdx && grep -q 'K8s CronJob' course-site/docs/module-12-triggers/reading/reference.mdx && grep -q 'release: kube-prometheus' course-site/docs/module-12-triggers/quiz/QUIZ.mdx && grep -q 'per-process\|per process' course-site/docs/module-12-triggers/quiz/QUIZ.mdx && grep -q 'Hermes [Cc]ron vs K8s CronJob' course-site/docs/module-12-triggers/quiz/QUIZ.mdx && ! grep -F '{alerts[0]' course-site/docs/module-12-triggers/reading/reference.mdx && ! grep -F '{alerts[0]' course-site/docs/module-12-triggers/quiz/QUIZ.mdx</automated>
  </verify>

  <done>
    reference.mdx has a new section with the 4-trigger comparison table and Phase 8 env var documentation. QUIZ.mdx has 3 new questions covering Hermes-cron-vs-K8s-CronJob tradeoff, AlertManager flow troubleshooting, and chat bot governance inheritance. Existing sections in both files are untouched. All new content references real Phase 8 facts that participants can verify against the lab and infrastructure files. No anti-patterns ({alerts[0]) anywhere.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run these phase-level checks:

**Step structure invariants:**
```bash
# 1. Both lab mirrors have exactly 16 GUIDED steps after extension
for f in course-site/docs/module-12-triggers/lab/LAB.mdx modules/module-12-triggers/LAB.md; do
  count=$(grep -c '^## Step ' "$f")
  [ "$count" = "16" ] && echo "OK: $f has 16 steps" || echo "FAIL: $f has $count steps (expected 16)"
done

# 2. Existing FREE EXPLORE PHASE survived
for f in course-site/docs/module-12-triggers/lab/LAB.mdx modules/module-12-triggers/LAB.md; do
  grep -q '^## FREE EXPLORE PHASE' "$f" && echo "OK: $f has FREE EXPLORE" || echo "FAIL: $f missing FREE EXPLORE"
done

# 3. Three challenges still present
for f in course-site/docs/module-12-triggers/lab/LAB.mdx modules/module-12-triggers/LAB.md; do
  count=$(grep -c '^## Challenge' "$f")
  [ "$count" = "3" ] && echo "OK: $f has 3 challenges" || echo "FAIL: $f has $count challenges"
done
```

**Cross-file references work:**
```bash
# Lab references match infrastructure files (created by Plans 08-01 and 08-02)
test -f infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml      # Plan 08-01
test -f infrastructure/scenarios/k8s/cronjob/Dockerfile                       # Plan 08-01
test -f infrastructure/scenarios/k8s/github-webhook/smee-setup.sh             # Plan 08-02
test -f infrastructure/scenarios/k8s/telegram-bot/README.md                   # Plan 08-02

# And the lab text actually points at them
grep -q 'infrastructure/scenarios/k8s/alertmanager/' course-site/docs/module-12-triggers/lab/LAB.mdx
grep -q 'infrastructure/scenarios/k8s/cronjob/' course-site/docs/module-12-triggers/lab/LAB.mdx
grep -q 'infrastructure/scenarios/k8s/github-webhook/' course-site/docs/module-12-triggers/lab/LAB.mdx
grep -q 'infrastructure/scenarios/k8s/telegram-bot/' course-site/docs/module-12-triggers/lab/LAB.mdx
```

**Critical anti-patterns absent from ALL Phase 8 lab/reading/quiz files:**
```bash
# Array index syntax must NOT appear in ANY Phase 8 file (D-RESEARCH constraint)
for f in course-site/docs/module-12-triggers/lab/LAB.mdx \
         modules/module-12-triggers/LAB.md \
         course-site/docs/module-12-triggers/reading/reference.mdx \
         course-site/docs/module-12-triggers/quiz/QUIZ.mdx; do
  if grep -F '{alerts[0]' "$f"; then
    echo "FAIL: anti-pattern in $f"
    exit 1
  fi
done
echo "OK: no array-index anti-patterns"
```

**Phase 8 env vars in all four content files:**
```bash
for var in GITHUB_TOKEN TELEGRAM_BOT_TOKEN SMEE_URL; do
  for f in course-site/docs/module-12-triggers/lab/LAB.mdx \
           modules/module-12-triggers/LAB.md \
           course-site/docs/module-12-triggers/reading/reference.mdx; do
    grep -q "$var" "$f" || echo "FAIL: $var missing from $f"
  done
done
```

**MDX/MD mirror parity:** every step heading present in MDX MUST be present in MD:
```bash
for n in $(seq 1 16); do
  mdx_present=$(grep -c "^## Step $n:" course-site/docs/module-12-triggers/lab/LAB.mdx)
  md_present=$(grep -c "^## Step $n:" modules/module-12-triggers/LAB.md)
  [ "$mdx_present" = "$md_present" ] || echo "FAIL: Step $n parity"
done
```
</verification>

<success_criteria>
- `course-site/docs/module-12-triggers/lab/LAB.mdx` has 16 GUIDED steps (was 8) — old Steps 1-8 untouched, new Steps 9-16 inserted in CONTEXT.md D-21 order
- `modules/module-12-triggers/LAB.md` has the same 16 steps with mirrored structure (admonitions converted to blockquotes)
- All Phase 8 lab text references real artifact paths from Plans 08-01 and 08-02 — no aspirational filenames, no placeholder paths
- Every new step includes the complete Phase 7 + Phase 8 export block per D-26
- TRIG-03 and TRIG-04 steps include Solo Learner callouts
- Per-track variants documented for K8s CronJob (Step 11) and Telegram /diagnose (Step 15)
- `reference.mdx` has a new section with the 4-trigger comparison table and Phase 8 env var docs
- `QUIZ.mdx` has 3 new questions covering D-25's required topics (cron tradeoff, AlertManager flow, chat bot governance)
- Zero `{alerts[0]...}` array index anti-patterns anywhere in lab, reading, or quiz content
- Existing FREE EXPLORE Challenges 1-3 untouched, present after new Step 16
</success_criteria>

<output>
After completion, create `.planning/phases/08-agent-triggers/08-03-SUMMARY.md` following the standard summary template. Include:
- Files modified with line count delta (before vs after)
- New step structure with insertion point and ordering
- Decisions honored (D-01 extend, D-21 step ordering, D-22 per-track, D-23 dual-mirror, D-24 reading light-touch, D-25 quiz updates, D-26 env vars)
- Research findings incorporated (prompt template constraint, github_comment built-in, Telegram adapter exists, smee.io npx pattern, polling conflict, label selector pitfall)
- Verification results (step counts, mirror parity, anti-pattern absence)
- Any open questions that emerged during execution (e.g., specific section number chosen for the new reference.mdx insertion based on what already existed in the file)
</output>
