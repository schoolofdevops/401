---
phase: 08-agent-triggers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infrastructure/helm/prometheus-lab-values.yaml
  - infrastructure/kind/cluster-config.yaml
  - infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml
  - infrastructure/scenarios/k8s/alertmanager/alertmanager-config.yaml
  - infrastructure/scenarios/k8s/alertmanager/README.md
  - infrastructure/scenarios/k8s/cronjob/Dockerfile
  - infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml
  - infrastructure/scenarios/k8s/cronjob/README.md
autonomous: true
requirements:
  - TRIG-01
  - TRIG-02
must_haves:
  truths:
    - "A participant runs helm upgrade with the updated prometheus-lab-values.yaml and AlertManager pods reach Ready state in the monitoring namespace"
    - "A participant applies infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml and the rule is loaded by Prometheus (visible via kubectl get prometheusrule -n monitoring and in the Prometheus Rules UI)"
    - "A participant applies the Phase 6 crashloop2 scenario and observes the PodCrashLooping alert transition from Pending to Firing within 2 minutes (visible in the AlertManager UI on port 30093 or via kubectl logs on alertmanager pod)"
    - "A participant running the AlertManager receiver config sees POST attempts to http://host.docker.internal:8644/webhooks/alertmanager in alertmanager pod logs once the alert fires"
    - "A participant builds the K8s CronJob Dockerfile with docker build and the image successfully completes with hermes CLI installed inside"
    - "A participant applies the K8s CronJob manifest infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml and kubectl get cronjob shows the resource with the correct schedule"
    - "The prompt template in the Hermes webhook subscription guidance uses {alerts} (full array) — NOT {alerts[0].labels.pod} (unsupported array index syntax)"
    - "All 3 tracks (A/B/C) have their own K8s CronJob manifest variant or a parameterized single manifest with per-track labels and env"
    - "The AlertManager PrometheusRule includes the label release: kube-prometheus so the kube-prometheus-stack ruleSelector discovers it (per RESEARCH Pitfall 1)"
    - "The K8s CronJob manifest includes imagePullPolicy: IfNotPresent so local kind load docker-image images work (per RESEARCH Pitfall for CronJob image pull)"
  artifacts:
    - path: "infrastructure/helm/prometheus-lab-values.yaml"
      provides: "kube-prometheus-stack with alertmanager.enabled: true and AlertManager receiver config pointing at Hermes gateway"
      contains: "alertmanager:\n  enabled: true"
    - path: "infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml"
      provides: "PrometheusRule manifest firing on k8s-trouble-crashloop namespace pod restarts"
      contains: "PodCrashLooping"
    - path: "infrastructure/scenarios/k8s/alertmanager/alertmanager-config.yaml"
      provides: "AlertManager receiver ConfigMap or helm values block pointing at host.docker.internal:8644"
      contains: "host.docker.internal:8644/webhooks/alertmanager"
    - path: "infrastructure/scenarios/k8s/alertmanager/README.md"
      provides: "Flow documentation for AlertManager → Hermes gateway pipeline"
      contains: "host.docker.internal"
    - path: "infrastructure/scenarios/k8s/cronjob/Dockerfile"
      provides: "Minimal hermes-agent container image for K8s CronJob (python:3.12-slim base)"
      contains: "FROM python:3.12-slim"
    - path: "infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml"
      provides: "K8s CronJob manifest invoking hermes CLI on schedule with per-track variants"
      contains: "kind: CronJob"
    - path: "infrastructure/scenarios/k8s/cronjob/README.md"
      provides: "Use-this-when decision criteria for Hermes cron vs K8s CronJob (per D-10)"
      contains: "Use Hermes cron when"
    - path: "infrastructure/kind/cluster-config.yaml"
      provides: "KIND cluster with port 8644 in extraPortMappings for Linux edge case (macOS Docker Desktop works without it, but Linux needs explicit mapping)"
      contains: "containerPort: 8644"
  key_links:
    - from: "infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml"
      to: "infrastructure/scenarios/k8s/02-crashloop-backoff.yaml (Phase 6 namespace k8s-trouble-crashloop)"
      via: "PromQL expression namespace label selector"
      pattern: "namespace=\"k8s-trouble-crashloop\""
    - from: "infrastructure/scenarios/k8s/alertmanager/alertmanager-config.yaml"
      to: "Hermes webhook gateway (host.docker.internal:8644/webhooks/alertmanager)"
      via: "AlertManager webhook_config receiver URL"
      pattern: "host.docker.internal:8644"
    - from: "infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml"
      to: "infrastructure/scenarios/k8s/cronjob/Dockerfile (hermes-lab:cronjob image)"
      via: "container image reference"
      pattern: "image: hermes-lab:cronjob"
    - from: "infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml"
      to: "Phase 7 governance wrappers (HERMES_LAB_GOVERNANCE env var)"
      via: "container env vars in jobTemplate.spec.template.spec.containers[0].env"
      pattern: "HERMES_LAB_GOVERNANCE"
---

<objective>
Build the KIND-side infrastructure for TRIG-01 (AlertManager webhook) and TRIG-02 (K8s CronJob). This plan ships 8 infrastructure files: two modified Phase 1 files (helm values, KIND cluster config), three new files in a new `alertmanager/` subdirectory, and three new files in a new `cronjob/` subdirectory. No participant-facing content is authored here — this plan produces the infrastructure that Plan 08-03 will reference in the lab walkthrough.

Purpose: Without these concrete files, the Module 12 lab extension in Plan 08-03 has nothing to `kubectl apply -f` or `docker build`. The lab text is only as concrete as the artifacts it references. This plan also resolves the open questions from RESEARCH: PrometheusRule label selector for kube-prometheus-stack auto-discovery, K8s CronJob image source (Dockerfile recommended over bloated official image), and `host.docker.internal` port mapping for the Linux edge case.

Phase 7 governance inheritance is baked into the CronJob manifest: HERMES_LAB_GOVERNANCE, HERMES_LAB_TRACK, and related env vars propagate into the container spec, so triggered agents are governed exactly like interactive ones (per CONTEXT.md D-02).

Output:
- `infrastructure/helm/prometheus-lab-values.yaml` — updated with `alertmanager.enabled: true` plus receiver config pointing at Hermes gateway
- `infrastructure/kind/cluster-config.yaml` — updated with `extraPortMappings` entry for port 8644 (with comment explaining macOS Docker Desktop doesn't need it but Linux does)
- `infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml` — PrometheusRule CRD firing on crashloop2 scenario
- `infrastructure/scenarios/k8s/alertmanager/alertmanager-config.yaml` — AlertManager receiver configuration as helm values snippet (to be merged into prometheus-lab-values.yaml OR kept as a separate reference file — executor chooses)
- `infrastructure/scenarios/k8s/alertmanager/README.md` — flow diagram + usage instructions
- `infrastructure/scenarios/k8s/cronjob/Dockerfile` — minimal hermes-agent image based on python:3.12-slim
- `infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml` — K8s CronJob manifest with per-track env var variants
- `infrastructure/scenarios/k8s/cronjob/README.md` — "use this when" decision criteria per D-10
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-agent-triggers/08-CONTEXT.md
@.planning/phases/08-agent-triggers/08-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Key facts the executor needs WITHOUT exploring the codebase. -->

Current state of files that will be MODIFIED:

From infrastructure/helm/prometheus-lab-values.yaml (36 lines):
```yaml
alertmanager:
  enabled: false    # Phase 8 flips to true

nodeExporter:
  resources:
    requests:
      cpu: 10m
      memory: 32Mi

prometheus:
  prometheusSpec:
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
    serviceMonitorSelectorNilUsesHelmValues: false
  service:
    type: NodePort
    nodePort: 30091

grafana:
  resources:
    requests:
      cpu: 50m
      memory: 128Mi
  service:
    type: NodePort
    nodePort: 30090
  adminPassword: admin
  adminUser: admin
```

From infrastructure/kind/cluster-config.yaml (38 lines):
```yaml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: lab
nodes:
  - role: control-plane
    kubeadmConfigPatches:
      - |
        kind: InitConfiguration
        nodeRegistration:
          kubeletExtraArgs:
            node-labels: "ingress-ready=true"
    extraPortMappings:
      - containerPort: 30080    # Reference app dashboard
        hostPort: 30080
        protocol: TCP
      - containerPort: 30090    # Grafana
        hostPort: 30090
        protocol: TCP
      - containerPort: 30091    # Prometheus
        hostPort: 30091
        protocol: TCP
      - containerPort: 30400    # Reserved (Hermes UI, ArgoCD, etc.)
        hostPort: 30400
        protocol: TCP
      - containerPort: 30500
        hostPort: 30500
        protocol: TCP
      - containerPort: 30600
        hostPort: 30600
        protocol: TCP
  - role: worker
  - role: worker
networking:
  apiServerAddress: "127.0.0.1"
```

From infrastructure/scenarios/k8s/02-crashloop-backoff.yaml (Phase 6 — DO NOT MODIFY):
```yaml
# Namespace: k8s-trouble-crashloop
# Deployment: crasher (replicas: 1)
# Container: busybox:1.36 with command ["sh", "-c", "echo 'starting...'; echo 'fatal: missing config'; exit 1"]
# Effect: Container exits with code 1 immediately → CrashLoopBackOff
```

Existing scenario doc pattern (from infrastructure/scenarios/k8s/02-crashloop-backoff.md):
- Header with failure mode description
- Apply: kubectl command
- Cleanup: kubectl delete namespace command
- Brief teaching notes

The AlertManager + CronJob README files SHOULD follow this same pattern for consistency.

### Hermes Facts (from 08-RESEARCH.md — DO NOT re-verify)

- Hermes webhook gateway default port: **8644** (confirmed in hermes_cli/webhook.py line 78)
- `hermes webhook subscribe alertmanager --events alertmanager-alert --prompt "..." --skill "..." --deliver local` — built-in subscription command, no custom code needed
- AlertManager payload structure: `{version, status, alerts[]}` where `alerts[].labels` and `alerts[].annotations` contain the per-alert details
- **CRITICAL:** Hermes `_render_prompt` does NOT support array index access. Use `{alerts}` (renders the full JSON array), NOT `{alerts[0].labels.pod}`. The agent prompt must instruct the agent to parse the array itself.
- Default webhook deliver type `local` prints to gateway stdout (good for lab observation)
- kube-prometheus-stack PrometheusRule label selector: **must include `release: kube-prometheus`** (or whatever release name was used in Phase 1 helm install) — otherwise the rule is silently ignored
- Hermes install from GitHub: `pip install "git+https://github.com/NousResearch/hermes-agent.git#egg=hermes-agent[messaging,cron]"` (NOT on PyPI)
- python:3.12-slim + hermes-agent[messaging,cron] results in ~700-900MB image (vs 2-3GB for the official debian:13.4 image that bundles Playwright/ffmpeg)

### Phase 7 Governance Inheritance (from 07-CONTEXT.md)

The CronJob container env block MUST include these env vars (per D-02 universal inheritance and D-26 env var set):
- `HERMES_LAB_MODE` (mock|live)
- `HERMES_LAB_SCENARIO` (crashloop2 for Track C demo)
- `HERMES_LAB_GOVERNANCE` (L2 default for scheduled agents)
- `HERMES_LAB_TRACK` (track-a|track-b|track-c)
- `MOCK_DATA_DIR` (`/data/mock-data/<track>`)
- `ANTHROPIC_API_KEY` (via secretKeyRef to `hermes-secrets` Secret)

The wrappers in `infrastructure/wrappers/` (Phase 7) read these env vars. The CronJob demonstrates that triggered agents inherit the same governance as interactive ones.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build TRIG-01 AlertManager infrastructure (helm values, PrometheusRule, receiver config, README)</name>

  <read_first>
    - infrastructure/helm/prometheus-lab-values.yaml (current state before modification)
    - infrastructure/kind/cluster-config.yaml (current state before modification)
    - infrastructure/scenarios/k8s/02-crashloop-backoff.yaml (Phase 6 reference — DO NOT modify, TRIG-01 fires on this namespace)
    - infrastructure/scenarios/k8s/02-crashloop-backoff.md (sibling doc pattern reference)
    - .planning/phases/08-agent-triggers/08-CONTEXT.md (D-03 through D-07, D-26 env vars)
    - .planning/phases/08-agent-triggers/08-RESEARCH.md (Pattern 1 AlertManager, Pitfall 1 PrometheusRule label selector, Pitfall 3 host.docker.internal on Linux, Code Examples section)
  </read_first>

  <files>
    - infrastructure/helm/prometheus-lab-values.yaml (MODIFY)
    - infrastructure/kind/cluster-config.yaml (MODIFY)
    - infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml (CREATE)
    - infrastructure/scenarios/k8s/alertmanager/alertmanager-config.yaml (CREATE)
    - infrastructure/scenarios/k8s/alertmanager/README.md (CREATE)
  </files>

  <action>
Implements TRIG-01 infrastructure per D-03, D-04, D-05, D-06, D-07.

**1. Modify `infrastructure/helm/prometheus-lab-values.yaml`** — Flip `alertmanager.enabled: false` to `true` and add the full AlertManager block with receiver config. Use this exact replacement for the top of the file (preserve the rest of the file untouched):

```yaml
# Prometheus + Grafana lab values — optimized for developer laptops
# Reduces resource requests so KIND doesn't OOM on 8GB machines
# Phase 8: AlertManager enabled for TRIG-01 (AlertManager webhook → Hermes gateway)

alertmanager:
  enabled: true
  alertmanagerSpec:
    resources:
      requests:
        cpu: 50m
        memory: 64Mi
  service:
    type: NodePort
    nodePort: 30093
  config:
    global:
      resolve_timeout: 5m
    route:
      group_by: ['alertname', 'namespace']
      group_wait: 10s
      group_interval: 1m
      repeat_interval: 12h
      receiver: hermes-webhook
    receivers:
      - name: hermes-webhook
        webhook_configs:
          - url: "http://host.docker.internal:8644/webhooks/alertmanager"
            send_resolved: false
```

The rest of the file (nodeExporter, prometheus, grafana sections) is PRESERVED unchanged.

Also add the AlertManager NodePort 30093 note at the top of the file as a comment.

**2. Modify `infrastructure/kind/cluster-config.yaml`** — Add two new `extraPortMappings` entries (30093 for AlertManager UI access AND 8644 for Hermes webhook gateway on the Linux edge case). Add them after the existing port 30091 Prometheus entry, BEFORE the reserved 30400 entry. Include a comment explaining the Linux vs macOS distinction for port 8644:

```yaml
      # AlertManager UI (Phase 8 / TRIG-01)
      - containerPort: 30093
        hostPort: 30093
        protocol: TCP
      # Hermes webhook gateway — REQUIRED on Linux Docker (not Docker Desktop);
      # macOS Docker Desktop resolves host.docker.internal from KIND pods without this mapping.
      # Leaving it always on is harmless on macOS and unblocks Linux participants.
      - containerPort: 8644
        hostPort: 8644
        protocol: TCP
```

**3. Create `infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml`** — PrometheusRule CRD firing on the Phase 6 crashloop2 namespace. MUST include `labels.release: kube-prometheus` for kube-prometheus-stack auto-discovery (per RESEARCH Pitfall 1). Use this exact content:

```yaml
# Phase 8 / TRIG-01: PrometheusRule firing on Phase 6 crashloop2 scenario
# Apply: kubectl apply -f infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml
# Cleanup: kubectl delete -f infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml
#
# The `release: kube-prometheus` label is REQUIRED — kube-prometheus-stack Helm chart
# configures its Prometheus CRD to only watch PrometheusRule resources matching this label.
# Without it, the rule is silently ignored. (See 08-RESEARCH.md Pitfall 1.)
<!-- divider -->
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: hermes-lab-rules
  namespace: monitoring
  labels:
    release: kube-prometheus
    phase: "08"
    trigger: alertmanager
spec:
  groups:
    - name: hermes-lab.k8s-crashloop
      rules:
        - alert: PodCrashLooping
          expr: |
            increase(kube_pod_container_status_restarts_total{namespace="k8s-trouble-crashloop"}[2m]) > 2
          for: 30s
          labels:
            severity: warning
            track: c
            trigger_source: alertmanager
          annotations:
            summary: "Pod {{ $labels.pod }} in namespace {{ $labels.namespace }} is crash-looping"
            description: "Container {{ $labels.container }} in pod {{ $labels.pod }} has restarted more than 2 times in 2 minutes."
            runbook_url: "https://hermes-lab.local/runbook/k8s-crashloop"
```

**4. Create `infrastructure/scenarios/k8s/alertmanager/alertmanager-config.yaml`** — Standalone reference file duplicating the AlertManager `config:` block from the helm values for teaching clarity. Participants read this file to understand the receiver shape; the actual runtime config comes from helm values. Use this exact content:

```yaml
# Phase 8 / TRIG-01: AlertManager receiver config (reference copy)
#
# This file mirrors the `alertmanager.config:` block in
# infrastructure/helm/prometheus-lab-values.yaml for teaching clarity.
# It is NOT applied directly — the actual AlertManager runtime config comes
# from the Helm release. Edit the helm values file to change behavior.
#
# Teaching point: AlertManager receiver URL uses host.docker.internal:8644,
# which resolves to the host machine from inside Docker Desktop KIND nodes.
# On Linux Docker (not Docker Desktop), use 172.17.0.1 instead (the docker0
# bridge gateway) OR rely on the extraPortMapping in cluster-config.yaml.
<!-- divider -->
global:
  resolve_timeout: 5m

route:
  group_by: ['alertname', 'namespace']
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 12h
  receiver: hermes-webhook

receivers:
  - name: hermes-webhook
    webhook_configs:
      - url: "http://host.docker.internal:8644/webhooks/alertmanager"
        send_resolved: false
        # send_resolved: false means AlertManager only POSTs when alerts fire,
        # not when they resolve. Flip to true in production to get paired
        # firing + resolved events.
```

**5. Create `infrastructure/scenarios/k8s/alertmanager/README.md`** — Flow documentation. Use this exact content:

```markdown
# AlertManager → Hermes Agent Webhook Flow

**Phase 8 / TRIG-01** — Real Prometheus + AlertManager stack on KIND firing on the Phase 6 `crashloop2` scenario.

## Flow Diagram

```
┌──────────────────────┐
│ kubectl apply -f     │
│ 02-crashloop-        │
│ backoff.yaml         │
│ (Phase 6)            │
└──────────┬───────────┘
           │ creates
           ▼
┌──────────────────────┐    scrapes    ┌──────────────────────┐
│ Pod crasher in       │◀──────────────│ kube-state-metrics   │
│ k8s-trouble-         │               │ (in monitoring ns)   │
│ crashloop ns         │               └──────────┬───────────┘
│ (restarts every ~5s) │                          │ exposes
└──────────────────────┘                          ▼
                                       ┌──────────────────────┐
                                       │ Prometheus           │
                                       │ evaluates rule       │
                                       │ PodCrashLooping      │
                                       └──────────┬───────────┘
                                                  │ fires after
                                                  │ increase > 2
                                                  │ for 30s
                                                  ▼
                                       ┌──────────────────────┐
                                       │ AlertManager         │
                                       │ receiver             │
                                       │ hermes-webhook       │
                                       └──────────┬───────────┘
                                                  │ POST
                                                  │ host.docker.internal
                                                  │ :8644/webhooks/
                                                  │ alertmanager
                                                  ▼
                                       ┌──────────────────────┐
                                       │ Hermes gateway       │
                                       │ webhook adapter      │
                                       │ (subscription:       │
                                       │  alertmanager)       │
                                       └──────────┬───────────┘
                                                  │ spawns agent run
                                                  ▼
                                       ┌──────────────────────┐
                                       │ sre-k8s-pod-health   │
                                       │ skill diagnoses pod  │
                                       │ → output to terminal │
                                       └──────────────────────┘
```

## Files

- `prometheus-rules.yaml` — PrometheusRule CRD (one alert: `PodCrashLooping`)
- `alertmanager-config.yaml` — Reference copy of the AlertManager receiver config
- `README.md` — This file

The runtime AlertManager config lives in `infrastructure/helm/prometheus-lab-values.yaml`
under the `alertmanager.config` block. Edit that file to change production AM behavior.

## Apply

```bash
# 1. Ensure AlertManager is enabled in the helm release
helm upgrade --install kube-prometheus prometheus-community/kube-prometheus-stack \
  --namespace monitoring --create-namespace \
  -f infrastructure/helm/prometheus-lab-values.yaml

# 2. Apply the PrometheusRule CRD
kubectl apply -f infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml

# 3. Start Hermes webhook gateway on host (another terminal)
hermes gateway run

# 4. Subscribe the alertmanager webhook (another terminal)
hermes webhook subscribe alertmanager \
  --events "alertmanager-alert" \
  --prompt "AlertManager PodCrashLooping alert fired. Details: {alerts}. Load the sre-k8s-pod-health skill and diagnose the affected pod in the namespace shown in the alert labels." \
  --skill "sre-k8s-pod-health" \
  --deliver local

# 5. Apply the Phase 6 crashloop2 scenario
kubectl apply -f infrastructure/scenarios/k8s/02-crashloop-backoff.yaml

# 6. Wait ~2 min — AlertManager fires → Hermes receives → agent runs
```

## IMPORTANT: Prompt template array syntax

Hermes `_render_prompt` does NOT support array index access. Use `{alerts}` (which
expands to the full JSON-serialized alerts array) and let the agent parse the array
itself. **Do NOT** use `{alerts[0].labels.pod}` — this will render as a literal string,
not the pod name.

The prompt above instructs the agent to "diagnose the affected pod in the namespace
shown in the alert labels" — the agent reads `{alerts}` and extracts the namespace
and pod name via its LLM reasoning.

## Cleanup

```bash
kubectl delete -f infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml
kubectl delete -f infrastructure/scenarios/k8s/02-crashloop-backoff.yaml
hermes webhook unsubscribe alertmanager
```

## Linux Docker Note

On Linux with native Docker (not Docker Desktop), `host.docker.internal` does not
resolve from inside KIND pods by default. Two options:

1. **Use the extraPortMapping** (already in cluster-config.yaml) — `localhost:8644`
   inside a KIND pod can reach the host gateway via the port mapping.
2. **Hardcode the docker0 bridge IP** — change the receiver URL to
   `http://172.17.0.1:8644/webhooks/alertmanager`.

macOS Docker Desktop resolves `host.docker.internal` natively — no change needed.
```
  </action>

  <acceptance_criteria>
    - `grep -q 'enabled: true' infrastructure/helm/prometheus-lab-values.yaml && head -20 infrastructure/helm/prometheus-lab-values.yaml | grep -q 'alertmanager:'` (alertmanager flipped to true)
    - `grep -q 'hermes-webhook' infrastructure/helm/prometheus-lab-values.yaml` (receiver name present in values)
    - `grep -q 'host.docker.internal:8644/webhooks/alertmanager' infrastructure/helm/prometheus-lab-values.yaml` (receiver URL present)
    - `grep -q 'containerPort: 8644' infrastructure/kind/cluster-config.yaml` (port 8644 added to extraPortMappings)
    - `grep -q 'containerPort: 30093' infrastructure/kind/cluster-config.yaml` (AlertManager UI port added)
    - `test -f infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml` (file exists)
    - `grep -q 'release: kube-prometheus' infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml` (label selector present — Pitfall 1 prevention)
    - `grep -q 'PodCrashLooping' infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml` (alert name correct)
    - `grep -q 'namespace="k8s-trouble-crashloop"' infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml` (Phase 6 namespace referenced)
    - `test -f infrastructure/scenarios/k8s/alertmanager/alertmanager-config.yaml` (file exists)
    - `test -f infrastructure/scenarios/k8s/alertmanager/README.md` (file exists)
    - `grep -q '{alerts}' infrastructure/scenarios/k8s/alertmanager/README.md` (correct template syntax documented)
    - `! grep -F '{alerts[0]' infrastructure/scenarios/k8s/alertmanager/README.md` (incorrect array index syntax NOT present anywhere)
    - `yq '.spec.groups[0].rules[0].alert' infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml` returns `PodCrashLooping` (valid yaml parses correctly)
    - Valid YAML: `yq . infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml > /dev/null` exits 0
    - Valid YAML: `yq . infrastructure/scenarios/k8s/alertmanager/alertmanager-config.yaml > /dev/null` exits 0
    - Valid YAML: `yq . infrastructure/helm/prometheus-lab-values.yaml > /dev/null` exits 0
    - Valid YAML: `yq . infrastructure/kind/cluster-config.yaml > /dev/null` exits 0
  </acceptance_criteria>

  <verify>
    <automated>yq . infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml > /dev/null && yq . infrastructure/scenarios/k8s/alertmanager/alertmanager-config.yaml > /dev/null && yq . infrastructure/helm/prometheus-lab-values.yaml > /dev/null && yq . infrastructure/kind/cluster-config.yaml > /dev/null && grep -q 'release: kube-prometheus' infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml && grep -q 'containerPort: 8644' infrastructure/kind/cluster-config.yaml && grep -q 'enabled: true' infrastructure/helm/prometheus-lab-values.yaml && ! grep -F '{alerts[0]' infrastructure/scenarios/k8s/alertmanager/README.md</automated>
  </verify>

  <done>
    All 5 files (2 modified + 3 new) are on disk. YAML is valid and parseable. PrometheusRule has the required label selector. Port 8644 is in KIND config. AlertManager receiver points at the correct URL. README documents the flow AND the array template syntax constraint. No `{alerts[0]...}` anti-pattern anywhere in the authored content.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build TRIG-02 K8s CronJob infrastructure (Dockerfile, agent manifest, README with use-this-when)</name>

  <read_first>
    - infrastructure/scenarios/k8s/02-crashloop-backoff.md (scenario doc pattern reference)
    - .planning/phases/08-agent-triggers/08-CONTEXT.md (D-08, D-09, D-10, D-11, D-22, D-26 env vars)
    - .planning/phases/08-agent-triggers/08-RESEARCH.md (Pattern 2 K8s CronJob, BLOCKER-02 resolution, Pitfall 4 imagePullPolicy, Code Examples minimum Dockerfile)
    - infrastructure/helm/prometheus-lab-values.yaml (already modified in Task 1 — read to confirm)
  </read_first>

  <files>
    - infrastructure/scenarios/k8s/cronjob/Dockerfile (CREATE)
    - infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml (CREATE)
    - infrastructure/scenarios/k8s/cronjob/README.md (CREATE)
  </files>

  <action>
Implements TRIG-02 infrastructure per D-08, D-09, D-10, D-11, D-22, D-26. Resolves BLOCKER-02 (K8s CronJob image source) with Option A: ship a minimal Dockerfile as a teaching artifact.

**1. Create `infrastructure/scenarios/k8s/cronjob/Dockerfile`** — Minimal hermes-agent container image based on `python:3.12-slim`. Use this exact content:

```dockerfile
# syntax=docker/dockerfile:1.6
# Phase 8 / TRIG-02: Minimal hermes-agent image for K8s CronJob
#
# Teaching artifact: demonstrates packaging an agent for K8s CronJob deployment.
# Used by infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml.
#
# Why not nousresearch/hermes-agent:latest?
#   The official image is ~2-3GB because it bundles Playwright, ffmpeg, Node.js,
#   and Chromium for the web-browsing Hermes use cases. For a diagnostic
#   CronJob, we only need the core agent + messaging + cron extras.
#   python:3.12-slim keeps the image to ~700-900MB.
#
# Build:
#   docker build -t hermes-lab:cronjob infrastructure/scenarios/k8s/cronjob/
#
# Load into KIND:
#   kind load docker-image hermes-lab:cronjob --name lab
#
# Verify inside image:
#   docker run --rm hermes-lab:cronjob hermes --version
FROM python:3.12-slim

# System dependencies for hermes-agent (git for pip source install, ripgrep for
# in-image search, curl for health checks, nodejs/npm for smee-client if needed)
RUN apt-get update && apt-get install -y --no-install-recommends \
        git \
        curl \
        ripgrep \
        ca-certificates \
    && rm -rf /var/lib/apt/lists/*

# Install hermes-agent with the extras the CronJob actually needs.
# Use git+https because hermes-agent is not published to PyPI.
RUN pip install --no-cache-dir \
    "git+https://github.com/NousResearch/hermes-agent.git#egg=hermes-agent[messaging,cron]"

WORKDIR /work

# Default entrypoint is the hermes CLI; the CronJob spec overrides `command`
# to invoke a specific skill with a specific prompt.
ENTRYPOINT ["hermes"]
```

**2. Create `infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml`** — K8s CronJob manifest with per-track variants. The file contains THREE separate CronJob resources (Track A, Track B, Track C) in a single multi-document YAML file, each with track-appropriate env vars and prompt. Participants uncomment (or kubectl apply with label selector) the track they care about. Use this exact content:

```yaml
# Phase 8 / TRIG-02: K8s CronJob running a scheduled Hermes agent
#
# Ships THREE per-track variants in one multi-document YAML file:
#   - hermes-track-a-health (Database — sre-dba-rds-slow-query)
#   - hermes-track-b-health (FinOps — sre-ec2-health-check)
#   - hermes-track-c-health (Kubernetes — sre-k8s-pod-health)
#
# Phase 7 governance inheritance: each CronJob sets HERMES_LAB_GOVERNANCE,
# HERMES_LAB_TRACK, HERMES_LAB_MODE, HERMES_LAB_SCENARIO, and MOCK_DATA_DIR.
# The wrappers in infrastructure/wrappers/ read these env vars and enforce
# the Phase 7 wrapper_allowlist. Scheduled agents are governed exactly like
# interactive ones. (See 08-CONTEXT.md D-02.)
#
# Prerequisites:
#   1. Build image:  docker build -t hermes-lab:cronjob infrastructure/scenarios/k8s/cronjob/
#   2. Load to KIND: kind load docker-image hermes-lab:cronjob --name lab
#   3. Create API key secret (avoid committing tokens to git):
#      kubectl create secret generic hermes-secrets \
#        --from-literal=anthropic-api-key="$ANTHROPIC_API_KEY"
#   4. (Optional) Mount mock data via ConfigMap if running in mock mode.
#
# Apply ONE track (Track C example):
#   kubectl apply -f infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml -l track=track-c
#
# Or apply all three:
#   kubectl apply -f infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml
#
# Observe:
#   kubectl get cronjob
#   kubectl get jobs
#   kubectl logs -l job-name=<latest-job-name>
#
# Cleanup:
#   kubectl delete -f infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml
<!-- divider -->
# =========================================================================
# Track A (Database) — Daily slow-query health check
# =========================================================================
apiVersion: batch/v1
kind: CronJob
metadata:
  name: hermes-track-a-health
  namespace: default
  labels:
    phase: "08"
    trigger: k8s-cronjob
    track: track-a
spec:
  schedule: "*/5 * * * *"   # Every 5 min for lab iteration speed
                            # Production: "0 8 * * *" (daily at 08:00 UTC)
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 0       # Don't retry failed jobs — surface failure to operator
      template:
        metadata:
          labels:
            phase: "08"
            trigger: k8s-cronjob
            track: track-a
        spec:
          restartPolicy: Never
          containers:
            - name: hermes-agent
              image: hermes-lab:cronjob
              imagePullPolicy: IfNotPresent   # REQUIRED for kind load docker-image workflow
              env:
                - name: HERMES_LAB_MODE
                  value: "mock"
                - name: HERMES_LAB_SCENARIO
                  value: "clean"
                - name: HERMES_LAB_GOVERNANCE
                  value: "L2"
                - name: HERMES_LAB_TRACK
                  value: "track-a"
                - name: MOCK_DATA_DIR
                  value: "/data/mock-data/database"
                - name: ANTHROPIC_API_KEY
                  valueFrom:
                    secretKeyRef:
                      name: hermes-secrets
                      key: anthropic-api-key
              command:
                - hermes
                - run
                - --skill
                - sre-dba-rds-slow-query
                - --prompt
                - "Run scheduled slow-query health check. Report only if queries over 1000ms are found."
              resources:
                requests:
                  cpu: 100m
                  memory: 256Mi
                limits:
                  cpu: 500m
                  memory: 1Gi
<!-- divider -->
# =========================================================================
# Track B (FinOps) — Daily EC2 health + cost check
# =========================================================================
apiVersion: batch/v1
kind: CronJob
metadata:
  name: hermes-track-b-health
  namespace: default
  labels:
    phase: "08"
    trigger: k8s-cronjob
    track: track-b
spec:
  schedule: "*/5 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 0
      template:
        metadata:
          labels:
            phase: "08"
            trigger: k8s-cronjob
            track: track-b
        spec:
          restartPolicy: Never
          containers:
            - name: hermes-agent
              image: hermes-lab:cronjob
              imagePullPolicy: IfNotPresent
              env:
                - name: HERMES_LAB_MODE
                  value: "mock"
                - name: HERMES_LAB_SCENARIO
                  value: "clean"
                - name: HERMES_LAB_GOVERNANCE
                  value: "L2"
                - name: HERMES_LAB_TRACK
                  value: "track-b"
                - name: MOCK_DATA_DIR
                  value: "/data/mock-data/finops"
                - name: ANTHROPIC_API_KEY
                  valueFrom:
                    secretKeyRef:
                      name: hermes-secrets
                      key: anthropic-api-key
              command:
                - hermes
                - run
                - --skill
                - sre-ec2-health-check
                - --prompt
                - "Run scheduled EC2 cost and health check. Report anomalies only."
              resources:
                requests:
                  cpu: 100m
                  memory: 256Mi
                limits:
                  cpu: 500m
                  memory: 1Gi
<!-- divider -->
# =========================================================================
# Track C (Kubernetes) — Pod health check, fires on Phase 6 crashloop2 scenario
# =========================================================================
apiVersion: batch/v1
kind: CronJob
metadata:
  name: hermes-track-c-health
  namespace: default
  labels:
    phase: "08"
    trigger: k8s-cronjob
    track: track-c
spec:
  schedule: "*/5 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 0
      template:
        metadata:
          labels:
            phase: "08"
            trigger: k8s-cronjob
            track: track-c
        spec:
          restartPolicy: Never
          containers:
            - name: hermes-agent
              image: hermes-lab:cronjob
              imagePullPolicy: IfNotPresent
              env:
                - name: HERMES_LAB_MODE
                  value: "mock"
                - name: HERMES_LAB_SCENARIO
                  value: "crashloop2"
                - name: HERMES_LAB_GOVERNANCE
                  value: "L2"
                - name: HERMES_LAB_TRACK
                  value: "track-c"
                - name: MOCK_DATA_DIR
                  value: "/data/mock-data/kubernetes"
                - name: ANTHROPIC_API_KEY
                  valueFrom:
                    secretKeyRef:
                      name: hermes-secrets
                      key: anthropic-api-key
              command:
                - hermes
                - run
                - --skill
                - sre-k8s-pod-health
                - --prompt
                - "Scheduled K8s pod health check on namespace k8s-trouble-crashloop. Report only if pods show errors or restart counts exceed 2."
              resources:
                requests:
                  cpu: 100m
                  memory: 256Mi
                limits:
                  cpu: 500m
                  memory: 1Gi
```

**3. Create `infrastructure/scenarios/k8s/cronjob/README.md`** — "Use this when" decision criteria per D-10, plus build/apply instructions. Use this exact content:

```markdown
# K8s CronJob — Scheduled Agent via Kubernetes Primitives

**Phase 8 / TRIG-02** — Demonstrates running a scheduled Hermes agent as a native K8s CronJob resource, for comparison with the Hermes-cron primary pattern taught in Module 12 Steps 2-4.

## When to use which pattern

Module 12 teaches **both** Hermes cron and K8s CronJob because they serve different use cases. Neither is universally better — pick based on what your agent actually needs.

### Use Hermes cron when:

- **The agent benefits from gateway-shared state** — loaded skills, audit trail, conversation history, config hot-reload. Hermes cron lives inside the gateway process and has immediate access to all of this.
- **You want one-stop CLI management** — `hermes cron create/list/trigger/pause/resume` is the whole API. No kubectl context switching.
- **You're iterating fast** — tweak a prompt, re-register the cron, done. No image rebuild, no kubectl apply cycle.
- **You need audit trail context** — Hermes sessions link cron runs to the skill and prompt version. K8s CronJob history is just pod logs.
- **You're not (yet) in Kubernetes** — Hermes cron runs wherever the gateway runs. Laptops, VMs, bare-metal servers.

### Use K8s CronJob when:

- **Stateless one-shot diagnostics** — the agent doesn't need any state carried from previous runs.
- **GitOps-managed schedules** — you want the schedule and prompt in git, reviewed via PR, deployed via ArgoCD or Flux. `CronJob` YAML fits this perfectly.
- **K8s-native observability** — you want Prometheus `kube_job_status_*` metrics, `kubectl get jobs`, pod log aggregation via Loki/Vector. All free with K8s.
- **Multi-tenant / multi-team environments** — namespace isolation, NetworkPolicies, resource quotas, Secrets, ServiceAccounts. All native K8s primitives.
- **You want declarative scaling** — `parallelism`, `completions`, `backoffLimit`, `ttlSecondsAfterFinished` — all on the K8s object.

### Real-world honest stance

Most agent work uses **Hermes cron** because state matters. The skill is already loaded in the gateway, the conversation history from yesterday's run informs today's run, and managing schedules alongside manual runs is frictionless.

**K8s CronJob shines for fire-and-forget diagnostic jobs** — the kind where you never need to correlate today's run with yesterday's, you just want "is the cluster OK?" dropped into a ticket at 08:00 UTC every day, deployed via the same GitOps pipeline as everything else.

## Files

- `Dockerfile` — Minimal hermes-agent container image (python:3.12-slim based)
- `agent-health-check.yaml` — Three per-track CronJob manifests (Track A/B/C)
- `README.md` — This file

## Build & apply

```bash
# 1. Build the minimal hermes-agent image (~700-900MB, mostly Python deps)
docker build -t hermes-lab:cronjob infrastructure/scenarios/k8s/cronjob/

# 2. Load the image into KIND (required because CronJob uses IfNotPresent pull policy)
kind load docker-image hermes-lab:cronjob --name lab

# 3. Create the Anthropic API key secret (never commit tokens to git)
kubectl create secret generic hermes-secrets \
  --from-literal=anthropic-api-key="$ANTHROPIC_API_KEY"

# 4. Apply ONE track's CronJob (pick your track)
kubectl apply -f infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml -l track=track-c

# 5. Watch jobs spawn
watch kubectl get jobs,pods

# 6. View logs from the latest job
kubectl logs -l job-name=$(kubectl get jobs -o jsonpath='{.items[-1].metadata.name}')
```

## Phase 7 governance inheritance

The CronJob container sets `HERMES_LAB_GOVERNANCE=L2` and `HERMES_LAB_TRACK=track-*`. These env vars propagate into the container, and the Phase 7 wrappers (`infrastructure/wrappers/mock-kubectl`, `mock-aws`, `mock-psql`) read them to enforce the `wrapper_allowlist` from the corresponding `governance/governance-L2.yaml` (or track-specific file).

**Teaching point:** Scheduled agents running unattended are MORE dangerous than interactive ones because no human is watching. Phase 7 governance applies automatically, so a scheduled agent inherits the same "delete/drain/exec" prohibitions as its interactive counterpart. There is no special "scheduled agent" governance — it's the same L1-L4 progression, just dispatched differently.

## Cleanup

```bash
kubectl delete -f infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml
kubectl delete secret hermes-secrets
```

## Why `imagePullPolicy: IfNotPresent` is required

Without it, each CronJob run attempts to pull `hermes-lab:cronjob` from a remote registry, which fails because the image was loaded locally via `kind load docker-image` and no registry entry exists. See 08-RESEARCH.md Pitfall 4.

## Why the image is ~700-900MB, not <100MB

hermes-agent has transitive dependencies on `python-telegram-bot`, `aiohttp`, `pydantic`, `openai`, `anthropic`, and the full `[messaging,cron]` extras set. The official image (`nousresearch/hermes-agent:latest`) is ~2-3GB because it also bundles Playwright, ffmpeg, Node.js, and Chromium for Hermes's web-automation use cases. This CronJob Dockerfile strips those out since a diagnostic agent doesn't need a headless browser.
```
  </action>

  <acceptance_criteria>
    - `test -f infrastructure/scenarios/k8s/cronjob/Dockerfile` (file exists)
    - `grep -q 'FROM python:3.12-slim' infrastructure/scenarios/k8s/cronjob/Dockerfile` (correct base image)
    - `grep -q 'hermes-agent\[messaging,cron\]' infrastructure/scenarios/k8s/cronjob/Dockerfile` (correct extras)
    - `grep -q 'git+https://github.com/NousResearch/hermes-agent.git' infrastructure/scenarios/k8s/cronjob/Dockerfile` (GitHub source install per RESEARCH)
    - `grep -q 'ENTRYPOINT \["hermes"\]' infrastructure/scenarios/k8s/cronjob/Dockerfile` (correct entrypoint)
    - `test -f infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml` (file exists)
    - Multi-doc YAML parses: `yq eval-all '. | length' infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml` returns `3` or more (3 docs)
    - `grep -c 'kind: CronJob' infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml` returns `3` (three CronJob resources — one per track)
    - `grep -q 'hermes-track-a-health' infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml` (Track A present)
    - `grep -q 'hermes-track-b-health' infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml` (Track B present)
    - `grep -q 'hermes-track-c-health' infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml` (Track C present)
    - `grep -c 'imagePullPolicy: IfNotPresent' infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml` returns `3` (Pitfall 4 prevention on all 3 tracks)
    - `grep -c 'HERMES_LAB_GOVERNANCE' infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml` returns `3` (governance inheritance on all 3 tracks)
    - `grep -c 'HERMES_LAB_TRACK' infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml` returns `3`
    - `grep -q 'crashloop2' infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml` (Track C uses the Phase 6 crashloop2 scenario)
    - `grep -q 'sre-k8s-pod-health' infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml` (Track C references Phase 6 skill)
    - `grep -q 'sre-dba-rds-slow-query' infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml` (Track A references Module 10 skill)
    - `grep -q 'sre-ec2-health-check' infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml` (Track B references Module 10 skill)
    - `test -f infrastructure/scenarios/k8s/cronjob/README.md` (file exists)
    - `grep -q 'Use Hermes cron when' infrastructure/scenarios/k8s/cronjob/README.md` (D-10 use-this-when present)
    - `grep -q 'Use K8s CronJob when' infrastructure/scenarios/k8s/cronjob/README.md` (D-10 use-this-when present)
    - `grep -q 'kind load docker-image' infrastructure/scenarios/k8s/cronjob/README.md` (correct KIND workflow documented)
    - `grep -q 'imagePullPolicy: IfNotPresent' infrastructure/scenarios/k8s/cronjob/README.md` (Pitfall 4 explained)
    - `grep -q 'Phase 7 governance' infrastructure/scenarios/k8s/cronjob/README.md` (inheritance story told)
    - `yq eval-all . infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml > /dev/null` exits 0 (valid multi-doc YAML)
  </acceptance_criteria>

  <verify>
    <automated>yq eval-all . infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml > /dev/null && [ "$(grep -c 'kind: CronJob' infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml)" = "3" ] && [ "$(grep -c 'imagePullPolicy: IfNotPresent' infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml)" = "3" ] && grep -q 'FROM python:3.12-slim' infrastructure/scenarios/k8s/cronjob/Dockerfile && grep -q 'Use Hermes cron when' infrastructure/scenarios/k8s/cronjob/README.md && grep -q 'Use K8s CronJob when' infrastructure/scenarios/k8s/cronjob/README.md</automated>
  </verify>

  <done>
    All 3 files (Dockerfile, agent-health-check.yaml, README.md) are on disk. Dockerfile uses python:3.12-slim + git install. Manifest has 3 per-track CronJob resources with governance env vars and correct skill references. README documents use-this-when criteria from D-10 and the KIND load workflow with the imagePullPolicy rationale.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run these phase-level checks:

**File presence:**
```bash
ls infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml \
   infrastructure/scenarios/k8s/alertmanager/alertmanager-config.yaml \
   infrastructure/scenarios/k8s/alertmanager/README.md \
   infrastructure/scenarios/k8s/cronjob/Dockerfile \
   infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml \
   infrastructure/scenarios/k8s/cronjob/README.md
```

**YAML sanity on all new/modified YAMLs:**
```bash
for f in infrastructure/helm/prometheus-lab-values.yaml \
         infrastructure/kind/cluster-config.yaml \
         infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml \
         infrastructure/scenarios/k8s/alertmanager/alertmanager-config.yaml; do
  yq . "$f" > /dev/null && echo "OK: $f" || echo "FAIL: $f"
done
yq eval-all . infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml > /dev/null && echo "OK: cronjob manifest"
```

**Key invariants (the five "must-not-break" properties):**
```bash
# 1. alertmanager is enabled in helm values
grep -q 'enabled: true' infrastructure/helm/prometheus-lab-values.yaml

# 2. PrometheusRule has the kube-prometheus-stack label selector
grep -q 'release: kube-prometheus' infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml

# 3. Alert references the Phase 6 namespace
grep -q 'k8s-trouble-crashloop' infrastructure/scenarios/k8s/alertmanager/prometheus-rules.yaml

# 4. KIND cluster has port 8644
grep -q 'containerPort: 8644' infrastructure/kind/cluster-config.yaml

# 5. CronJob manifest has exactly 3 track variants, all with IfNotPresent and HERMES_LAB_GOVERNANCE
test "$(grep -c 'kind: CronJob' infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml)" = "3"
test "$(grep -c 'imagePullPolicy: IfNotPresent' infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml)" = "3"
test "$(grep -c 'HERMES_LAB_GOVERNANCE' infrastructure/scenarios/k8s/cronjob/agent-health-check.yaml)" = "3"

# 6. Array index anti-pattern absent from all Phase 8 files
! grep -rF '{alerts[0]' infrastructure/scenarios/k8s/alertmanager/ infrastructure/scenarios/k8s/cronjob/
```

**Docker build smoke test (optional — requires Docker running):**
```bash
docker build -t hermes-lab:cronjob-test infrastructure/scenarios/k8s/cronjob/ && docker images | grep hermes-lab && docker rmi hermes-lab:cronjob-test
```

If Docker is not running, skip — the Dockerfile is syntactically validated by the file creation itself and Plan 08-03 will exercise the actual build as part of lab verification.
</verification>

<success_criteria>
- All 8 files (2 modified + 6 new) on disk
- All YAML files parse successfully with `yq`
- PrometheusRule includes `release: kube-prometheus` label
- K8s CronJob manifest has 3 per-track variants (A/B/C)
- All CronJob variants include `imagePullPolicy: IfNotPresent` and `HERMES_LAB_GOVERNANCE` env var
- AlertManager receiver URL uses `host.docker.internal:8644`
- Zero occurrences of the `{alerts[0]` array-index anti-pattern anywhere in Phase 8 files
- Dockerfile uses `python:3.12-slim` base + GitHub source install (not `pip install hermes-agent` which would fail — not on PyPI)
- README files document the flow, use-this-when criteria (D-10), and Phase 7 governance inheritance
</success_criteria>

<output>
After completion, create `.planning/phases/08-agent-triggers/08-01-SUMMARY.md` following the standard summary template. Include:
- Files created/modified with sizes
- Key decisions honored (D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-22, D-26)
- Research findings incorporated (BLOCKER-02 Option A resolution, Pitfall 1 label selector, Pitfall 4 imagePullPolicy, prompt template array syntax constraint)
- Verification results (yq parses, grep checks, optional docker build smoke test)
- Any open questions that emerged during execution (e.g., ruleSelector label needs verification against actual helm release name)
</output>
