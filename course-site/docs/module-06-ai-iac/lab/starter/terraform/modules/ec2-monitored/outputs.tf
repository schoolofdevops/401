# TODO: Add outputs for:
# - instance_id (from aws_instance.this.id)
# - instance_public_ip (from aws_instance.this.public_ip)
# - alarm_arn (from aws_cloudwatch_metric_alarm.cpu_high.arn)
# - sns_topic_arn (from aws_sns_topic.alerts.arn)
