# ArgoCD Application CRD for the reference app
# TODO: Use AI to fill in the complete specification
# Context: See CLAUDE.md in this directory before generating
#
# IMPORTANT: ArgoCD pulls from Git, not your local filesystem.
# You must fork the course repo and use your fork URL below.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: reference-app
  namespace: argocd
spec:
  project: default
  source:
    # TODO: Replace with your GitHub fork URL
    repoURL: https://github.com/PARTICIPANT/agentic-devops-course
    targetRevision: HEAD
    path: reference-app/helm/reference-app
    # TODO: Add Helm configuration (releaseName, valueFiles)
  destination:
    # TODO: Add cluster server and target namespace
  # TODO: Add syncPolicy with automated prune and selfHeal
